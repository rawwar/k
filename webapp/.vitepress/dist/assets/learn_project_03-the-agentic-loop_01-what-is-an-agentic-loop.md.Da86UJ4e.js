import{_ as t,o,c as n,a2 as a}from"./chunks/framework.B72_uj9U.js";const u=JSON.parse('{"title":"What Is an Agentic Loop","description":"Define the agentic loop pattern and explain how it differs from simple request-response chatbot interactions.","frontmatter":{"title":"What Is an Agentic Loop","description":"Define the agentic loop pattern and explain how it differs from simple request-response chatbot interactions."},"headers":[],"relativePath":"learn/project/03-the-agentic-loop/01-what-is-an-agentic-loop.md","filePath":"learn/project/03-the-agentic-loop/01-what-is-an-agentic-loop.md"}'),s={name:"learn/project/03-the-agentic-loop/01-what-is-an-agentic-loop.md"};function l(i,e,r,p,c,h){return o(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="what-is-an-agentic-loop" tabindex="-1">What Is an Agentic Loop <a class="header-anchor" href="#what-is-an-agentic-loop" aria-label="Permalink to &quot;What Is an Agentic Loop&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>The fundamental difference between a chatbot (single turn) and an agent (multi-turn with tool use)</li><li>How the prompt-call-execute-observe cycle enables autonomous task completion</li><li>Why the loop pattern is the backbone of every modern coding agent from Claude Code to Cursor</li></ul></blockquote><p>In Chapter 2 you built something that can talk to an LLM: your CLI sends a message, the API returns a response, and you print it. That is a chatbot. It works, but it is fundamentally limited. If you ask it to &quot;create a file called hello.rs with a main function,&quot; it will write out the file contents in its reply -- but it cannot actually create the file. It can describe what to do, but it cannot <em>do</em> anything.</p><p>An agentic loop changes this. Instead of a single request-response exchange, the agent runs a <em>loop</em>. It sends the user&#39;s prompt to the model. The model responds, and that response might contain a tool-use request -- &quot;I want to call the <code>write_file</code> tool with these arguments.&quot; The agent executes that tool, collects the result (the <em>observation</em>), feeds it back into the conversation, and calls the model again. The model sees the observation, decides whether to call another tool or respond to the user, and the cycle repeats.</p><p>This is the <strong>prompt-call-execute-observe</strong> cycle, and it is the single most important pattern in this entire book.</p><h2 id="from-chatbot-to-agent" tabindex="-1">From Chatbot to Agent <a class="header-anchor" href="#from-chatbot-to-agent" aria-label="Permalink to &quot;From Chatbot to Agent&quot;">​</a></h2><p>Let&#39;s make this concrete. Imagine you type: &quot;Read the file src/main.rs and add error handling to the unwrap calls.&quot;</p><p>A <strong>chatbot</strong> would respond with something like: &quot;Here&#39;s how you could modify the file...&quot; and show you code in its reply. You would then copy that code, paste it into your editor, and hope it works.</p><p>An <strong>agent</strong> running an agentic loop would:</p><ol><li>Call the <code>read_file</code> tool to get the current contents of <code>src/main.rs</code></li><li>Receive the file contents as an observation</li><li>Analyze the code and decide what changes to make</li><li>Call the <code>write_file</code> tool to write the updated version</li><li>Receive confirmation that the file was written</li><li>Optionally call a <code>run_command</code> tool to run <code>cargo check</code> and verify the changes compile</li><li>Receive the compilation output</li><li>Report back to the user: &quot;I&#39;ve updated src/main.rs to replace the unwrap calls with proper error handling. The project compiles successfully.&quot;</li></ol><p>That sequence involved <em>four</em> LLM calls and <em>three</em> tool executions, all happening automatically within a single user request. The user typed one message and got back a completed task. That is the power of the agentic loop.</p><h2 id="the-cycle-in-pseudocode" tabindex="-1">The Cycle in Pseudocode <a class="header-anchor" href="#the-cycle-in-pseudocode" aria-label="Permalink to &quot;The Cycle in Pseudocode&quot;">​</a></h2><p>Before we touch any Rust, let&#39;s see the loop in plain pseudocode:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>messages = [system_prompt, user_message]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>loop {</span></span>
<span class="line"><span>    response = call_llm(messages)</span></span>
<span class="line"><span>    append response to messages</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    if response.stop_reason == &quot;end_turn&quot; {</span></span>
<span class="line"><span>        // Model is done, show result to user</span></span>
<span class="line"><span>        break</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    if response.stop_reason == &quot;tool_use&quot; {</span></span>
<span class="line"><span>        for each tool_call in response {</span></span>
<span class="line"><span>            result = execute_tool(tool_call.name, tool_call.input)</span></span>
<span class="line"><span>            append tool_result to messages</span></span>
<span class="line"><span>        }</span></span>
<span class="line"><span>        // Continue the loop -- model needs to see the results</span></span>
<span class="line"><span>    }</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>That is the entire pattern. Everything else in this chapter -- message types, conversation state, turn management, stop conditions -- is about implementing this loop correctly, efficiently, and robustly in Rust.</p><h2 id="why-a-loop-and-not-a-pipeline" tabindex="-1">Why a Loop and Not a Pipeline? <a class="header-anchor" href="#why-a-loop-and-not-a-pipeline" aria-label="Permalink to &quot;Why a Loop and Not a Pipeline?&quot;">​</a></h2><p>You might wonder: why not design this as a pipeline or a chain of fixed steps? Some frameworks do take that approach. LangChain, for example, lets you define &quot;chains&quot; where each step feeds into the next. But a chain requires you to know the steps in advance. The whole point of an agentic loop is that the <em>model</em> decides what to do next. It might need one tool call or ten. It might realize halfway through that it needs to read a different file than the one it started with. The loop gives the model the freedom to adapt.</p><p>This is also why the loop is <em>tool-agnostic</em>. The loop itself does not know about file operations or shell commands or code search. It only knows three things: how to call the LLM, how to detect tool-use requests in the response, and how to feed observations back. The specific tools are plugged in separately (that is Chapter 4). This separation is what makes the architecture extensible.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>If you have used Python agent frameworks, the agentic loop might feel familiar. In LangChain you would write an <code>AgentExecutor</code> that calls <code>.invoke()</code> in a loop. In a raw Python implementation, you would write:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">while</span><span style="color:#CBA6F7;"> True</span><span style="color:#9399B2;">:</span></span>
<span class="line"><span style="color:#CDD6F4;">    response </span><span style="color:#94E2D5;">=</span><span style="color:#CDD6F4;"> client</span><span style="color:#9399B2;">.</span><span style="color:#CDD6F4;">messages</span><span style="color:#9399B2;">.</span><span style="color:#89B4FA;">create</span><span style="color:#9399B2;">(</span><span style="color:#EBA0AC;font-style:italic;">model</span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;">&quot;claude-sonnet-4-20250514&quot;</span><span style="color:#9399B2;">,</span><span style="color:#EBA0AC;font-style:italic;"> messages</span><span style="color:#94E2D5;">=</span><span style="color:#CDD6F4;">messages</span><span style="color:#9399B2;">)</span></span>
<span class="line"><span style="color:#CDD6F4;">    messages</span><span style="color:#9399B2;">.</span><span style="color:#89B4FA;">append</span><span style="color:#9399B2;">({</span><span style="color:#A6E3A1;">&quot;role&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;assistant&quot;</span><span style="color:#9399B2;">,</span><span style="color:#A6E3A1;"> &quot;content&quot;</span><span style="color:#9399B2;">:</span><span style="color:#CDD6F4;"> response</span><span style="color:#9399B2;">.</span><span style="color:#CDD6F4;">content</span><span style="color:#9399B2;">})</span></span>
<span class="line"><span style="color:#CBA6F7;">    if</span><span style="color:#CDD6F4;"> response</span><span style="color:#9399B2;">.</span><span style="color:#CDD6F4;">stop_reason </span><span style="color:#94E2D5;">==</span><span style="color:#A6E3A1;"> &quot;end_turn&quot;</span><span style="color:#9399B2;">:</span></span>
<span class="line"><span style="color:#CBA6F7;">        break</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">    # extract tool calls, execute them, append results</span></span></code></pre></div><p>The Rust version follows the same structure, but with stronger typing. Instead of dictionaries and string matching, you will use enums and pattern matching, which means the compiler catches errors like forgetting to handle a message type.</p></div><h2 id="the-loop-in-every-production-agent" tabindex="-1">The Loop in Every Production Agent <a class="header-anchor" href="#the-loop-in-every-production-agent" aria-label="Permalink to &quot;The Loop in Every Production Agent&quot;">​</a></h2><p>This is not an academic exercise. Every production coding agent uses some variation of this loop:</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Claude Code, Anthropic&#39;s own CLI coding agent, runs an agentic loop at its core. When you give it a task, it enters a loop that calls the Claude API, checks for tool use in the response, executes tools (file reads, writes, shell commands), feeds the results back, and continues until the model signals it is done. OpenCode, an open-source Go-based agent, uses the same fundamental pattern with a <code>for</code> loop that alternates between LLM calls and tool dispatch. Codex from OpenAI follows the identical cycle. The specifics differ -- how they handle streaming, what safety checks they add, how they manage context -- but the core loop is the same everywhere.</p></div><h2 id="what-you-are-building" tabindex="-1">What You Are Building <a class="header-anchor" href="#what-you-are-building" aria-label="Permalink to &quot;What You Are Building&quot;">​</a></h2><p>Over the next eleven subchapters, you will implement the complete agentic loop for your coding agent. Here is the roadmap:</p><ul><li><strong>Architecture</strong>: How the loop is structured as a state machine with clear phases</li><li><strong>Message types</strong>: The Rust enums and structs that represent every kind of message in the conversation</li><li><strong>Conversation state</strong>: The <code>Vec&lt;Message&gt;</code> that grows with each iteration and forms the model&#39;s context</li><li><strong>Turn management</strong>: Counting iterations and enforcing limits so the loop cannot run forever</li><li><strong>Stop conditions</strong>: Detecting when the model is done, when limits are hit, or when something goes wrong</li><li><strong>The core loop</strong>: The actual <code>async fn agent_loop</code> implementation in Rust</li><li><strong>Tool call handling</strong>: Extracting tool-use requests from the model&#39;s response and routing them</li><li><strong>Observation feeding</strong>: Packaging tool results and injecting them back into the conversation</li><li><strong>Single vs multi-turn</strong>: Understanding when the loop iterates once versus many times</li><li><strong>Debugging</strong>: Logging, tracing, and diagnosing when the loop goes wrong</li></ul><p>By the end of this chapter, your agent will have a complete, working agentic loop. The tool execution will be stubbed -- returning placeholder results -- but the loop structure will be fully operational. Chapter 4 will fill in the stubs with real tool implementations.</p><p>Let&#39;s start by looking at the architecture of the loop.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>An agentic loop transforms a single-turn chatbot into a multi-turn autonomous agent that can take actions and observe their results</li><li>The core cycle is <strong>prompt-call-execute-observe</strong>: send messages to the LLM, detect tool-use requests, execute tools, feed results back, repeat</li><li>The loop is tool-agnostic by design -- it only knows how to call the LLM and detect whether the response requires action, making it extensible to any set of tools</li><li>Every production coding agent (Claude Code, OpenCode, Codex) uses this same fundamental pattern</li><li>The model, not the programmer, decides what to do next -- the loop gives the model freedom to adapt its approach as it discovers new information</li></ul>`,29)])])}const g=t(s,[["render",l]]);export{u as __pageData,g as default};
