import{_ as t,o,c as n,a2 as s}from"./chunks/framework.B72_uj9U.js";const u=JSON.parse('{"title":"Summary","description":"A recap of LLM fundamentals for agent builders, from tokenization through API design to prompt engineering best practices.","frontmatter":{"title":"Summary","description":"A recap of LLM fundamentals for agent builders, from tokenization through API design to prompt engineering best practices."},"headers":[],"relativePath":"learn/linear/03-understanding-llms/16-summary.md","filePath":"learn/linear/03-understanding-llms/16-summary.md"}'),a={name:"learn/linear/03-understanding-llms/16-summary.md"};function i(r,e,l,c,d,p){return o(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>A consolidated reference of the LLM concepts most critical for agent implementation</li><li>The key API differences between Anthropic and OpenAI that affect provider abstraction design</li><li>How the LLM knowledge from this chapter directly informs the agentic loop design in Chapter 4</li></ul></blockquote><p>This chapter covered a lot of ground -- from how tokens work to how to structure system prompts for reliable tool use. Let&#39;s consolidate the key concepts you will use directly as you build your coding agent. This is not a repetition of every detail; it is a distillation of the practical knowledge that shapes implementation decisions.</p><h2 id="the-llm-as-a-component" tabindex="-1">The LLM as a Component <a class="header-anchor" href="#the-llm-as-a-component" aria-label="Permalink to &quot;The LLM as a Component&quot;">​</a></h2><p>The core mental model: an LLM is a <strong>stateless text-processing function</strong>. You send it a sequence of messages, and it returns a continuation. It has no memory between calls. It does not execute code. It does not access the internet. Everything it &quot;knows&quot; comes from its training data and the context you provide in each request.</p><p>This means your agent must:</p><ul><li><strong>Manage conversation history</strong> -- accumulate messages across turns and send the full history on each call</li><li><strong>Provide tools</strong> -- give the model structured ways to interact with the environment</li><li><strong>Feed back results</strong> -- send tool execution results back so the model can reason about them</li><li><strong>Handle the output</strong> -- parse responses, dispatch tool calls, and detect when the task is done</li></ul><p>The model handles the intelligence (deciding what to do, generating code, reasoning about errors). Your agent handles everything else (execution, state management, I/O, safety).</p><h2 id="tokenization-and-context-the-constraints" tabindex="-1">Tokenization and Context: The Constraints <a class="header-anchor" href="#tokenization-and-context-the-constraints" aria-label="Permalink to &quot;Tokenization and Context: The Constraints&quot;">​</a></h2><p>Every design decision in your agent is constrained by tokens and context:</p><ul><li><p><strong>Tokens</strong> are the unit of measurement for everything: input, output, cost, and rate limits. Code tokenizes at roughly 3-3.5 characters per token. JSON is more expensive (more structural characters). Tool definitions cost 100-300 tokens each.</p></li><li><p><strong>Context windows</strong> set the hard ceiling on what the model can consider in a single request. With a 200K context window, you have space for substantial conversation history, but it fills faster than you expect when tool results include full files and shell output.</p></li><li><p><strong>Output limits</strong> restrict how much the model can generate in one response (separate from the context window). Check <code>stop_reason</code> to detect when the model was cut off.</p></li></ul><p>The practical implication: your agent needs a context management strategy from day one. Track token usage from API responses. Truncate large tool results. Plan for summarization or compaction when conversations get long. These are not optimizations -- they are core features.</p><h2 id="the-tool-use-protocol-how-agents-act" tabindex="-1">The Tool Use Protocol: How Agents Act <a class="header-anchor" href="#the-tool-use-protocol-how-agents-act" aria-label="Permalink to &quot;The Tool Use Protocol: How Agents Act&quot;">​</a></h2><p>The tool use protocol is the mechanism that transforms a text generator into an agent. The lifecycle is:</p><ol><li><strong>Define</strong> tools with name, description, and JSON Schema in the API request</li><li><strong>Receive</strong> tool calls from the model (structured JSON with tool name and arguments)</li><li><strong>Execute</strong> the tool in your agent code (your safety checks, sandboxing, and logic)</li><li><strong>Return</strong> results linked by ID to the original tool call</li></ol><p>Your agent loop checks the <code>stop_reason</code> on every response:</p><ul><li><code>tool_use</code> / <code>tool_calls</code> -- execute tools, append results, make another API call</li><li><code>end_turn</code> / <code>stop</code> -- display the response, wait for user input</li><li><code>max_tokens</code> / <code>length</code> -- handle truncation</li></ul><p>This loop is the agentic loop, and you will build it in the next chapter.</p><h3 id="tool-description-quality-matters" tabindex="-1">Tool Description Quality Matters <a class="header-anchor" href="#tool-description-quality-matters" aria-label="Permalink to &quot;Tool Description Quality Matters&quot;">​</a></h3><p>The tool description is the model&#39;s primary input for deciding when and how to use each tool. Invest in clear descriptions that include:</p><ul><li>What the tool does and what it returns</li><li>When to use it (and when not to)</li><li>Common usage patterns</li></ul><p>Bad description: &quot;Runs a command&quot; Good description: &quot;Execute a shell command and return stdout, stderr, and exit code. Use for builds (cargo check), tests (cargo test), search (grep, ripgrep), and version control (git). Do not use for reading file contents (use read_file instead).&quot;</p><h2 id="provider-differences-the-abstraction-challenge" tabindex="-1">Provider Differences: The Abstraction Challenge <a class="header-anchor" href="#provider-differences-the-abstraction-challenge" aria-label="Permalink to &quot;Provider Differences: The Abstraction Challenge&quot;">​</a></h2><p>Building a provider-agnostic agent requires handling these structural differences:</p><table tabindex="0"><thead><tr><th>Aspect</th><th>Anthropic</th><th>OpenAI</th></tr></thead><tbody><tr><td>Authentication</td><td><code>x-api-key</code> header</td><td><code>Authorization: Bearer</code> header</td></tr><tr><td>System prompt</td><td>Top-level <code>system</code> field</td><td><code>role: &quot;system&quot;</code> message</td></tr><tr><td>Tool definitions</td><td><code>input_schema</code> key</td><td><code>function.parameters</code> key, wrapped in <code>{&quot;type&quot;:&quot;function&quot;}</code></td></tr><tr><td>Tool calls</td><td>Content blocks in <code>content</code> array</td><td>Separate <code>tool_calls</code> field</td></tr><tr><td>Tool arguments</td><td>Parsed JSON object (<code>input</code>)</td><td>JSON string (<code>arguments</code>)</td></tr><tr><td>Tool results</td><td><code>tool_result</code> block in user message</td><td>Separate message with <code>role: &quot;tool&quot;</code></td></tr><tr><td>Stop signal</td><td><code>stop_reason: &quot;tool_use&quot;</code></td><td><code>finish_reason: &quot;tool_calls&quot;</code></td></tr><tr><td>Token usage</td><td><code>input_tokens</code> / <code>output_tokens</code></td><td><code>prompt_tokens</code> / <code>completion_tokens</code></td></tr></tbody></table><p>Your internal data model should use a unified representation:</p><div class="language-rust"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">enum</span><span style="color:#F9E2AF;font-style:italic;"> ContentBlock</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">    Text</span><span style="color:#9399B2;">(</span><span style="color:#F9E2AF;font-style:italic;">String</span><span style="color:#9399B2;">),</span></span>
<span class="line"><span style="color:#F9E2AF;font-style:italic;">    ToolUse</span><span style="color:#9399B2;"> {</span><span style="color:#CDD6F4;"> id</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> String</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> name</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> String</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> input</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> Value</span><span style="color:#9399B2;"> },</span></span>
<span class="line"><span style="color:#F9E2AF;font-style:italic;">    ToolResult</span><span style="color:#9399B2;"> {</span><span style="color:#CDD6F4;"> tool_use_id</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> String</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> content</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> String</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> is_error</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> bool</span><span style="color:#9399B2;"> },</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span>
<span class="line"></span>
<span class="line"><span style="color:#CBA6F7;">struct</span><span style="color:#F9E2AF;font-style:italic;"> Message</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#CDD6F4;">    role</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> Role</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#CDD6F4;">    content</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> Vec</span><span style="color:#89DCEB;">&lt;</span><span style="color:#F9E2AF;font-style:italic;">ContentBlock</span><span style="color:#89DCEB;">&gt;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>Then implement serialization to each provider&#39;s format at the API boundary. The core agent loop works with the unified representation and never sees provider-specific details.</p><h2 id="streaming-why-and-how" tabindex="-1">Streaming: Why and How <a class="header-anchor" href="#streaming-why-and-how" aria-label="Permalink to &quot;Streaming: Why and How&quot;">​</a></h2><p>Streaming delivers tokens incrementally over SSE (Server-Sent Events), starting within hundreds of milliseconds. For agents, streaming provides:</p><ul><li><strong>Responsiveness</strong> -- users see the model&#39;s reasoning as it generates</li><li><strong>Cancellation</strong> -- users can stop a response going in the wrong direction</li><li><strong>Progress visibility</strong> -- long operations show continuous activity</li></ul><p>The implementation challenge: text deltas can be displayed immediately, but tool call JSON must be accumulated until the content block completes. You cannot parse <code>{&quot;pa</code> -- you need the full <code>{&quot;path&quot;: &quot;src/main.rs&quot;}</code>.</p><p>Both providers use SSE but with different event structures. Anthropic uses typed events (<code>content_block_start</code>, <code>content_block_delta</code>, <code>content_block_stop</code>). OpenAI uses flat <code>data:</code> lines with JSON chunks. Your streaming parser handles this difference behind the provider abstraction.</p><h2 id="prompt-engineering-guiding-agent-behavior" tabindex="-1">Prompt Engineering: Guiding Agent Behavior <a class="header-anchor" href="#prompt-engineering-guiding-agent-behavior" aria-label="Permalink to &quot;Prompt Engineering: Guiding Agent Behavior&quot;">​</a></h2><p>System prompt engineering for agents focuses on:</p><ol><li><strong>Tool usage rules</strong> -- when to use each tool, mandatory verification steps</li><li><strong>Workflow patterns</strong> -- step-by-step procedures for common tasks</li><li><strong>Error recovery</strong> -- what to do when tool calls fail</li><li><strong>Output formatting</strong> -- how verbose to be, when to show code</li><li><strong>Safety constraints</strong> -- forbidden operations, confirmation requirements</li></ol><p>The process is iterative: start minimal, observe failures, add targeted rules, test regressions. Keep the system prompt between 500-1,500 tokens for the static portion.</p><p>Key techniques:</p><ul><li>Explicit &quot;Do NOT&quot; instructions prevent known failure modes</li><li>&quot;Good approach / Bad approach&quot; examples are highly effective</li><li>Think-before-acting instructions improve tool call quality</li><li>Error recovery instructions prevent premature task abandonment</li></ul><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>As a Python developer, you are used to flexible, dynamically-typed APIs where you can iterate quickly. The Rust agent you are about to build will use strongly-typed structs for messages, tool definitions, and API responses. This might feel like more upfront work, but it pays off: the compiler catches malformed tool results, missing fields, and type mismatches at build time rather than in production. The serde library makes JSON serialization and deserialization ergonomic, and Rust enums are perfect for representing the variant types (text blocks, tool calls, tool results) in the message protocol.</p></div><h2 id="cost-and-performance-operational-concerns" tabindex="-1">Cost and Performance: Operational Concerns <a class="header-anchor" href="#cost-and-performance-operational-concerns" aria-label="Permalink to &quot;Cost and Performance: Operational Concerns&quot;">​</a></h2><p>Agent operations compound costs because:</p><ul><li>Each API call resends the full conversation history (growing input tokens)</li><li>Output tokens cost 3-5x more than input tokens</li><li>A single task may involve 5-30 API calls</li></ul><p>Optimize with:</p><ul><li><strong>Prompt caching</strong> -- reduces cost of repetitive system prompt and tool definitions</li><li><strong>Model routing</strong> -- cheaper models for simple operations, frontier models for complex reasoning</li><li><strong>Context compaction</strong> -- summarize old tool results, truncate large outputs</li><li><strong>Token tracking</strong> -- monitor usage and display costs to the user</li></ul><p>Implement retry logic with exponential backoff and jitter for rate limits (HTTP 429) and overloaded conditions (HTTP 529/503).</p><h2 id="what-comes-next" tabindex="-1">What Comes Next <a class="header-anchor" href="#what-comes-next" aria-label="Permalink to &quot;What Comes Next&quot;">​</a></h2><p>With this understanding of how LLMs work, how their APIs are structured, and how to guide their behavior through prompts and tool definitions, you are ready to build the agentic loop. In Chapter 4, you will implement the core loop that:</p><ol><li>Sends messages to the LLM</li><li>Parses the response</li><li>Detects and executes tool calls</li><li>Feeds results back</li><li>Continues until the task is complete</li></ol><p>Every concept from this chapter -- token management, message formatting, tool use protocol, streaming, and error handling -- will be applied directly in that implementation. The LLM knowledge you have built here is the foundation; the agentic loop is where it all comes together.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Every production coding agent -- Claude Code, OpenCode, Codex, and others -- implements the concepts covered in this chapter. They all manage conversation history, define tools with JSON Schema, parse tool calls from model responses, handle streaming, implement retry logic, and tune their system prompts iteratively. The differences between agents are in the details: which tools they provide, how they manage context, what safety constraints they enforce, and how they present results to the user. The fundamentals are universal, and they are what you now understand.</p></div><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><p>These exercises focus on building practical intuition about LLM behavior, token economics, and prompt design for agent systems. They are analytical rather than implementation-focused.</p><h3 id="exercise-1-token-estimation-challenge-easy" tabindex="-1">Exercise 1: Token Estimation Challenge (Easy) <a class="header-anchor" href="#exercise-1-token-estimation-challenge-easy" aria-label="Permalink to &quot;Exercise 1: Token Estimation Challenge (Easy)&quot;">​</a></h3><p>Estimate the token count for each of these inputs without using a tokenizer, then check your estimates against a real tokenizer (such as the Anthropic or OpenAI tokenizer playground):</p><ol><li>A 50-line Python function with docstrings and type hints</li><li>A JSON object with 10 key-value pairs (string keys, mixed value types)</li><li>A tool definition with name, description (3 sentences), and a schema with 4 parameters</li><li>A 200-word natural language paragraph explaining a bug</li></ol><p><strong>Deliverable:</strong> Your estimates, the actual counts, and a paragraph reflecting on which inputs you over/underestimated and why.</p><h3 id="exercise-2-tool-description-a-b-test-medium" tabindex="-1">Exercise 2: Tool Description A/B Test (Medium) <a class="header-anchor" href="#exercise-2-tool-description-a-b-test-medium" aria-label="Permalink to &quot;Exercise 2: Tool Description A/B Test (Medium)&quot;">​</a></h3><p>Write two versions of a tool description for a <code>search_code</code> tool -- one minimal (1-2 sentences) and one comprehensive (following the five-area template from this chapter: what, returns, when, when-not, edge cases). Then design three test scenarios where you predict the LLM would make different tool selection decisions based on which description it received.</p><p><strong>What to consider:</strong> Think about ambiguous scenarios where the model might confuse <code>search_code</code> with <code>read_file</code> or <code>run_command grep</code>. Consider what happens when the user asks to &quot;find all uses of this function&quot; -- does the model search or read? How does description quality affect this decision?</p><p><strong>Deliverable:</strong> Both descriptions, three test scenarios with predicted behavior differences, and an analysis of why description quality matters for agent reliability.</p><h3 id="exercise-3-cost-calculation-for-a-real-task-medium" tabindex="-1">Exercise 3: Cost Calculation for a Real Task (Medium) <a class="header-anchor" href="#exercise-3-cost-calculation-for-a-real-task-medium" aria-label="Permalink to &quot;Exercise 3: Cost Calculation for a Real Task (Medium)&quot;">​</a></h3><p>Calculate the total API cost for a coding agent completing this task: &quot;Add input validation to the user registration endpoint.&quot; Assume the agent needs to (1) read 3 files to understand the codebase, (2) reason about what validation to add, (3) edit 2 files, (4) run the test suite, and (5) fix one test failure. Estimate token counts for each API call (system prompt, conversation history growth, tool results, model output) and compute costs using current Anthropic pricing.</p><p><strong>What to consider:</strong> Remember that conversation history grows with each turn -- the fifth API call includes all previous messages. Factor in the system prompt and tool definitions being resent on every call. Consider how prompt caching affects the total cost.</p><p><strong>Deliverable:</strong> A breakdown table showing each API call&#39;s input tokens, output tokens, cost, and cumulative conversation size. Include a total cost and a comparison showing the cost with and without prompt caching.</p><h3 id="exercise-4-provider-abstraction-design-hard" tabindex="-1">Exercise 4: Provider Abstraction Design (Hard) <a class="header-anchor" href="#exercise-4-provider-abstraction-design-hard" aria-label="Permalink to &quot;Exercise 4: Provider Abstraction Design (Hard)&quot;">​</a></h3><p>Using the provider differences table from this chapter, design the type signatures (not implementations) for a provider abstraction layer. Define: a unified <code>Message</code> type, a <code>ToolCall</code> type, a <code>ToolResult</code> type, and a <code>Provider</code> trait with methods for sending messages and parsing responses. Then identify the three hardest provider differences to abstract over and explain your strategy for each.</p><p><strong>What to consider:</strong> Some differences are structural (system prompt as a field vs. a message), some are semantic (parsed JSON vs. JSON string for tool arguments), and some are in streaming behavior. Your abstraction needs to handle all three categories without leaking provider details into the agent loop.</p><p><strong>Deliverable:</strong> Rust type signatures for the unified types and the Provider trait, plus a written analysis of the three hardest abstraction challenges and your solutions.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>The LLM is a stateless text-processing function -- your agent provides all context on every call and manages state, execution, and safety around it</li><li>The tool use protocol (define, receive, execute, return) is the mechanism that transforms text generation into autonomous action, driven by the <code>stop_reason</code> field in the agent loop</li><li>Provider abstraction requires handling structural differences in authentication, system prompts, tool definitions, tool calls, and token reporting -- use a unified internal representation with provider-specific serialization</li><li>Prompt engineering for agents is iterative and focuses on tool usage rules, workflow patterns, error recovery, and preventing specific failure modes -- it is the highest-leverage ongoing maintenance activity</li><li>Start with Claude Sonnet as your default model, implement model routing for cost optimization, and build a task-specific benchmark suite to evaluate new models as they are released</li></ul>`,71)])])}const m=t(a,[["render",i]]);export{u as __pageData,m as default};
