import{_ as c,C as d,o,c as h,a2 as n,b as s,w as t,a as i,E as r,a3 as l}from"./chunks/framework.B72_uj9U.js";const A=JSON.parse('{"title":"Ante Agentic Loop","description":"","frontmatter":{"title":"Ante Agentic Loop","status":"complete"},"headers":[],"relativePath":"research/agents/ante/agentic-loop.md","filePath":"research/agents/ante/agentic-loop.md"}'),u={name:"research/agents/ante/agentic-loop.md"};function g(p,e,m,f,b,y){const a=d("Mermaid");return o(),h("div",null,[e[2]||(e[2]=n('<h1 id="ante-agentic-loop" tabindex="-1">Ante Agentic Loop <a class="header-anchor" href="#ante-agentic-loop" aria-label="Permalink to &quot;Ante Agentic Loop&quot;">​</a></h1><blockquote><p>Ante, by Antigma Labs, is a Rust-built terminal coding agent featuring a self-organizing multi-agent architecture. Its agentic loop diverges significantly from the single-agent paradigm common in most coding assistants. Instead of one agent iterating in a plan-act-observe cycle, Ante introduces a <strong>meta-agent orchestrator</strong> that decomposes tasks and delegates to concurrent sub-agents — each running its own inner loop. This document reconstructs the agentic loop from publicly available descriptions; Ante is closed-source, so specifics are inferred rather than confirmed from code.</p></blockquote><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Most coding agents follow a straightforward loop: receive a prompt, call an LLM, execute tools, observe results, repeat. Ante layers a <strong>two-tier loop</strong> on top of this pattern:</p><ol><li>An <strong>outer loop</strong> driven by the meta-agent, responsible for task decomposition, delegation, and result synthesis.</li><li>An <strong>inner loop</strong> within each sub-agent, following a plan-act-observe-decide cycle for its assigned sub-task.</li></ol><p>The combination produces a fan-out / fan-in execution model where multiple sub-agents work concurrently, coordinated loosely by the meta-agent rather than managed step-by-step.</p><h2 id="meta-agent-orchestration-loop" tabindex="-1">Meta-Agent Orchestration Loop <a class="header-anchor" href="#meta-agent-orchestration-loop" aria-label="Permalink to &quot;Meta-Agent Orchestration Loop&quot;">​</a></h2><p>The top-level loop is the meta-agent&#39;s responsibility. When a user submits a request, the meta-agent does not directly start editing files or running commands. Instead, it operates as an orchestrator — what Antigma Labs describes as an &quot;organization of agents to scale.&quot;</p><p>The meta-agent loop proceeds roughly as follows:</p><ol><li><strong>Receive</strong> the user request (natural language prompt, potentially with file context).</li><li><strong>Decompose</strong> the request into discrete sub-tasks. For a feature request, this might mean separating the data-layer changes from the API changes from the test updates.</li><li><strong>Delegate</strong> each sub-task to a sub-agent, providing it with the necessary context (relevant files, constraints, dependencies on other sub-tasks).</li><li><strong>Monitor</strong> sub-agent progress. The meta-agent may receive intermediate status or wait for completion signals.</li><li><strong>Collect</strong> results from all sub-agents (fan-in).</li><li><strong>Synthesize</strong> the combined output — resolving conflicts, verifying coherence across sub-task results, and assembling a unified response.</li><li><strong>Present</strong> the final result to the user.</li></ol>',10)),(o(),s(l,null,{default:t(()=>[r(a,{id:"mermaid-75",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20U%5B%22User%20Request%22%5D%0A%20%20%20%20M1%5B%22Meta-Agent%5CnDecompose%20%26%20Delegate%22%5D%0A%20%20%20%20FO%5B%22Fan-Out%5Cn(dispatch%20to%20sub-agents%20concurrently)%22%5D%0A%20%20%20%20SA%5B%22Sub-Agents%20Execute%5Cn(each%20runs%20inner%20loop)%22%5D%0A%20%20%20%20FI%5B%22Fan-In%5Cn(collect%20results)%22%5D%0A%20%20%20%20M2%5B%22Meta-Agent%20Synthesize%5Cn(combine%2C%20verify%2C%20present)%22%5D%0A%20%20%20%20R%5B%22User%20Response%22%5D%0A%0A%20%20%20%20U%20--%3E%20M1%20--%3E%20FO%20--%3E%20SA%20--%3E%20FI%20--%3E%20M2%20--%3E%20R%0A"})]),fallback:t(()=>[...e[0]||(e[0]=[i(" Loading... ",-1)])]),_:1})),e[3]||(e[3]=n('<p>The meta-agent itself likely relies on an LLM call to perform decomposition and synthesis — meaning the outer loop contains its own LLM interaction steps. The decomposition quality directly determines how well the system parallelizes work and whether sub-agents receive coherent, independent sub-tasks.</p><h2 id="sub-agent-execution-loop" tabindex="-1">Sub-Agent Execution Loop <a class="header-anchor" href="#sub-agent-execution-loop" aria-label="Permalink to &quot;Sub-Agent Execution Loop&quot;">​</a></h2><p>Each sub-agent, once it receives a delegated sub-task, runs a self-contained agentic loop that mirrors the classic plan-act-observe pattern:</p><ol><li><strong>Plan</strong>: The sub-agent&#39;s LLM reasons about the approach — which files to read, what edits to make, what commands to run.</li><li><strong>Act</strong>: Tool calls are executed — file reads, file writes, shell commands, code search, and other operations.</li><li><strong>Observe</strong>: Tool outputs are collected and fed back into the conversation context. The sub-agent evaluates whether the action succeeded or produced errors.</li><li><strong>Decide</strong>: The sub-agent determines whether the sub-task is complete. If not, it loops back to the Plan step with updated context. If done, it reports its results back to the meta-agent.</li></ol>',4)),(o(),s(l,null,{default:t(()=>[r(a,{id:"mermaid-107",class:"mermaid",graph:"flowchart%20TD%0A%20%20%20%20RT%5B%22Receive%20Task%20from%20Meta-Agent%22%5D%0A%20%20%20%20P%5B%22Plan%5Cn(LLM%20reasons%20about%20approach)%22%5D%0A%20%20%20%20A%5B%22Act%5Cn(execute%20tool%20calls%3A%20file%20I%2FO%2C%20shell%2C%20etc.)%22%5D%0A%20%20%20%20O%5B%22Observe%5Cn(collect%20tool%20output%2C%20check%20results)%22%5D%0A%20%20%20%20D%7B%22Decide%5Cn(done%3F)%22%7D%0A%20%20%20%20RPT%5B%22Report%20to%20Meta-Agent%22%5D%0A%0A%20%20%20%20RT%20--%3E%20P%20--%3E%20A%20--%3E%20O%20--%3E%20D%0A%20%20%20%20D%20--%3E%7Cyes%7C%20RPT%0A%20%20%20%20D%20--%3E%7Cno%7C%20P%0A"})]),fallback:t(()=>[...e[1]||(e[1]=[i(" Loading... ",-1)])]),_:1})),e[4]||(e[4]=n('<p>Sub-agents are expected to be narrowly scoped. A sub-agent assigned to &quot;update the database migration&quot; should not also start modifying API handlers — that would be a different sub-agent&#39;s concern. This scoping is what enables concurrency: independent sub-tasks on independent file regions can proceed in parallel without conflict.</p><h2 id="self-organizing-behavior" tabindex="-1">Self-Organizing Behavior <a class="header-anchor" href="#self-organizing-behavior" aria-label="Permalink to &quot;Self-Organizing Behavior&quot;">​</a></h2><p>A distinguishing design principle in Ante is <strong>self-organization</strong>. Traditional multi-agent systems often use rigid orchestration: a central controller issues precise instructions, monitors every step, and handles all coordination. Ante&#39;s philosophy, as described publicly, leans toward emergent coordination — agents behave more like teammates than subordinates.</p><p>Key characteristics of this self-organizing approach:</p><ul><li><strong>The user is &quot;another agent&quot;</strong>: Rather than the user sitting outside the system issuing commands, the user is modeled as a participant in the agent network. This blurs the boundary between human input and agent-generated context.</li><li><strong>Loose coupling</strong>: Sub-agents coordinate through shared context and results rather than through direct message-passing or rigid protocols. The meta-agent provides the initial decomposition, but sub-agents may have latitude in how they accomplish their tasks.</li><li><strong>Emergent task refinement</strong>: If a sub-agent discovers during execution that its sub-task is different than expected (e.g., a file doesn&#39;t exist, an API has changed), it can adapt its plan autonomously rather than requiring the meta-agent to re-plan.</li><li><strong>Collaborative rather than hierarchical</strong>: The mental model is less &quot;boss and workers&quot; and more &quot;team of specialists.&quot; The meta-agent is a coordinator, not a micromanager.</li></ul><p>This philosophy has practical implications for the loop. Error handling, for example, may be distributed: a sub-agent that encounters a build error can attempt to fix it within its own loop before escalating to the meta-agent. This reduces round-trips through the outer loop and keeps the system responsive.</p><blockquote><p><strong>Note</strong>: The degree to which self-organization is implemented versus aspirational is unclear from public descriptions alone. The architectural intent is clear, but the specific mechanisms (agent-to-agent communication, conflict resolution, etc.) are not publicly documented.</p></blockquote><h2 id="lock-free-scheduling" tabindex="-1">Lock-Free Scheduling <a class="header-anchor" href="#lock-free-scheduling" aria-label="Permalink to &quot;Lock-Free Scheduling&quot;">​</a></h2><p>Ante is built in Rust, and its runtime leverages a <strong>lock-free scheduler</strong> for concurrent sub-agent execution. This is a critical enabler for the multi-agent loop.</p><p>In a lock-based concurrency model, agents contending for shared resources (file system access, context memory, LLM API connections) would block each other, serializing what should be parallel work. Ante&#39;s lock-free approach means:</p><ul><li><strong>No mutex contention</strong>: Sub-agents do not acquire locks to read or update shared state. Instead, atomic operations or lock-free data structures (common in Rust&#39;s ecosystem via crates like <code>crossbeam</code>) enable safe concurrent access.</li><li><strong>Independent progress</strong>: Each sub-agent can make progress regardless of what other agents are doing. One agent waiting on an LLM API response does not block another agent from executing a file write.</li><li><strong>Predictable latency</strong>: Lock-free designs avoid priority inversion and convoy effects, which helps keep the overall loop latency predictable even as the number of concurrent sub-agents increases.</li><li><strong>Rust&#39;s ownership model</strong>: Rust&#39;s compile-time guarantees around data races complement the lock-free design. The borrow checker ensures that shared mutable state is handled correctly without relying on runtime locking.</li></ul><p>From a loop perspective, the lock-free scheduler means the fan-out step in the meta-agent&#39;s outer loop is genuinely concurrent — sub-agents are dispatched and run in parallel, with the fan-in step collecting results as they arrive rather than polling or waiting in sequence.</p><h2 id="llm-interaction-pattern" tabindex="-1">LLM Interaction Pattern <a class="header-anchor" href="#llm-interaction-pattern" aria-label="Permalink to &quot;LLM Interaction Pattern&quot;">​</a></h2><p>Ante supports multiple LLM backends, spanning both cloud and local providers:</p><ul><li><strong>Cloud</strong>: Anthropic Claude, Google Gemini (and potentially others via API-compatible endpoints).</li><li><strong>Local</strong>: Possibly through <code>nanochat-rs</code> or similar local inference libraries, enabling fully offline operation.</li></ul><p>The LLM interaction pattern within the loop must account for these differences:</p><h3 id="cloud-api-calls" tabindex="-1">Cloud API Calls <a class="header-anchor" href="#cloud-api-calls" aria-label="Permalink to &quot;Cloud API Calls&quot;">​</a></h3><p>For cloud-hosted models, each LLM interaction in the loop involves:</p><ol><li>Constructing the prompt (system message, conversation history, tool definitions).</li><li>Sending an HTTP request to the provider&#39;s API.</li><li>Waiting for the response (potentially streaming).</li><li>Parsing the response for content and/or tool-use requests.</li></ol><p>Cloud calls introduce network latency, which is why concurrency matters — while one sub-agent waits for a Claude response, others can continue executing tool calls or processing their own LLM responses.</p><h3 id="local-inference" tabindex="-1">Local Inference <a class="header-anchor" href="#local-inference" aria-label="Permalink to &quot;Local Inference&quot;">​</a></h3><p>In offline or local mode, the LLM call is replaced by local model inference:</p><ol><li>The same prompt construction occurs.</li><li>Inference runs on the local machine (CPU or GPU).</li><li>Latency depends on model size and hardware rather than network conditions.</li></ol><p>Local inference puts the model in the critical path of the loop — a slow local model directly increases each iteration&#39;s cycle time. The lock-free scheduler still helps here, as multiple sub-agents can interleave their compute phases.</p><h3 id="unified-interface" tabindex="-1">Unified Interface <a class="header-anchor" href="#unified-interface" aria-label="Permalink to &quot;Unified Interface&quot;">​</a></h3><p>Regardless of backend, the loop logic likely uses a unified interface for LLM calls — the sub-agent issues a &quot;complete this conversation&quot; request, and the backend abstraction handles whether that goes to a cloud API or a local model. This keeps the loop structure identical across deployment modes.</p><h2 id="tool-execution-cycle" tabindex="-1">Tool Execution Cycle <a class="header-anchor" href="#tool-execution-cycle" aria-label="Permalink to &quot;Tool Execution Cycle&quot;">​</a></h2><p>Within each sub-agent&#39;s inner loop, the Act phase involves executing tool calls. Ante, as a terminal-native coding agent, likely supports a standard set of tools:</p><ul><li><strong>File reads</strong>: Read file contents, list directories, search codebases.</li><li><strong>File writes</strong>: Create, edit, or delete files. Edits may be patch-based (surgical replacements) or full-file rewrites.</li><li><strong>Shell commands</strong>: Run build commands, tests, linters, git operations, and other CLI tools.</li><li><strong>Code search</strong>: Grep-like or AST-aware search across the codebase.</li></ul><p>The tool execution cycle within a single inner-loop iteration:</p><ol><li>The LLM response includes one or more tool-use requests (structured as tool name + parameters).</li><li>The agent runtime validates and executes each tool call.</li><li>Tool outputs (stdout, stderr, file contents, error messages) are captured.</li><li>Outputs are appended to the conversation context.</li><li>The updated context is sent back to the LLM for the next iteration.</li></ol><p>When multiple tool calls are returned in a single LLM response, Ante may execute them concurrently (leveraging the same lock-free scheduler) or sequentially depending on dependencies between the calls.</p><p>Tool execution errors are a key part of the loop&#39;s self-correction mechanism. A failed <code>cargo build</code> produces compiler errors that the sub-agent feeds back to the LLM, which then plans a fix — this is the standard observe → plan → act recovery cycle.</p><h2 id="offline-loop-variant" tabindex="-1">Offline Loop Variant <a class="header-anchor" href="#offline-loop-variant" aria-label="Permalink to &quot;Offline Loop Variant&quot;">​</a></h2><p>Ante advertises offline capability, meaning the entire agentic loop can run without network access. In this mode:</p><ul><li><strong>LLM inference</strong> is performed locally (no cloud API calls).</li><li><strong>All tool execution</strong> remains local (file I/O, shell commands are inherently local).</li><li><strong>The meta-agent loop</strong> still operates the same way — decompose, delegate, collect, synthesize — but every LLM call in both the outer and inner loops hits the local model.</li></ul><p>The offline variant has distinct performance characteristics:</p><ul><li><strong>Latency profile shifts</strong>: Network latency disappears, but local inference latency dominates. For small models on capable hardware, this may be faster than cloud calls. For larger models on limited hardware, each loop iteration slows significantly.</li><li><strong>No rate limiting</strong>: Cloud APIs impose rate limits and token quotas. Local inference has no such constraints — the loop can iterate as fast as hardware allows.</li><li><strong>Privacy</strong>: All code and prompts stay on the local machine, which is a key motivation for the offline mode.</li></ul><p>The lock-free scheduler is equally important in offline mode. Even with local inference, multiple sub-agents benefit from concurrent execution — while one agent&#39;s inference runs, another can execute tool calls, and a third can be assembling its next prompt.</p><h2 id="comparison-to-single-agent-loops" tabindex="-1">Comparison to Single-Agent Loops <a class="header-anchor" href="#comparison-to-single-agent-loops" aria-label="Permalink to &quot;Comparison to Single-Agent Loops&quot;">​</a></h2><p>To contextualize Ante&#39;s approach, it helps to contrast with the single-agent loop used by most coding agents (Claude Code, Aider, Cursor Agent, etc.):</p><table tabindex="0"><thead><tr><th>Aspect</th><th>Single-Agent Loop</th><th>Ante Multi-Agent Loop</th></tr></thead><tbody><tr><td><strong>Structure</strong></td><td>One loop: prompt → LLM → tools → observe → repeat</td><td>Two-tier: outer meta-agent loop + inner sub-agent loops</td></tr><tr><td><strong>Concurrency</strong></td><td>Sequential (one LLM call at a time)</td><td>Concurrent sub-agents via lock-free scheduler</td></tr><tr><td><strong>Task handling</strong></td><td>Single agent handles entire request</td><td>Meta-agent decomposes; sub-agents handle parts</td></tr><tr><td><strong>Error recovery</strong></td><td>Same agent retries</td><td>Sub-agent retries locally; escalates if needed</td></tr><tr><td><strong>Context window</strong></td><td>One conversation grows over time</td><td>Each sub-agent has a focused, smaller context</td></tr><tr><td><strong>Scaling</strong></td><td>Bounded by single context window</td><td>Can scale to multiple parallel contexts</td></tr></tbody></table><p>The multi-agent approach trades simplicity for potential throughput gains. A complex refactor that touches many files might complete faster with parallel sub-agents, but the decomposition overhead and synthesis step add complexity that a single-agent loop avoids.</p><h2 id="open-questions" tabindex="-1">Open Questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open Questions&quot;">​</a></h2><p>Since Ante is closed-source, several aspects of the loop remain unclear:</p><ul><li><strong>Inter-agent communication</strong>: Can sub-agents communicate with each other during execution, or only through the meta-agent? Direct agent-to-agent messaging would enable richer coordination but adds complexity.</li><li><strong>Conflict resolution</strong>: When two sub-agents modify the same file, how are conflicts detected and resolved? The meta-agent&#39;s synthesis step likely handles this, but the specific mechanism is unknown.</li><li><strong>Dynamic re-planning</strong>: Can the meta-agent revise its decomposition mid-execution if a sub-agent reports unexpected findings? This would require a feedback loop within the outer loop itself.</li><li><strong>Context sharing</strong>: How much context do sub-agents share? Full codebase context for each would be expensive; selective context based on the sub-task would be more efficient but risks missing relevant information.</li><li><strong>Agent memory</strong>: Whether sub-agents or the meta-agent maintain memory across requests (session persistence) is not documented publicly.</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Ante&#39;s agentic loop is a two-tier, multi-agent system:</p><ul><li>The <strong>meta-agent outer loop</strong> handles decomposition, delegation, collection, and synthesis.</li><li><strong>Sub-agent inner loops</strong> each run a plan-act-observe-decide cycle for their assigned sub-tasks.</li><li><strong>Lock-free scheduling</strong> in Rust enables genuine concurrency across sub-agents.</li><li><strong>Self-organizing behavior</strong> favors emergent coordination over rigid top-down control.</li><li>The loop operates identically across <strong>cloud and offline modes</strong>, with only the LLM backend differing.</li><li>The architecture trades single-agent simplicity for parallel throughput and modular task handling.</li></ul>',49))])}const k=c(u,[["render",g]]);export{A as __pageData,k as default};
