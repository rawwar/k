import{_ as t,o,c as a,a2 as s}from"./chunks/framework.B72_uj9U.js";const u=JSON.parse('{"title":"What Are Tools","description":"Define what tools mean in the context of LLM agents and why they are necessary for real-world task completion.","frontmatter":{"title":"What Are Tools","description":"Define what tools mean in the context of LLM agents and why they are necessary for real-world task completion."},"headers":[],"relativePath":"learn/project/04-building-a-tool-system/01-what-are-tools.md","filePath":"learn/project/04-building-a-tool-system/01-what-are-tools.md"}'),n={name:"learn/project/04-building-a-tool-system/01-what-are-tools.md"};function l(i,e,c,r,d,p){return o(),a("div",null,[...e[0]||(e[0]=[s(`<h1 id="what-are-tools" tabindex="-1">What Are Tools <a class="header-anchor" href="#what-are-tools" aria-label="Permalink to &quot;What Are Tools&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How tools extend an LLM&#39;s capabilities beyond text generation to interact with the real world</li><li>The difference between tools the model calls (function calling) and tools a human uses</li><li>Why a coding agent needs file, shell, and search tools as its minimum viable set</li></ul></blockquote><p>In Chapter 3 you built an agentic loop that can detect when the model wants to call a tool. But &quot;wanting&quot; is not &quot;doing.&quot; Right now, when your loop encounters a <code>tool_use</code> block, it has nowhere to send it. The model asks to read a file and your agent shrugs. This chapter changes that. Before you write any code, though, you need a clear mental model of what tools actually are and why they matter.</p><h2 id="the-llm-s-limitation" tabindex="-1">The LLM&#39;s Limitation <a class="header-anchor" href="#the-llm-s-limitation" aria-label="Permalink to &quot;The LLM&#39;s Limitation&quot;">​</a></h2><p>A large language model is, at its core, a text predictor. Given a sequence of tokens, it produces the next most likely token. This means it can reason about code, explain algorithms, draft documentation, and carry on a conversation. What it cannot do is <em>act</em>. It cannot read a file on your disk. It cannot run a shell command. It cannot open a network connection. It cannot check the current time.</p><p>This is not a bug -- it is a fundamental architectural boundary. The model runs in a stateless inference environment. It has no file system, no process table, no network stack. Every &quot;fact&quot; it produces comes from patterns in its training data or from what you put in its context window.</p><p>Tools are the bridge across this boundary. A tool is a function that your agent code provides, which the model can request to invoke. When the model decides it needs to read a file, it does not somehow reach out to your operating system. Instead, it emits a structured message -- a <code>tool_use</code> block -- that says &quot;I want to call the <code>read_file</code> tool with the argument <code>path: src/main.rs</code>.&quot; Your agent code receives this message, executes the actual file read, and feeds the result back to the model as an observation.</p><p>Think of it this way: the model is the brain, and tools are its arms and eyes.</p><h2 id="function-calling-the-mechanism" tabindex="-1">Function Calling: The Mechanism <a class="header-anchor" href="#function-calling-the-mechanism" aria-label="Permalink to &quot;Function Calling: The Mechanism&quot;">​</a></h2><p>The specific mechanism that enables tools is called <strong>function calling</strong> (or tool use, depending on the API). Here is how it works with the Anthropic Messages API:</p><ol><li><p><strong>You declare tools.</strong> When you send a request to the API, you include a <code>tools</code> array that describes each tool: its name, a description of what it does, and a JSON schema defining its input parameters.</p></li><li><p><strong>The model decides to use one.</strong> Based on the conversation and the tool descriptions, the model may produce a response with <code>stop_reason: &quot;tool_use&quot;</code> and one or more <code>tool_use</code> content blocks. Each block contains the tool name, a unique <code>id</code>, and the input arguments as a JSON object.</p></li><li><p><strong>You execute it.</strong> Your code receives the <code>tool_use</code> block, looks up the tool by name, validates the input, runs the tool&#39;s logic, and collects the result.</p></li><li><p><strong>You feed the result back.</strong> You append a <code>tool_result</code> content block to the conversation, referencing the <code>tool_use_id</code>, and send the updated conversation back to the API. The model now sees the result and can reason about it.</p></li></ol><p>This cycle repeats inside the agentic loop you built in Chapter 3. The model may call tools several times in succession -- read a file, then edit it, then run a test -- before it decides the task is complete and produces a final text response.</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">{</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">tools</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> [</span></span>
<span class="line"><span style="color:#9399B2;">    {</span></span>
<span class="line"><span style="color:#9399B2;">      &quot;</span><span style="color:#89B4FA;">name</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;read_file&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">      &quot;</span><span style="color:#89B4FA;">description</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;Read the contents of a file at the given path.&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">      &quot;</span><span style="color:#89B4FA;">input_schema</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#9399B2;">        &quot;</span><span style="color:#89B4FA;">type</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;object&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">        &quot;</span><span style="color:#89B4FA;">properties</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#9399B2;">          &quot;</span><span style="color:#89B4FA;">path</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#9399B2;">            &quot;</span><span style="color:#89B4FA;">type</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;string&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">            &quot;</span><span style="color:#89B4FA;">description</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;The absolute or relative path to the file.&quot;</span></span>
<span class="line"><span style="color:#9399B2;">          }</span></span>
<span class="line"><span style="color:#9399B2;">        },</span></span>
<span class="line"><span style="color:#9399B2;">        &quot;</span><span style="color:#89B4FA;">required</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> [</span><span style="color:#A6E3A1;">&quot;path&quot;</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#9399B2;">      }</span></span>
<span class="line"><span style="color:#9399B2;">    }</span></span>
<span class="line"><span style="color:#9399B2;">  ]</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>This JSON snippet shows a single tool declaration. The <code>input_schema</code> is a JSON Schema object that tells the model exactly what arguments the tool accepts. You will learn how to construct these schemas in Rust in the upcoming subchapters.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>If you have used the OpenAI or Anthropic Python SDKs, you may have declared tools using dictionaries or Pydantic models. The concept is identical in Rust -- the only difference is that you construct the JSON schema using <code>serde_json::json!</code> macros or derive it from Rust structs. The structured typing actually makes Rust a natural fit for this: your tool&#39;s input types are checked at compile time.</p></div><h2 id="tools-vs-prompts" tabindex="-1">Tools vs. Prompts <a class="header-anchor" href="#tools-vs-prompts" aria-label="Permalink to &quot;Tools vs. Prompts&quot;">​</a></h2><p>A common question is: why not just ask the model to &quot;imagine&quot; it can read files and include the file contents in the prompt? Some early agent prototypes did exactly that. The problem is reliability. When the model &quot;imagines&quot; a file read, it is generating plausible-looking content from its training data. It might hallucinate file contents, invent function signatures that do not exist, or produce output that looks correct but is subtly wrong.</p><p>Tools give you <strong>grounded</strong> information. When the model calls <code>read_file</code>, it gets the actual bytes on disk, not a guess. When it calls <code>run_shell</code>, it gets real stdout and stderr, not simulated output. This grounding is what makes the difference between an agent that <em>sounds</em> helpful and one that <em>is</em> helpful.</p><h2 id="the-minimum-viable-toolset" tabindex="-1">The Minimum Viable Toolset <a class="header-anchor" href="#the-minimum-viable-toolset" aria-label="Permalink to &quot;The Minimum Viable Toolset&quot;">​</a></h2><p>What tools does a coding agent need at minimum? Look at what a human developer does:</p><ol><li><strong>Read files</strong> -- You open a file to understand the current code.</li><li><strong>Write/edit files</strong> -- You make changes to implement a feature or fix a bug.</li><li><strong>Run commands</strong> -- You execute tests, build the project, run linters.</li><li><strong>Search</strong> -- You grep through a codebase to find relevant code.</li></ol><p>Every production coding agent has some version of these four categories. Claude Code has <code>Read</code>, <code>Write</code>, <code>Edit</code>, <code>Bash</code>, and <code>Grep</code> (among others). OpenAI&#39;s Codex provides <code>shell</code>, <code>read</code>, <code>write</code>, and <code>patch</code>. The specific tool names differ, but the capabilities map to the same developer workflow.</p><p>In this chapter you will build the <em>framework</em> that makes these tools possible. You will define the trait that every tool must implement, the registry that stores them, and the dispatch logic that routes tool calls. In Chapter 5 you will implement the actual file tools, and in Chapter 6 you will add shell execution.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Claude Code ships with approximately a dozen tools, including <code>Read</code>, <code>Write</code>, <code>Edit</code>, <code>Bash</code>, <code>Glob</code>, <code>Grep</code>, and <code>WebFetch</code>. Each tool is a self-contained module that registers itself with the tool system at startup. OpenCode takes a similar approach with tools like <code>shell</code>, <code>read</code>, <code>write</code>, and <code>patch</code>. Both agents structure their tools around what a developer does at a terminal, not around abstract capabilities.</p></div><h2 id="tools-as-a-contract" tabindex="-1">Tools as a Contract <a class="header-anchor" href="#tools-as-a-contract" aria-label="Permalink to &quot;Tools as a Contract&quot;">​</a></h2><p>From a software design perspective, a tool is a contract between three parties:</p><ol><li><strong>The model</strong> sees a name, a description, and an input schema. It uses this information to decide when to call the tool and what arguments to pass.</li><li><strong>The agent code</strong> sees an interface (in Rust, a trait) that it can call with the provided arguments. It does not care about the tool&#39;s internal logic -- it just calls <code>execute</code> and collects the result.</li><li><strong>The tool implementation</strong> does the actual work. It reads a file, runs a command, queries a database. It returns a result string that becomes the observation.</li></ol><p>This three-way contract is the design principle behind everything you will build in this chapter. The <code>Tool</code> trait encodes the contract. The JSON schema formalizes the input side. The tool result formalizes the output side. Get this contract right, and adding new tools later is trivial.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>Tools bridge the gap between the model&#39;s reasoning ability and the real world -- they let the model read files, run commands, and observe results instead of guessing.</li><li>Function calling is the API mechanism: you declare tools in the request, the model emits <code>tool_use</code> blocks, you execute them and feed <code>tool_result</code> observations back.</li><li>A coding agent needs at minimum: file reading, file writing/editing, shell execution, and search. Everything else is an enhancement.</li><li>Tools are a three-way contract between the model (which sees descriptions and schemas), the agent framework (which calls <code>execute</code>), and the implementation (which does the work).</li><li>This chapter builds the framework; Chapters 5 and 6 fill it with concrete tool implementations.</li></ul>`,30)])])}const m=t(n,[["render",l]]);export{u as __pageData,m as default};
