import{_ as t,o as n,c as o,a2 as s}from"./chunks/framework.B72_uj9U.js";const h=JSON.parse('{"title":"Tokens and Tokenization","description":"How text is broken into tokens, why token counts differ from character counts, and how this impacts agent context management.","frontmatter":{"title":"Tokens and Tokenization","description":"How text is broken into tokens, why token counts differ from character counts, and how this impacts agent context management."},"headers":[],"relativePath":"learn/linear/03-understanding-llms/02-tokens-and-tokenization.md","filePath":"learn/linear/03-understanding-llms/02-tokens-and-tokenization.md"}'),a={name:"learn/linear/03-understanding-llms/02-tokens-and-tokenization.md"};function i(r,e,l,c,p,u){return n(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="tokens-and-tokenization" tabindex="-1">Tokens and Tokenization <a class="header-anchor" href="#tokens-and-tokenization" aria-label="Permalink to &quot;Tokens and Tokenization&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How BPE and similar tokenization algorithms split text into subword tokens</li><li>Why code tokenizes differently than natural language and how this affects context budgets</li><li>How to estimate token counts for different content types to manage context window usage</li></ul></blockquote><p>Every interaction with an LLM is measured in tokens -- not characters, not words, not lines. Tokens are the fundamental unit of input and output for language models, and they directly determine how much context you can fit in a request, how fast the model responds, and how much each API call costs. As an agent builder, you will think about tokens constantly: &quot;How many tokens is this file?&quot; &quot;Can I fit the error output in the remaining context?&quot; &quot;Is this conversation getting too long?&quot; This subchapter gives you the foundation to answer those questions.</p><h2 id="what-is-a-token" tabindex="-1">What Is a Token? <a class="header-anchor" href="#what-is-a-token" aria-label="Permalink to &quot;What Is a Token?&quot;">​</a></h2><p>A token is a chunk of text that the model treats as a single unit. Tokens are not characters. They are not words. They are subword units determined by a tokenization algorithm during model training. The most common algorithm is <strong>Byte Pair Encoding (BPE)</strong>, and understanding it intuitively will help you reason about token counts.</p><p>Here is how BPE works conceptually:</p><ol><li>Start with every individual character as its own token</li><li>Find the most frequently occurring pair of adjacent tokens in the training corpus</li><li>Merge that pair into a new single token</li><li>Repeat thousands of times</li></ol><p>After thousands of merges, common words become single tokens, common subwords (like &quot;ing&quot;, &quot;tion&quot;, &quot;pre&quot;) become single tokens, and rare or unusual strings remain as individual characters or small pieces.</p><p>For example, the word &quot;function&quot; is common enough in English and code that most tokenizers encode it as a single token. But the word &quot;defenestrate&quot; would likely be split into something like <code>[&quot;def&quot;, &quot;en&quot;, &quot;est&quot;, &quot;rate&quot;]</code> because it is rare.</p><h2 id="how-code-tokenizes" tabindex="-1">How Code Tokenizes <a class="header-anchor" href="#how-code-tokenizes" aria-label="Permalink to &quot;How Code Tokenizes&quot;">​</a></h2><p>This is where things get particularly relevant for coding agents. Code tokenizes differently than prose, and the differences matter for context management.</p><p>Common programming keywords and patterns tend to be single tokens:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>&quot;function&quot;  -&gt; 1 token</span></span>
<span class="line"><span>&quot;return&quot;    -&gt; 1 token</span></span>
<span class="line"><span>&quot;const&quot;     -&gt; 1 token</span></span>
<span class="line"><span>&quot;import&quot;    -&gt; 1 token</span></span></code></pre></div><p>But code also contains many characters that tokenize less efficiently:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>&quot;    &quot;      -&gt; might be 1 token (4 spaces) or multiple tokens</span></span>
<span class="line"><span>&quot;{&quot;         -&gt; 1 token</span></span>
<span class="line"><span>&quot;}&quot;         -&gt; 1 token</span></span>
<span class="line"><span>&quot;::&quot;        -&gt; often 1 token (common in Rust/C++)</span></span>
<span class="line"><span>&quot;=&gt;&quot;        -&gt; often 1 token</span></span></code></pre></div><p>Here is a practical example. Consider this Rust function:</p><div class="language-rust"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">fn</span><span style="color:#89B4FA;font-style:italic;"> process_file</span><span style="color:#9399B2;">(</span><span style="color:#EBA0AC;">path</span><span style="color:#94E2D5;">:</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#F9E2AF;font-style:italic;">str</span><span style="color:#9399B2;">)</span><span style="color:#94E2D5;"> -&gt;</span><span style="color:#F9E2AF;font-style:italic;"> Result</span><span style="color:#89DCEB;">&lt;</span><span style="color:#F9E2AF;font-style:italic;">String</span><span style="color:#9399B2;">,</span><span style="color:#EBA0AC;"> std</span><span style="color:#94E2D5;">::</span><span style="color:#EBA0AC;">io</span><span style="color:#94E2D5;">::</span><span style="color:#F9E2AF;font-style:italic;">Error</span><span style="color:#89DCEB;">&gt;</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#CBA6F7;">    let</span><span style="color:#CDD6F4;"> content </span><span style="color:#94E2D5;">=</span><span style="color:#F9E2AF;"> std</span><span style="color:#94E2D5;">::</span><span style="color:#F9E2AF;">fs</span><span style="color:#94E2D5;">::</span><span style="color:#89B4FA;font-style:italic;">read_to_string</span><span style="color:#9399B2;">(</span><span style="color:#CDD6F4;">path</span><span style="color:#9399B2;">)</span><span style="color:#94E2D5;">?</span><span style="color:#9399B2;">;</span></span>
<span class="line"><span style="color:#F9E2AF;font-style:italic;">    Ok</span><span style="color:#9399B2;">(</span><span style="color:#CDD6F4;">content</span><span style="color:#94E2D5;">.</span><span style="color:#89B4FA;font-style:italic;">to_uppercase</span><span style="color:#9399B2;">())</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>This is 3 lines and roughly 100 characters. With Claude&#39;s tokenizer, it comes out to approximately 30-35 tokens. The rough rule of thumb -- <strong>1 token is approximately 4 characters of English text or 3 characters of code</strong> -- holds reasonably well, but there is meaningful variance.</p><p>Whitespace in code is a particular concern. Python&#39;s significant whitespace and common 4-space indentation means deeply nested Python code burns tokens on indentation. Rust&#39;s curly-brace syntax uses fewer whitespace tokens but adds brace tokens. In practice, most code averages about <strong>2.5-3.5 characters per token</strong>.</p><h2 id="token-counts-in-practice" tabindex="-1">Token Counts in Practice <a class="header-anchor" href="#token-counts-in-practice" aria-label="Permalink to &quot;Token Counts in Practice&quot;">​</a></h2><p>Let&#39;s look at some real numbers that matter for agent design. These are approximate and vary by tokenizer, but they give you reliable estimates:</p><table tabindex="0"><thead><tr><th>Content Type</th><th>Approximate Tokens per 1K Characters</th></tr></thead><tbody><tr><td>English prose</td><td>250 tokens</td></tr><tr><td>Python code</td><td>300 tokens</td></tr><tr><td>Rust code</td><td>320 tokens</td></tr><tr><td>JSON data</td><td>350 tokens</td></tr><tr><td>Minified code</td><td>400+ tokens</td></tr><tr><td>Base64 data</td><td>500+ tokens</td></tr></tbody></table><p>JSON is particularly expensive because of all the structural characters -- quotes, colons, commas, and braces. This matters for agents because tool results are often JSON, and a large JSON response from a tool can consume a surprising amount of context.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>In Python, you might estimate string lengths with <code>len(text)</code>. For tokens, the equivalent is using a tokenizer library. The <code>tiktoken</code> library handles OpenAI&#39;s tokenizers, and Anthropic provides token counts in API responses. In your Rust agent, you will typically use the <code>input_tokens</code> and <code>output_tokens</code> counts returned by the API rather than counting tokens client-side, though client-side estimation is useful for pre-checking whether content fits in the context window.</p></div><h2 id="tokenizer-differences-between-providers" tabindex="-1">Tokenizer Differences Between Providers <a class="header-anchor" href="#tokenizer-differences-between-providers" aria-label="Permalink to &quot;Tokenizer Differences Between Providers&quot;">​</a></h2><p>Different model families use different tokenizers. This means the same text produces different token counts depending on which model you are using:</p><ul><li><strong>Anthropic (Claude)</strong> uses a custom BPE tokenizer. Claude&#39;s tokenizer tends to be efficient with code, often producing slightly fewer tokens than GPT tokenizers for the same source code.</li><li><strong>OpenAI (GPT-4, GPT-4o)</strong> uses the <code>cl100k_base</code> tokenizer (for GPT-4) or the <code>o200k_base</code> tokenizer (for GPT-4o). The <code>o200k_base</code> tokenizer has a 200K vocabulary and is more efficient with many common patterns.</li></ul><p>In practice, the differences are small enough (within 10-15%) that you can use a single estimation approach in your agent and rely on the API&#39;s reported token counts for precise tracking.</p><h2 id="special-tokens" tabindex="-1">Special Tokens <a class="header-anchor" href="#special-tokens" aria-label="Permalink to &quot;Special Tokens&quot;">​</a></h2><p>Beyond text tokens, LLM APIs use special tokens that you do not see in the raw text but that count toward your token budget. These include:</p><ul><li><strong>Message role markers</strong> -- each message in the conversation has overhead tokens for the role labels and structural formatting</li><li><strong>Tool definitions</strong> -- when you send tool schemas, those consume tokens too</li><li><strong>Stop tokens</strong> -- special tokens that signal the model to stop generating</li></ul><p>The overhead from message structure typically adds 3-5 tokens per message. Tool definitions can be much more expensive -- a single tool with a detailed JSON Schema description might consume 100-200 tokens. If you register 10 tools, that is 1,000-2,000 tokens consumed before the conversation even starts.</p><p>This is why the API response includes a <code>usage</code> object that reports exact token counts:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">{</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">usage</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#9399B2;">    &quot;</span><span style="color:#89B4FA;">input_tokens</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;"> 1247</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">    &quot;</span><span style="color:#89B4FA;">output_tokens</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;"> 356</span></span>
<span class="line"><span style="color:#9399B2;">  }</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>You should track these values in your agent to monitor context consumption and make decisions about when to summarize or truncate conversation history.</p><h2 id="estimating-token-counts-for-agent-operations" tabindex="-1">Estimating Token Counts for Agent Operations <a class="header-anchor" href="#estimating-token-counts-for-agent-operations" aria-label="Permalink to &quot;Estimating Token Counts for Agent Operations&quot;">​</a></h2><p>When building your agent, you will frequently need to estimate whether something fits in the remaining context. Here are practical estimation formulas:</p><p><strong>For a source file:</strong></p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>estimated_tokens = file_size_bytes * 0.3</span></span></code></pre></div><p>A 10KB source file is roughly 3,000 tokens. This is conservative but safe.</p><p><strong>For a shell command output:</strong></p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>estimated_tokens = output_length_chars / 3.5</span></span></code></pre></div><p><strong>For conversation history:</strong></p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>estimated_tokens = sum(message_lengths_chars / 3.5) + (num_messages * 4)</span></span></code></pre></div><p>The <code>+ (num_messages * 4)</code> accounts for the per-message overhead.</p><p>These estimates help you make decisions like &quot;should I read this entire file into context, or should I search for the relevant section?&quot; In a coding agent with a 200K token context window, a 10KB file is trivial. But if the user&#39;s project has a 500KB generated file, that alone would consume roughly 150K tokens -- most of your context budget.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Production coding agents like Claude Code are careful about what they put into context. Rather than reading entire files, they use tools like grep and targeted file reads to pull in only relevant sections. When shell command output is very long, they truncate it and let the model know the output was truncated. This token-aware approach to context management is a major differentiator between a working agent and one that runs out of context on real-world tasks.</p></div><h2 id="the-input-output-token-distinction" tabindex="-1">The Input/Output Token Distinction <a class="header-anchor" href="#the-input-output-token-distinction" aria-label="Permalink to &quot;The Input/Output Token Distinction&quot;">​</a></h2><p>API pricing and rate limits distinguish between <strong>input tokens</strong> (what you send to the model) and <strong>output tokens</strong> (what the model generates). This distinction matters for two reasons:</p><ol><li><p><strong>Cost</strong>: Output tokens are typically 3-5x more expensive than input tokens. A response where the model writes 2,000 tokens of code costs significantly more than the 2,000 tokens of context you sent as input.</p></li><li><p><strong>Speed</strong>: Output token generation is the bottleneck. The model generates output tokens sequentially (one at a time), while input tokens are processed in parallel. A request with 100K input tokens and 500 output tokens might take only a few seconds, while a request with 1K input tokens and 4,000 output tokens could take 10+ seconds.</p></li></ol><p>For agents, this means that long model responses (detailed code generation, verbose explanations) are both slower and more expensive than short responses (tool calls, brief confirmations). This is one reason why well-designed tool descriptions encourage the model to take action through tools rather than generating long text explanations.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>Tokens are subword units determined by BPE -- common words are single tokens, rare words are split into pieces, and code tokenizes at roughly 3-3.5 characters per token</li><li>Tool definitions, message structure, and JSON formatting all consume tokens beyond the visible text, and the overhead can be significant with many tools registered</li><li>Always use the API&#39;s reported <code>usage</code> counts for precise tracking, but use the formula <code>bytes * 0.3</code> for quick client-side estimates of file sizes</li><li>Output tokens are more expensive and slower to generate than input tokens, which has direct implications for agent design -- prefer concise tool calls over verbose text generation</li><li>Different providers use different tokenizers, but the variation is small enough (10-15%) that a single estimation strategy works in practice</li></ul>`,53)])])}const g=t(a,[["render",i]]);export{h as __pageData,g as default};
