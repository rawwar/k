import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.B72_uj9U.js";const h=JSON.parse('{"title":"Summary","description":"Review of code intelligence tools and techniques with connections to version control integration in the next chapter.","frontmatter":{"title":"Summary","description":"Review of code intelligence tools and techniques with connections to version control integration in the next chapter."},"headers":[],"relativePath":"learn/linear/11-code-intelligence/12-summary.md","filePath":"learn/linear/11-code-intelligence/12-summary.md"}'),r={name:"learn/linear/11-code-intelligence/12-summary.md"};function n(i,e,c,l,d,p){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How tree-sitter, ripgrep, glob patterns, and LSP form a comprehensive code intelligence stack for a coding agent</li><li>Which code intelligence capabilities are essential for the version control integration features in Chapter 12</li><li>Key architectural decisions for code intelligence and their impact on agent accuracy and performance across languages</li></ul></blockquote><p>This chapter took you from the limitations of text search to a complete code intelligence stack. Let&#39;s consolidate what you have learned and see how these tools work together in practice.</p><h2 id="the-code-intelligence-stack" tabindex="-1">The Code Intelligence Stack <a class="header-anchor" href="#the-code-intelligence-stack" aria-label="Permalink to &quot;The Code Intelligence Stack&quot;">​</a></h2><p>You now have four layers of code understanding, each serving a different purpose:</p><p><strong>Layer 1: File Discovery (glob + ignore crate).</strong> Before you can analyze code, you need to find the right files. The <code>ignore</code> crate provides gitignore-aware, parallel directory walking that efficiently enumerates files matching glob patterns like <code>**/*.rs</code> or <code>src/**/*.{ts,tsx}</code>. This layer respects <code>.gitignore</code> to avoid searching generated code and dependencies.</p><p><strong>Layer 2: Text Search (ripgrep).</strong> Fast, language-agnostic text search across files. Ripgrep combines parallel directory traversal, memory-mapped I/O, and SIMD-accelerated regex matching to search large codebases in milliseconds. It finds exact strings, regex patterns, and text occurrences — but cannot distinguish definitions from references or code from comments.</p><p><strong>Layer 3: Structural Analysis (tree-sitter).</strong> Parses source code into concrete syntax trees that capture the full grammatical structure. Tree-sitter queries let you match patterns like &quot;all public functions returning Result&quot; or &quot;every struct that derives Serialize.&quot; The incremental parser handles edits in microseconds, and error recovery produces useful trees even for broken code.</p><p><strong>Layer 4: Semantic Analysis (LSP).</strong> Full language-specific understanding including type inference, cross-file reference resolution, trait dispatch, and macro expansion. LSP provides this through a standard JSON-RPC protocol that works with rust-analyzer, pyright, gopls, and dozens of other language servers.</p><p>Each layer is more powerful but more expensive. A well-designed agent starts at Layer 1, moves to Layer 2 for text search, uses Layer 3 for structural questions, and only reaches for Layer 4 when deep semantic understanding is required.</p><h2 id="how-the-layers-work-together" tabindex="-1">How the Layers Work Together <a class="header-anchor" href="#how-the-layers-work-together" aria-label="Permalink to &quot;How the Layers Work Together&quot;">​</a></h2><p>Consider a realistic agent task: &quot;Add error handling to all database functions that currently use <code>.unwrap()</code>.&quot;</p><p>Here is how the layers combine to handle this:</p><div class="language-rust"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#6C7086;font-style:italic;">// Step 1: File Discovery — find all Rust files in the project</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Layer 1: glob + ignore crate</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Result: [&quot;src/db.rs&quot;, &quot;src/db/queries.rs&quot;, &quot;src/db/connection.rs&quot;, ...]</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Step 2: Text Search — narrow to files containing &quot;.unwrap()&quot;</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Layer 2: ripgrep with --files-with-matches</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Result: [&quot;src/db/queries.rs&quot;, &quot;src/db/connection.rs&quot;]</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Step 3: Structural Analysis — find functions containing unwrap calls</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Layer 3: tree-sitter query for function_item nodes containing</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">//          call expressions on .unwrap()</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Result: [</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">//   (&quot;src/db/queries.rs&quot;, &quot;fetch_user&quot;, line 15),</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">//   (&quot;src/db/queries.rs&quot;, &quot;save_record&quot;, line 42),</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">//   (&quot;src/db/connection.rs&quot;, &quot;connect&quot;, line 8),</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// ]</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Step 4: Semantic Analysis — determine return types to choose</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">//         the right error handling strategy</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Layer 4: LSP hover on each function to get its resolved return type</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// Result: [</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">//   (&quot;fetch_user&quot; returns &quot;User&quot; — needs to change to &quot;Result&lt;User, DbError&gt;&quot;),</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">//   (&quot;save_record&quot; returns &quot;()&quot; — needs to change to &quot;Result&lt;(), DbError&gt;&quot;),</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">//   (&quot;connect&quot; returns &quot;Connection&quot; — needs to change to &quot;Result&lt;Connection, DbError&gt;&quot;),</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">// ]</span></span></code></pre></div><p>Each layer reduces the search space for the next. File discovery finds 200 Rust files. Ripgrep narrows to 2 files containing <code>.unwrap()</code>. Tree-sitter identifies 3 specific functions. LSP provides the type information needed to generate the correct fix. Without this pipeline, the agent would need to read all 200 files in their entirety — consuming the context window and slowing everything down.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>The layered approach mirrors what experienced Python developers do manually: <code>find . -name &quot;*.py&quot;</code> to locate files, <code>grep</code> to find candidates, <code>ast.parse()</code> to understand structure, and <code>pyright</code> or <code>mypy</code> for type analysis. The difference is that in a Rust agent, all four layers are programmatically accessible and can be composed into automated pipelines.</p></div><h2 id="key-concepts-reviewed" tabindex="-1">Key Concepts Reviewed <a class="header-anchor" href="#key-concepts-reviewed" aria-label="Permalink to &quot;Key Concepts Reviewed&quot;">​</a></h2><p><strong>Tree-sitter fundamentals.</strong> Tree-sitter is an incremental GLR parser that produces concrete syntax trees. It handles error recovery gracefully, works across 150+ languages with a uniform API, and supports a declarative query language for pattern matching. The Rust bindings (<code>tree-sitter</code> crate) provide <code>Parser</code>, <code>Tree</code>, <code>Node</code>, <code>TreeCursor</code>, <code>Query</code>, and <code>QueryCursor</code> as the core types.</p><p><strong>Parsing and navigation.</strong> <code>Parser::parse()</code> takes source bytes and returns a <code>Tree</code>. Navigate with <code>Node::child()</code>, <code>Node::child_by_field_name()</code>, or <code>TreeCursor</code> for efficient depth-first traversal. Nodes expose <code>kind()</code> for the type name, byte ranges for text extraction, and positions for line/column information.</p><p><strong>Query language.</strong> S-expression patterns match tree structures: <code>(function_item name: (identifier) @name)</code>. Predicates like <code>#eq?</code> and <code>#match?</code> add text constraints. Multi-pattern queries extract multiple symbol types in one pass. Scoped queries run against subtrees for targeted extraction.</p><p><strong>Semantic extraction.</strong> Type annotations, function signatures, and scope chains can be extracted from syntax trees. This provides useful semantic signal without a full type checker. The gap — inferred types, trait resolution, macro expansion — requires LSP.</p><p><strong>Grammar ecosystem.</strong> Grammars are JavaScript DSL files compiled to C parsers. The ecosystem covers all major languages with varying quality. A multi-language parser maps file extensions to grammars, with content detection as fallback.</p><p><strong>Ripgrep.</strong> High-performance text search using parallel traversal, gitignore filtering, mmap, and SIMD regex. Use as a subprocess for simplicity or via the <code>grep</code> crate family for in-process search. Combine with tree-sitter for structural validation of text matches.</p><p><strong>Glob patterns and file discovery.</strong> The <code>ignore</code> crate provides gitignore-aware directory walking with parallel support. Glob syntax (<code>*</code>, <code>**</code>, <code>?</code>, <code>[...]</code>, <code>{a,b}</code>) selects files by name pattern. Always cap results and sort for deterministic agent behavior.</p><p><strong>Code navigation.</strong> File outlines, go-to-definition, and find-references built on tree-sitter queries. A navigation index pre-computes symbol locations for fast repeated queries. These features are heuristic but cover common cases accurately.</p><p><strong>Symbol resolution.</strong> Import statement extraction, module path resolution, and cross-file symbol indexing. Each language has its own resolution rules; heuristic approaches handle 90% of cases.</p><p><strong>LSP.</strong> JSON-RPC protocol providing full semantic analysis. Core requests include definition, references, hover, and completion. More powerful than tree-sitter but heavier — use on demand for deep analysis, not as a default for every file.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>The trend in production agents is toward richer code intelligence. Early agents like the first versions of GitHub Copilot relied almost entirely on the LLM&#39;s ability to understand code from context. Newer agents increasingly pre-process code before sending it to the model — extracting outlines, resolving imports, and providing type information as structured context. This reduces hallucination, improves accuracy on refactoring tasks, and makes the agent more token-efficient. The tools in this chapter are the building blocks of that pre-processing pipeline.</p></div><h2 id="looking-ahead-chapter-12" tabindex="-1">Looking Ahead: Chapter 12 <a class="header-anchor" href="#looking-ahead-chapter-12" aria-label="Permalink to &quot;Looking Ahead: Chapter 12&quot;">​</a></h2><p>Chapter 12 covers version control integration — working with Git repositories, understanding diffs, staging changes, and managing branches. The code intelligence skills from this chapter directly feed into version control features:</p><ul><li><strong>File discovery</strong> identifies which files are tracked, modified, or untracked in the repository.</li><li><strong>Structural analysis</strong> helps the agent understand what changed in a diff — not just which lines were modified, but which functions, structs, or modules were affected.</li><li><strong>Symbol resolution</strong> connects changes across files — when a function signature changes in one file, the agent can find and update all callers.</li><li><strong>Code navigation</strong> powers intelligent commit message generation — the agent can describe changes in terms of their semantic impact rather than raw line counts.</li></ul><p>The code intelligence stack you built here becomes the foundation for making the agent a thoughtful collaborator on version-controlled codebases.</p><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><h3 id="exercise-1-grep-vs-ast-based-search-trade-offs-easy" tabindex="-1">Exercise 1: Grep vs. AST-Based Search Trade-offs (Easy) <a class="header-anchor" href="#exercise-1-grep-vs-ast-based-search-trade-offs-easy" aria-label="Permalink to &quot;Exercise 1: Grep vs. AST-Based Search Trade-offs (Easy)&quot;">​</a></h3><p>A user asks the agent to &quot;find all functions that accept a <code>Config</code> parameter.&quot; Compare how you would solve this with ripgrep alone versus tree-sitter queries. For each approach, describe the query you would write, the false positives or negatives you would expect, and the performance characteristics. Under what circumstances is the simpler ripgrep approach good enough, and when does the tree-sitter approach become necessary?</p><h3 id="exercise-2-write-a-tree-sitter-query-for-error-handling-patterns-medium" tabindex="-1">Exercise 2: Write a Tree-sitter Query for Error Handling Patterns (Medium) <a class="header-anchor" href="#exercise-2-write-a-tree-sitter-query-for-error-handling-patterns-medium" aria-label="Permalink to &quot;Exercise 2: Write a Tree-sitter Query for Error Handling Patterns (Medium)&quot;">​</a></h3><p>Design a tree-sitter S-expression query that matches Rust functions containing <code>unwrap()</code> calls that are not inside test modules. Think through the node types involved: <code>function_item</code>, <code>call_expression</code>, <code>method_call</code>, <code>identifier</code>, and <code>mod_item</code> with <code>#[cfg(test)]</code>. Write out the query pattern and explain which parts are straightforward to match structurally and which would require post-processing in Rust code. Consider how your approach would differ for Python&#39;s bare <code>except:</code> clauses.</p><h3 id="exercise-3-code-navigation-strategy-design-medium" tabindex="-1">Exercise 3: Code Navigation Strategy Design (Medium) <a class="header-anchor" href="#exercise-3-code-navigation-strategy-design-medium" aria-label="Permalink to &quot;Exercise 3: Code Navigation Strategy Design (Medium)&quot;">​</a></h3><p>You are building a &quot;go to definition&quot; feature for an agent that must work across five languages (Rust, Python, TypeScript, Go, Java) without requiring language servers to be installed. Design a heuristic strategy using only tree-sitter and ripgrep. For each language, identify the definition patterns you would match (function declarations, class definitions, type definitions) and the resolution rules for imports. Where would your heuristic approach fail compared to LSP, and how would you communicate those limitations to the model?</p><h3 id="exercise-4-symbol-resolution-across-a-monorepo-hard" tabindex="-1">Exercise 4: Symbol Resolution Across a Monorepo (Hard) <a class="header-anchor" href="#exercise-4-symbol-resolution-across-a-monorepo-hard" aria-label="Permalink to &quot;Exercise 4: Symbol Resolution Across a Monorepo (Hard)&quot;">​</a></h3><p>Consider a monorepo with 50 packages across Rust, TypeScript, and Python. A user asks the agent to &quot;rename the <code>process_event</code> function everywhere it is used.&quot; Design a multi-layer resolution strategy that identifies: (a) the canonical definition, (b) all direct call sites, (c) re-exports and aliases, and (d) dynamic references (strings containing the function name in configs or tests). For each category, specify which code intelligence layer (glob, ripgrep, tree-sitter, LSP) you would use and why. Discuss the trade-off between completeness (finding every reference) and precision (avoiding false renames).</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>The code intelligence stack has four layers — file discovery (glob/ignore), text search (ripgrep), structural analysis (tree-sitter), and semantic analysis (LSP) — each more powerful but more expensive than the last</li><li>A well-designed agent pipelines these layers: glob to find files, ripgrep to narrow candidates, tree-sitter for structural precision, LSP for deep semantic questions</li><li>Tree-sitter is the workhorse: instant startup, error-tolerant parsing, language-agnostic API, and a declarative query language make it the default tool for most code intelligence tasks</li><li>LSP fills the gaps that tree-sitter cannot: type inference, cross-file resolution, trait dispatch, and macro expansion — use it on demand, not as a default</li><li>These code intelligence capabilities directly enable the version control features in the next chapter, where understanding code structure improves diff analysis, commit generation, and cross-file change tracking</li></ul>`,43)])])}const g=t(r,[["render",n]]);export{h as __pageData,g as default};
