import{_ as s,o as a,c as n,a2 as o}from"./chunks/framework.B72_uj9U.js";const y=JSON.parse('{"title":"Cargo Basics","description":"Learn how Cargo manages dependencies, builds projects, and runs tests as the central tool in every Rust workflow.","frontmatter":{"title":"Cargo Basics","description":"Learn how Cargo manages dependencies, builds projects, and runs tests as the central tool in every Rust workflow."},"headers":[],"relativePath":"learn/project/01-hello-rust-cli/03-cargo-basics.md","filePath":"learn/project/01-hello-rust-cli/03-cargo-basics.md"}'),t={name:"learn/project/01-hello-rust-cli/03-cargo-basics.md"};function l(c,e,p,i,r,d){return a(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="cargo-basics" tabindex="-1">Cargo Basics <a class="header-anchor" href="#cargo-basics" aria-label="Permalink to &quot;Cargo Basics&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How to create a new project with <code>cargo new</code> and understand the generated file layout</li><li>How to add, update, and manage crate dependencies in <code>Cargo.toml</code></li><li>How to use <code>cargo build</code>, <code>cargo run</code>, and <code>cargo test</code> in your daily development cycle</li></ul></blockquote><p>Cargo is the tool you reach for every single day as a Rust developer. It creates projects, manages dependencies, compiles your code, runs tests, generates documentation, and publishes packages. In the Python world, you need separate tools for each of these tasks — <code>pip</code> for dependencies, <code>pytest</code> for tests, <code>build</code> for packaging, <code>black</code> for formatting, <code>pyproject.toml</code> for configuration. Cargo does all of it in one unified tool.</p><p>Let&#39;s get familiar with it by creating a project from scratch.</p><h2 id="creating-a-new-project" tabindex="-1">Creating a New Project <a class="header-anchor" href="#creating-a-new-project" aria-label="Permalink to &quot;Creating a New Project&quot;">​</a></h2><p>Open your terminal and run:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> new</span><span style="color:#A6E3A1;"> kodai</span></span>
<span class="line"><span style="color:#F38BA8;font-style:italic;">cd</span><span style="color:#A6E3A1;"> kodai</span></span></code></pre></div><p>The name <code>kodai</code> is what we call the coding agent you build throughout this book (you can choose any name, but the examples use <code>kodai</code>). Cargo creates a new directory with this structure:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>kodai/</span></span>
<span class="line"><span>  Cargo.toml</span></span>
<span class="line"><span>  src/</span></span>
<span class="line"><span>    main.rs</span></span>
<span class="line"><span>  .gitignore</span></span></code></pre></div><p>Cargo also initializes a Git repository automatically. Let&#39;s look at each file.</p><h3 id="cargo-toml-—-the-project-manifest" tabindex="-1">Cargo.toml — The Project Manifest <a class="header-anchor" href="#cargo-toml-—-the-project-manifest" aria-label="Permalink to &quot;Cargo.toml — The Project Manifest&quot;">​</a></h3><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">package</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CDD6F4;">name </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;kodai&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">version </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;0.1.0&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">edition </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;2021&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">dependencies</span><span style="color:#9399B2;">]</span></span></code></pre></div><p>This is Rust&#39;s equivalent of <code>pyproject.toml</code>. The <code>[package]</code> section describes your project, and <code>[dependencies]</code> is where you list external crates (Rust&#39;s term for packages/libraries).</p><p>The <code>edition</code> field specifies which Rust edition your code uses. Editions are how Rust introduces breaking changes without breaking old code — think of it like Python 2 vs. Python 3, but backward-compatible. The <code>2021</code> edition is standard for all new projects.</p><h3 id="src-main-rs-—-the-entry-point" tabindex="-1">src/main.rs — The Entry Point <a class="header-anchor" href="#src-main-rs-—-the-entry-point" aria-label="Permalink to &quot;src/main.rs — The Entry Point&quot;">​</a></h3><div class="language-rust"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">fn</span><span style="color:#89B4FA;font-style:italic;"> main</span><span style="color:#9399B2;">()</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">    println!</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;Hello, world!&quot;</span><span style="color:#9399B2;">);</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>Every Rust binary needs a <code>main</code> function in <code>src/main.rs</code>. This is where execution begins — the equivalent of Python&#39;s <code>if __name__ == &quot;__main__&quot;:</code> block.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>In Python, <code>pyproject.toml</code> describes your project metadata and dependencies. Rust&#39;s <code>Cargo.toml</code> serves exactly the same purpose, but Cargo also replaces <code>pip</code>, <code>venv</code>, <code>build</code>, and <code>setuptools</code>. One file, one tool, everything works together.</p></div><h2 id="building-and-running" tabindex="-1">Building and Running <a class="header-anchor" href="#building-and-running" aria-label="Permalink to &quot;Building and Running&quot;">​</a></h2><p>The two commands you use most often:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#6C7086;font-style:italic;"># Compile the project (debug mode by default)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> build</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Compile AND run the project</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> run</span></span></code></pre></div><p><code>cargo run</code> is what you want 90% of the time during development — it compiles your code and immediately executes the resulting binary. You should see:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>   Compiling kodai v0.1.0 (/path/to/kodai)</span></span>
<span class="line"><span>    Finished \`dev\` profile [unoptimized + debuginfo] target(s) in 0.50s</span></span>
<span class="line"><span>     Running \`target/debug/kodai\`</span></span>
<span class="line"><span>Hello, world!</span></span></code></pre></div><p>Notice the <code>[unoptimized + debuginfo]</code> tag. By default, <code>cargo build</code> produces a <strong>debug build</strong> — fast to compile but slower to run. When you are ready to ship, you create a <strong>release build</strong>:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> build</span><span style="color:#A6E3A1;"> --release</span></span></code></pre></div><p>Release builds take longer to compile but produce a significantly faster binary. The output goes to <code>target/release/kodai</code> instead of <code>target/debug/kodai</code>.</p><h2 id="checking-without-building" tabindex="-1">Checking Without Building <a class="header-anchor" href="#checking-without-building" aria-label="Permalink to &quot;Checking Without Building&quot;">​</a></h2><p>Sometimes you just want to know if your code compiles without producing a binary:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> check</span></span></code></pre></div><p>This is faster than <code>cargo build</code> because it skips code generation. Use it as a quick feedback loop while developing: make a change, run <code>cargo check</code>, see if it compiles.</p><h2 id="adding-dependencies" tabindex="-1">Adding Dependencies <a class="header-anchor" href="#adding-dependencies" aria-label="Permalink to &quot;Adding Dependencies&quot;">​</a></h2><p>The Rust ecosystem publishes crates to <a href="https://crates.io" target="_blank" rel="noreferrer">crates.io</a>, the official package registry (equivalent to PyPI). To add a dependency:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> add</span><span style="color:#A6E3A1;"> serde</span></span></code></pre></div><p>This adds the <code>serde</code> crate to your <code>Cargo.toml</code>:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">dependencies</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CDD6F4;">serde </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;1.0.219&quot;</span></span></code></pre></div><p>The version string <code>&quot;1.0.219&quot;</code> follows semantic versioning. By default, Cargo uses a caret requirement: <code>&quot;1.0.219&quot;</code> means &quot;any version &gt;= 1.0.219 and &lt; 2.0.0&quot;. This gives you bug fixes and minor updates automatically while protecting you from breaking changes.</p><p>You can also add dependencies with specific features enabled:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> add</span><span style="color:#A6E3A1;"> serde</span><span style="color:#A6E3A1;"> --features</span><span style="color:#A6E3A1;"> derive</span></span></code></pre></div><p>Features are Rust&#39;s way of enabling optional functionality in a crate. The <code>derive</code> feature of <code>serde</code>, for example, lets you automatically generate serialization code using annotations.</p><h3 id="cargo-lock-—-the-exact-dependency-snapshot" tabindex="-1">Cargo.lock — The Exact Dependency Snapshot <a class="header-anchor" href="#cargo-lock-—-the-exact-dependency-snapshot" aria-label="Permalink to &quot;Cargo.lock — The Exact Dependency Snapshot&quot;">​</a></h3><p>When you build for the first time after adding a dependency, Cargo creates a <code>Cargo.lock</code> file. This records the exact version of every dependency (including transitive dependencies). It serves the same purpose as <code>pip freeze &gt; requirements.txt</code> or Poetry&#39;s <code>poetry.lock</code>.</p><p>For binary projects (like our coding agent), you <strong>should</strong> commit <code>Cargo.lock</code> to version control. It ensures everyone building your project gets exactly the same dependency versions.</p><h2 id="running-tests" tabindex="-1">Running Tests <a class="header-anchor" href="#running-tests" aria-label="Permalink to &quot;Running Tests&quot;">​</a></h2><p>Cargo has a built-in test runner:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> test</span></span></code></pre></div><p>This compiles your code with the test harness enabled and runs all functions annotated with <code>#[test]</code>. Let&#39;s add a simple test. Open <code>src/main.rs</code> and replace its contents with:</p><div class="language-rust"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">fn</span><span style="color:#89B4FA;font-style:italic;"> greet</span><span style="color:#9399B2;">(</span><span style="color:#EBA0AC;">name</span><span style="color:#94E2D5;">:</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#F9E2AF;font-style:italic;">str</span><span style="color:#9399B2;">)</span><span style="color:#94E2D5;"> -&gt;</span><span style="color:#F9E2AF;font-style:italic;"> String</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">    format!</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;Hello, </span><span style="color:#9399B2;">{</span><span style="color:#A6E3A1;">name</span><span style="color:#9399B2;">}</span><span style="color:#A6E3A1;">!&quot;</span><span style="color:#9399B2;">)</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span>
<span class="line"></span>
<span class="line"><span style="color:#CBA6F7;">fn</span><span style="color:#89B4FA;font-style:italic;"> main</span><span style="color:#9399B2;">()</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">    println!</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;</span><span style="color:#9399B2;">{}</span><span style="color:#A6E3A1;">&quot;</span><span style="color:#9399B2;">,</span><span style="color:#89B4FA;font-style:italic;"> greet</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;world&quot;</span><span style="color:#9399B2;">));</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F9E2AF;font-style:italic;">#</span><span style="color:#9399B2;">[</span><span style="color:#F9E2AF;font-style:italic;">cfg</span><span style="color:#9399B2;">(</span><span style="color:#F9E2AF;font-style:italic;">test</span><span style="color:#9399B2;">)]</span></span>
<span class="line"><span style="color:#CBA6F7;">mod</span><span style="color:#FAB387;"> tests</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#CBA6F7;">    use</span><span style="color:#CBA6F7;"> super</span><span style="color:#94E2D5;">::*</span><span style="color:#9399B2;">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F9E2AF;font-style:italic;">    #</span><span style="color:#9399B2;">[</span><span style="color:#F9E2AF;font-style:italic;">test</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CBA6F7;">    fn</span><span style="color:#89B4FA;font-style:italic;"> test_greet</span><span style="color:#9399B2;">()</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">        assert_eq!</span><span style="color:#9399B2;">(</span><span style="color:#89B4FA;font-style:italic;">greet</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;Rust&quot;</span><span style="color:#9399B2;">),</span><span style="color:#A6E3A1;"> &quot;Hello, Rust!&quot;</span><span style="color:#9399B2;">);</span></span>
<span class="line"><span style="color:#9399B2;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F9E2AF;font-style:italic;">    #</span><span style="color:#9399B2;">[</span><span style="color:#F9E2AF;font-style:italic;">test</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CBA6F7;">    fn</span><span style="color:#89B4FA;font-style:italic;"> test_greet_empty</span><span style="color:#9399B2;">()</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">        assert_eq!</span><span style="color:#9399B2;">(</span><span style="color:#89B4FA;font-style:italic;">greet</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;&quot;</span><span style="color:#9399B2;">),</span><span style="color:#A6E3A1;"> &quot;Hello, !&quot;</span><span style="color:#9399B2;">);</span></span>
<span class="line"><span style="color:#9399B2;">    }</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>Now run <code>cargo test</code>:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>   Compiling kodai v0.1.0 (/path/to/kodai)</span></span>
<span class="line"><span>    Finished \`test\` profile [unoptimized + debuginfo] target(s) in 0.50s</span></span>
<span class="line"><span>     Running unittests src/main.rs (target/debug/deps/kodai-abc123)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>running 2 tests</span></span>
<span class="line"><span>test tests::test_greet ... ok</span></span>
<span class="line"><span>test tests::test_greet_empty ... ok</span></span>
<span class="line"><span></span></span>
<span class="line"><span>test result: ok. 2 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out</span></span></code></pre></div><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>In Python, you typically write tests in separate files and run them with <code>pytest</code>. In Rust, tests live <em>inside the same file</em> as the code they test, wrapped in a <code>#[cfg(test)]</code> module. This means tests are always right next to the implementation — no hunting for the matching test file. The <code>#[cfg(test)]</code> attribute ensures test code is not included in the release binary.</p></div><h2 id="other-useful-cargo-commands" tabindex="-1">Other Useful Cargo Commands <a class="header-anchor" href="#other-useful-cargo-commands" aria-label="Permalink to &quot;Other Useful Cargo Commands&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Command</th><th>What It Does</th><th>Python Equivalent</th></tr></thead><tbody><tr><td><code>cargo fmt</code></td><td>Format all code</td><td><code>black .</code></td></tr><tr><td><code>cargo clippy</code></td><td>Run the linter</td><td><code>flake8</code> or <code>pylint</code></td></tr><tr><td><code>cargo doc --open</code></td><td>Generate and open documentation</td><td><code>sphinx-build</code></td></tr><tr><td><code>cargo update</code></td><td>Update dependencies to latest compatible versions</td><td><code>pip install --upgrade</code></td></tr><tr><td><code>cargo clean</code></td><td>Delete compiled artifacts (<code>target/</code>)</td><td><code>rm -rf __pycache__/ .pyc</code></td></tr></tbody></table><p>A typical development session looks like this:</p><ol><li>Write code</li><li><code>cargo run</code> to compile and test your changes</li><li><code>cargo clippy</code> to catch common mistakes</li><li><code>cargo fmt</code> to keep formatting consistent</li><li><code>cargo test</code> to verify nothing is broken</li></ol><p>You will use this workflow for the rest of the book.</p><h2 id="the-target-directory" tabindex="-1">The target/ Directory <a class="header-anchor" href="#the-target-directory" aria-label="Permalink to &quot;The target/ Directory&quot;">​</a></h2><p>When you build, Cargo puts all compiled artifacts in the <code>target/</code> directory:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>target/</span></span>
<span class="line"><span>  debug/           # Debug builds</span></span>
<span class="line"><span>    kodai          # Your compiled binary</span></span>
<span class="line"><span>    deps/          # Compiled dependencies</span></span>
<span class="line"><span>    build/         # Build script outputs</span></span>
<span class="line"><span>  release/         # Release builds (when using --release)</span></span></code></pre></div><p>This directory can grow large (hundreds of megabytes for projects with many dependencies). It is already in your <code>.gitignore</code> — never commit it to version control. If you need to free disk space, <code>cargo clean</code> deletes the entire <code>target/</code> directory.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li><code>cargo new &lt;name&gt;</code> creates a complete project with <code>Cargo.toml</code>, <code>src/main.rs</code>, and a git repository — ready to build immediately.</li><li><code>cargo run</code> is your primary command during development — it compiles and runs your binary in one step.</li><li>Dependencies are added with <code>cargo add &lt;crate&gt;</code> and recorded in <code>Cargo.toml</code>. The <code>Cargo.lock</code> file pins exact versions for reproducible builds.</li><li>Tests live alongside your code in <code>#[cfg(test)]</code> modules and run with <code>cargo test</code> — no separate test framework needed.</li><li><code>cargo fmt</code> and <code>cargo clippy</code> keep your code clean and idiomatic — use them early and often.</li></ul>`,61)])])}const h=s(t,[["render",l]]);export{y as __pageData,h as default};
