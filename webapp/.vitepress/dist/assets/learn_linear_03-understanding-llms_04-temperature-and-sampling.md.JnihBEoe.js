import{_ as t,o as a,c as s,a2 as o}from"./chunks/framework.B72_uj9U.js";const m=JSON.parse('{"title":"Temperature and Sampling","description":"How temperature, top-p, and other sampling parameters control LLM output randomness and their optimal settings for coding agents.","frontmatter":{"title":"Temperature and Sampling","description":"How temperature, top-p, and other sampling parameters control LLM output randomness and their optimal settings for coding agents."},"headers":[],"relativePath":"learn/linear/03-understanding-llms/04-temperature-and-sampling.md","filePath":"learn/linear/03-understanding-llms/04-temperature-and-sampling.md"}'),n={name:"learn/linear/03-understanding-llms/04-temperature-and-sampling.md"};function r(l,e,i,p,c,d){return a(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="temperature-and-sampling" tabindex="-1">Temperature and Sampling <a class="header-anchor" href="#temperature-and-sampling" aria-label="Permalink to &quot;Temperature and Sampling&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How temperature scales the probability distribution over tokens and affects output creativity vs determinism</li><li>What top-p (nucleus sampling) and top-k do and when to use them alongside temperature</li><li>The recommended sampling settings for coding agents where correctness matters more than creativity</li></ul></blockquote><p>When the model predicts the next token, it does not just pick the single most likely one. It produces a probability distribution across its entire vocabulary -- tens of thousands of possible tokens, each with an associated probability. Sampling parameters control how the model selects from this distribution. Getting these settings right for a coding agent is important: too much randomness and you get unreliable code, too little and you get repetitive, unimaginative solutions.</p><h2 id="how-temperature-works" tabindex="-1">How Temperature Works <a class="header-anchor" href="#how-temperature-works" aria-label="Permalink to &quot;How Temperature Works&quot;">​</a></h2><p>Temperature is a single number, typically between 0 and 2, that controls the &quot;sharpness&quot; of the probability distribution. Here is the intuition:</p><p><strong>Temperature = 0 (or very close to 0):</strong> The model always picks the single highest-probability token. This is called &quot;greedy decoding.&quot; The output is deterministic -- the same input always produces the same output.</p><p><strong>Temperature = 1.0:</strong> The model samples directly from the learned probability distribution. If token A has probability 0.7 and token B has probability 0.2, the model picks A roughly 70% of the time and B roughly 20% of the time.</p><p><strong>Temperature &gt; 1.0:</strong> The distribution is flattened, making less-likely tokens more probable. This produces more creative, surprising, and often less coherent output.</p><p><strong>Temperature &lt; 1.0:</strong> The distribution is sharpened, making the most-likely tokens even more dominant. This produces more predictable, conservative output.</p><p>Mathematically, temperature divides the logits (raw model scores) before the softmax function:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>probability(token_i) = exp(logit_i / temperature) / sum(exp(logit_j / temperature))</span></span></code></pre></div><p>You do not need to memorize that formula. Just remember: <strong>lower temperature = more focused and deterministic, higher temperature = more varied and creative</strong>.</p><p>Let&#39;s see a concrete example. Suppose the model is generating the next token after <code>let result = </code> and the top candidates are:</p><table tabindex="0"><thead><tr><th>Token</th><th>Probability (T=1.0)</th><th>Probability (T=0.3)</th><th>Probability (T=1.5)</th></tr></thead><tbody><tr><td><code>vec</code></td><td>0.35</td><td>0.72</td><td>0.25</td></tr><tr><td><code>String</code></td><td>0.25</td><td>0.18</td><td>0.21</td></tr><tr><td><code>Ok</code></td><td>0.20</td><td>0.07</td><td>0.19</td></tr><tr><td><code>HashMap</code></td><td>0.10</td><td>0.02</td><td>0.16</td></tr><tr><td><code>fetch</code></td><td>0.05</td><td>0.001</td><td>0.10</td></tr><tr><td>other</td><td>0.05</td><td>0.001</td><td>0.09</td></tr></tbody></table><p>At T=0.3, <code>vec</code> is chosen about 72% of the time -- the model is very confident. At T=1.5, the distribution is much flatter, and even unlikely tokens like <code>fetch</code> get a 10% chance. This is how temperature controls the randomness of generation.</p><h2 id="top-p-nucleus-sampling" tabindex="-1">Top-p (Nucleus Sampling) <a class="header-anchor" href="#top-p-nucleus-sampling" aria-label="Permalink to &quot;Top-p (Nucleus Sampling)&quot;">​</a></h2><p>Top-p is an alternative (or complement) to temperature that works differently. Instead of scaling the entire distribution, it truncates it.</p><p>With top-p = 0.9, the model considers only the smallest set of tokens whose cumulative probability is at least 90%, and samples from just those tokens. Everything below that threshold is excluded.</p><p>Using our example above with T=1.0:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>top_p = 0.9:</span></span>
<span class="line"><span>  vec (0.35) + String (0.25) + Ok (0.20) + HashMap (0.10) = 0.90</span></span>
<span class="line"><span>  → Sample from {vec, String, Ok, HashMap} only</span></span>
<span class="line"><span>  → &quot;fetch&quot; and all rarer tokens are excluded</span></span></code></pre></div><p>Top-p dynamically adapts to the model&#39;s confidence. When the model is very confident (one token has 0.95 probability), top-p = 0.9 effectively selects just that one token. When the model is uncertain (many tokens with similar probability), top-p = 0.9 keeps many candidates in the pool.</p><h2 id="top-k-sampling" tabindex="-1">Top-k Sampling <a class="header-anchor" href="#top-k-sampling" aria-label="Permalink to &quot;Top-k Sampling&quot;">​</a></h2><p>Top-k is simpler: it considers only the k most probable tokens. With top-k = 40, the model samples from the 40 highest-probability tokens regardless of their actual probabilities.</p><p>Top-k is less commonly used in modern APIs. OpenAI does not expose a top-k parameter. Anthropic supports it but recommends using top-p instead for most use cases.</p><h2 id="how-parameters-interact" tabindex="-1">How Parameters Interact <a class="header-anchor" href="#how-parameters-interact" aria-label="Permalink to &quot;How Parameters Interact&quot;">​</a></h2><p>You can combine temperature and top-p. When both are set, they apply sequentially:</p><ol><li>Temperature first adjusts the distribution</li><li>Top-p then truncates the adjusted distribution</li></ol><p>The API docs for both Anthropic and OpenAI recommend <strong>changing one or the other, not both simultaneously</strong>. In practice, for coding agents:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">{</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">temperature</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;"> 0.0</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">top_p</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;"> 1.0</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>or equivalently, just set temperature to 0 and leave top-p at its default.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>If you have used the OpenAI Python SDK, you have seen these parameters in <code>client.chat.completions.create(temperature=0.7, top_p=0.9)</code>. The Rust implementation passes them as fields in a JSON request body. The semantics are identical across languages -- these are API parameters, not SDK features.</p></div><h2 id="optimal-settings-for-coding-agents" tabindex="-1">Optimal Settings for Coding Agents <a class="header-anchor" href="#optimal-settings-for-coding-agents" aria-label="Permalink to &quot;Optimal Settings for Coding Agents&quot;">​</a></h2><p>For a coding agent, the priority is <strong>correctness and consistency</strong> over creativity. You want the model to:</p><ul><li>Generate syntactically valid code reliably</li><li>Make predictable tool use decisions</li><li>Not introduce random variations between runs</li></ul><p>The recommended settings:</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Recommended Value</th><th>Reasoning</th></tr></thead><tbody><tr><td>temperature</td><td>0.0 - 0.2</td><td>Minimize randomness for code generation</td></tr><tr><td>top_p</td><td>1.0 (default)</td><td>Let temperature handle sampling; do not combine</td></tr><tr><td>max_tokens</td><td>Model maximum (e.g., 8192)</td><td>Let the model decide response length</td></tr></tbody></table><p><strong>Temperature 0</strong> is the safest choice for most agent operations. When the model decides which tool to call, you want deterministic behavior -- the same situation should produce the same tool call every time. When generating code, you want the most likely (and typically most correct) completion.</p><p>There are cases where a slightly higher temperature (0.1-0.3) can help:</p><ul><li><strong>When the model gets stuck in a loop</strong> -- repeating the same failed approach. A small amount of randomness can help it try a different path.</li><li><strong>When generating creative content</strong> -- if the agent is writing documentation or commit messages, a bit of variety is welcome.</li><li><strong>When exploring multiple solutions</strong> -- some agent architectures generate several candidate solutions at higher temperature and then evaluate them.</li></ul><p>However, for your initial agent implementation, start with temperature 0 and only increase it if you observe specific problems that randomness would solve.</p><h2 id="the-stop-parameter" tabindex="-1">The <code>stop</code> Parameter <a class="header-anchor" href="#the-stop-parameter" aria-label="Permalink to &quot;The \`stop\` Parameter&quot;">​</a></h2><p>Beyond sampling, you can control generation with the <code>stop</code> parameter -- a list of sequences that cause the model to stop generating when encountered:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">{</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">model</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;claude-sonnet-4-20250514&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">max_tokens</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;"> 4096</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">temperature</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;"> 0</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">stop_sequences</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> [</span><span style="color:#A6E3A1;">&quot;</span><span style="color:#F5C2E7;">\\n</span><span style="color:#A6E3A1;">\`\`\`</span><span style="color:#F5C2E7;">\\n</span><span style="color:#A6E3A1;">&quot;</span><span style="color:#9399B2;">],</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">messages</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> [</span><span style="color:#CDD6F4;">...</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>This is rarely needed for agents that use the tool use protocol (the model stops naturally after generating a tool call), but it can be useful if you are doing raw text generation and want to prevent runaway output.</p><h2 id="seed-parameter-for-reproducibility" tabindex="-1">Seed Parameter for Reproducibility <a class="header-anchor" href="#seed-parameter-for-reproducibility" aria-label="Permalink to &quot;Seed Parameter for Reproducibility&quot;">​</a></h2><p>OpenAI offers a <code>seed</code> parameter that, combined with temperature 0, provides near-deterministic outputs:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">{</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">model</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;gpt-4o&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">temperature</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;"> 0</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">seed</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;"> 42</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">  &quot;</span><span style="color:#89B4FA;">messages</span><span style="color:#9399B2;">&quot;</span><span style="color:#9399B2;">:</span><span style="color:#9399B2;"> [</span><span style="color:#CDD6F4;">...</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>Anthropic does not currently offer a seed parameter, but temperature 0 is already nearly deterministic for most practical purposes. The rare cases where temperature 0 produces different outputs are due to internal floating-point nondeterminism, and they almost never change the semantic content of the response.</p><p>For testing and debugging your agent, deterministic outputs are valuable. If you can reproduce the exact same model response for a given input, debugging unexpected behavior becomes much easier.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Production coding agents typically use temperature 0 or very close to it. Claude Code uses a low temperature setting for tool use decisions where consistency matters. Some agents implement a dynamic temperature strategy -- using temperature 0 for code generation and tool use, but bumping it up slightly for conversational responses to feel more natural. This is a refinement you can add later, but starting with a fixed low temperature is the right approach.</p></div><h2 id="common-pitfalls" tabindex="-1">Common Pitfalls <a class="header-anchor" href="#common-pitfalls" aria-label="Permalink to &quot;Common Pitfalls&quot;">​</a></h2><p><strong>Pitfall 1: Using high temperature for code generation.</strong> Temperature 0.7 or above produces creative-sounding prose but introduces bugs into code. Variable names vary randomly, sometimes the model picks a less common API pattern, and occasionally it generates completely invalid syntax.</p><p><strong>Pitfall 2: Setting both temperature and top-p.</strong> This creates confusing interactions. If you set temperature to 0.5 and top-p to 0.5, the effective behavior is hard to reason about. Pick one lever and leave the other at its default.</p><p><strong>Pitfall 3: Ignoring these parameters entirely.</strong> Many APIs default to temperature 1.0, which is too high for agent use. Always explicitly set temperature in your agent&#39;s API calls.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>Temperature controls the sharpness of the probability distribution: lower values (0-0.2) produce deterministic, consistent output ideal for coding agents; higher values (0.7+) increase randomness</li><li>Top-p (nucleus sampling) truncates the distribution to the most probable tokens and adapts dynamically to model confidence -- use one or the other, not both</li><li>For coding agents, temperature 0 is the recommended starting point because correctness and consistency matter more than creativity</li><li>Always explicitly set temperature in your API calls -- the default (often 1.0) is too high for agent operations</li><li>The seed parameter (OpenAI) provides additional reproducibility for testing, and temperature 0 alone is nearly deterministic in practice</li></ul>`,56)])])}const h=t(n,[["render",r]]);export{m as __pageData,h as default};
