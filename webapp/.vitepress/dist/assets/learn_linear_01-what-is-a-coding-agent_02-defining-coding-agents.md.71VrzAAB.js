import{_ as t,o,c as a,a2 as i}from"./chunks/framework.B72_uj9U.js";const g=JSON.parse('{"title":"Defining Coding Agents","description":"A precise definition of coding agents, distinguishing them from code assistants, copilots, and general-purpose chatbots.","frontmatter":{"title":"Defining Coding Agents","description":"A precise definition of coding agents, distinguishing them from code assistants, copilots, and general-purpose chatbots."},"headers":[],"relativePath":"learn/linear/01-what-is-a-coding-agent/02-defining-coding-agents.md","filePath":"learn/linear/01-what-is-a-coding-agent/02-defining-coding-agents.md"}'),n={name:"learn/linear/01-what-is-a-coding-agent/02-defining-coding-agents.md"};function s(r,e,l,h,c,d){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="defining-coding-agents" tabindex="-1">Defining Coding Agents <a class="header-anchor" href="#defining-coding-agents" aria-label="Permalink to &quot;Defining Coding Agents&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>The formal characteristics that define a coding agent versus other AI coding tools</li><li>The three essential capabilities every coding agent must possess: perception, reasoning, and action</li><li>How autonomy, tool use, and iterative execution separate agents from simpler systems</li></ul></blockquote><h2 id="what-exactly-is-a-coding-agent" tabindex="-1">What Exactly Is a Coding Agent? <a class="header-anchor" href="#what-exactly-is-a-coding-agent" aria-label="Permalink to &quot;What Exactly Is a Coding Agent?&quot;">​</a></h2><p>The term &quot;coding agent&quot; gets thrown around loosely. Marketing departments apply it to everything from a glorified autocomplete plugin to a fully autonomous system that can implement features end-to-end. Before we spend an entire tutorial track building one, let&#39;s pin down exactly what we mean.</p><p>A <strong>coding agent</strong> is a software system that uses a large language model to autonomously perform multi-step programming tasks by perceiving the state of a codebase, reasoning about what actions to take, and executing those actions through tools — iterating until the task is complete or the system determines it cannot proceed.</p><p>That&#39;s a dense definition, so let&#39;s unpack it piece by piece.</p><h2 id="the-three-pillars-perceive-reason-act" tabindex="-1">The Three Pillars: Perceive, Reason, Act <a class="header-anchor" href="#the-three-pillars-perceive-reason-act" aria-label="Permalink to &quot;The Three Pillars: Perceive, Reason, Act&quot;">​</a></h2><p>Every coding agent, regardless of its implementation language or target use case, must have three core capabilities. Think of these as the pillars that hold up the entire system.</p><h3 id="perception-reading-the-world" tabindex="-1">Perception: Reading the World <a class="header-anchor" href="#perception-reading-the-world" aria-label="Permalink to &quot;Perception: Reading the World&quot;">​</a></h3><p>A coding agent must be able to observe its environment. In practice, this means reading files from the filesystem, examining directory structures, searching through code for patterns, inspecting git history, reading terminal output, and understanding error messages.</p><p>If you think of a Python developer working on a bug fix, the perception phase is everything they do before they start typing. They read the stack trace, open the relevant source file, check what the function is supposed to do, look at the test that&#39;s failing, maybe search the codebase for other callers of the function. A coding agent does all of this, but through tools — a <code>read_file</code> tool, a <code>search</code> tool, a <code>list_directory</code> tool.</p><p>The quality of perception directly determines the quality of everything that follows. An agent that reads only the file it intends to modify, without understanding the broader context, will produce code that compiles in isolation but breaks the system. This is why every production coding agent invests heavily in perception tools.</p><h3 id="reasoning-deciding-what-to-do" tabindex="-1">Reasoning: Deciding What to Do <a class="header-anchor" href="#reasoning-deciding-what-to-do" aria-label="Permalink to &quot;Reasoning: Deciding What to Do&quot;">​</a></h3><p>The language model at the center of the agent is the reasoning engine. It takes in everything the agent has perceived — file contents, error messages, conversation history, the user&#39;s original request — and decides what to do next. Should it read another file to gather more context? Should it write a fix? Should it run the test suite to verify its changes? Should it ask the user a clarifying question?</p><p>This is where the magic of large language models comes in. The model isn&#39;t executing a fixed algorithm or following a decision tree. It&#39;s applying the patterns it learned during training across billions of lines of code to reason about the specific situation in front of it. It can recognize that a <code>NullPointerException</code> in Java often means a missing null check, that a failing import in Python usually means a missing dependency, that a type error in Rust often means you need to convert between <code>&amp;str</code> and <code>String</code>.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Claude Code&#39;s reasoning is particularly visible in its &quot;extended thinking&quot; mode, where the model explicitly works through its chain of thought before acting. You can watch it reason: &quot;The user wants pagination. Let me check the existing route handlers to understand the pattern. I see they use an <code>offset</code> and <code>limit</code> parameter in the database queries. I&#39;ll follow the same pattern.&quot; This externalized reasoning makes the agent&#39;s decision-making transparent and debuggable.</p></div><h3 id="action-changing-the-world" tabindex="-1">Action: Changing the World <a class="header-anchor" href="#action-changing-the-world" aria-label="Permalink to &quot;Action: Changing the World&quot;">​</a></h3><p>Perception without action is just reading. Reasoning without action is just thinking. A coding agent must be able to <em>do things</em> — write files, create directories, execute shell commands, run tests, install dependencies, make git commits.</p><p>Each action is implemented as a <strong>tool</strong>. A tool is a well-defined operation that the language model can invoke by name, passing structured parameters. When the model decides it needs to modify a file, it doesn&#39;t generate the raw text of a file write system call. Instead, it produces a structured tool call like: &quot;call the <code>write_file</code> tool with path <code>src/handlers/users.rs</code> and the following content.&quot; The agent&#39;s runtime executes this tool call and returns the result to the model.</p><p>This separation between deciding what to do (the model&#39;s job) and executing the operation (the runtime&#39;s job) is fundamental. It means the runtime can add safety checks, request user permission, log operations, enforce sandboxing — all without the model needing to know about these concerns.</p><h2 id="the-loop-that-makes-it-agentic" tabindex="-1">The Loop That Makes It Agentic <a class="header-anchor" href="#the-loop-that-makes-it-agentic" aria-label="Permalink to &quot;The Loop That Makes It Agentic&quot;">​</a></h2><p>The three pillars alone don&#39;t make an agent. What makes a system truly agentic is that perceive-reason-act happens in a <strong>loop</strong>. The agent doesn&#39;t just read, think, and write once. It reads, thinks, writes, then <em>observes the result of its writing</em>, thinks again, and possibly acts again.</p><p>Consider a concrete scenario. You ask an agent to fix a failing test. Here&#39;s what the loop might look like:</p><ol><li><strong>Perceive:</strong> Read the test failure output. Read the test file. Read the source file being tested.</li><li><strong>Reason:</strong> The test expects a function to return a list, but the function returns a single item. The fix is to wrap the return value in a list.</li><li><strong>Act:</strong> Modify the source file.</li><li><strong>Perceive:</strong> Run the test suite. Read the new output.</li><li><strong>Reason:</strong> The original test passes now, but a different test broke because it was relying on the single-item return value.</li><li><strong>Act:</strong> Modify the second test to account for the list return type.</li><li><strong>Perceive:</strong> Run the test suite again. All tests pass.</li><li><strong>Reason:</strong> Task is complete. Report the result to the user.</li></ol><p>This multi-turn loop is the heartbeat of a coding agent. Without it, you just have a chatbot that generates code in one shot and hopes for the best. The loop is what allows the agent to recover from mistakes, handle unexpected complications, and converge on a working solution.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>If you&#39;ve used Python&#39;s <code>subprocess</code> module to run commands and check their output in a script, you&#39;ve written something conceptually similar to an agentic loop — a program that takes an action, observes the result, and decides what to do next. The difference is that in an agent, the &quot;deciding what to do next&quot; part is handled by a language model rather than hardcoded <code>if/else</code> logic. Think of it as replacing your control flow with an LLM.</p></div><h2 id="the-spectrum-of-autonomy" tabindex="-1">The Spectrum of Autonomy <a class="header-anchor" href="#the-spectrum-of-autonomy" aria-label="Permalink to &quot;The Spectrum of Autonomy&quot;">​</a></h2><p>Not all coding agents are equally autonomous. It&#39;s useful to think of autonomy as a spectrum rather than a binary property.</p><p>At the <strong>low-autonomy end</strong>, you have tools like GitHub Copilot. It perceives (reads surrounding code), reasons (predicts the next tokens), and acts (inserts a suggestion), but only within a single completion. It doesn&#39;t iterate, doesn&#39;t run tests, and doesn&#39;t recover from errors. You might call it a &quot;micro-agent&quot; — agentic for one step, but not across multiple steps.</p><p>In the <strong>middle of the spectrum</strong>, you have tools like Cursor or Windsurf in their chat mode. They can read files you point them to, generate code changes, and show you diffs. But they typically require you to approve each change, run the tests yourself, and paste back any errors. They&#39;re agentic in their reasoning but rely on you for the loop.</p><p>At the <strong>high-autonomy end</strong>, you have Claude Code, Codex, and similar agents that can execute the full perceive-reason-act loop many times without human intervention. You give them a task, and they iterate until they believe they&#39;re done. You supervise the process, and you can intervene at any point, but the default mode is autonomous execution.</p><p>The agent we&#39;ll build in this tutorial sits firmly at the high-autonomy end. It will be able to read files, write files, execute commands, observe results, and iterate — all orchestrated by the language model, with you supervising rather than steering.</p><h2 id="formal-characteristics" tabindex="-1">Formal Characteristics <a class="header-anchor" href="#formal-characteristics" aria-label="Permalink to &quot;Formal Characteristics&quot;">​</a></h2><p>Let&#39;s distill the definition into a checklist. A system qualifies as a coding agent if it has all of the following:</p><ol><li><p><strong>An LLM as the reasoning core.</strong> The system uses a language model (not rule-based logic) to decide what to do at each step.</p></li><li><p><strong>Tool access.</strong> The system can perform operations on the external environment — reading and writing files, executing commands, searching code — through structured tool interfaces.</p></li><li><p><strong>An iterative execution loop.</strong> The system operates in a loop where each cycle involves perception, reasoning, and action. The result of one cycle informs the next.</p></li><li><p><strong>Autonomous multi-step execution.</strong> The system can chain multiple perceive-reason-act cycles together without requiring human input between each step.</p></li><li><p><strong>Task-oriented behavior.</strong> The system works toward completing a user-defined goal rather than simply responding to a single prompt.</p></li></ol><p>If a system lacks any one of these, it&#39;s something other than a coding agent. It might be an excellent tool — Copilot is incredibly useful — but it occupies a different category.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>OpenCode demonstrates these five characteristics clearly. Its LLM core can be swapped between providers (Anthropic, OpenAI, or local models). Its tool system gives the model access to file reading, file writing, shell execution, and LSP-powered code navigation. Its main loop runs tool calls until the model produces a response with no tool invocations, signaling task completion. And it operates autonomously — you ask it to do something, and it works through the steps until it&#39;s done.</p></div><h2 id="why-precision-matters" tabindex="-1">Why Precision Matters <a class="header-anchor" href="#why-precision-matters" aria-label="Permalink to &quot;Why Precision Matters&quot;">​</a></h2><p>You might wonder why we need such a precise definition. After all, won&#39;t we just build the thing and see what it does?</p><p>The precision matters because it guides our architecture. When we design the system in later chapters, every architectural decision maps back to these characteristics. The agentic loop is the main execution path. The tool system is the action layer. The LLM integration is the reasoning layer. The file and command tools are the perception layer. By defining these clearly now, we have a blueprint that tells us what to build and why.</p><p>It also helps you evaluate other agents. When a new tool launches and claims to be an &quot;AI coding agent,&quot; you can test it against these five characteristics. Does it have tool access? Does it iterate? Can it chain steps together autonomously? If yes, it&#39;s an agent. If not, it&#39;s something else — possibly valuable, but architecturally different.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>A coding agent is defined by five characteristics: LLM-based reasoning, tool access, an iterative execution loop, autonomous multi-step execution, and task-oriented behavior.</li><li>The perceive-reason-act cycle is the fundamental unit of agent behavior, and it&#39;s the loop — not any single step — that makes a system truly agentic.</li><li>Autonomy exists on a spectrum from single-step suggestion (Copilot) to fully autonomous multi-step execution (Claude Code, Codex), and our agent targets the high-autonomy end.</li><li>The separation between reasoning (what the model does) and execution (what the runtime does) is a core architectural principle that enables safety, logging, and user control.</li><li>This precise definition serves as an architectural blueprint: every component we build maps directly to one of the five characteristics.</li></ul>',43)])])}const p=t(n,[["render",s]]);export{g as __pageData,p as default};
