import{_ as a,o as t,c as s,a2 as o}from"./chunks/framework.B72_uj9U.js";const y=JSON.parse('{"title":"Summary","description":"Recap of the multi-provider support chapter, reviewing the provider abstraction, adapter implementations, and runtime features that make the agent provider-agnostic.","frontmatter":{"title":"Summary","description":"Recap of the multi-provider support chapter, reviewing the provider abstraction, adapter implementations, and runtime features that make the agent provider-agnostic."},"headers":[],"relativePath":"learn/project/13-multi-provider-support/12-summary.md","filePath":"learn/project/13-multi-provider-support/12-summary.md"}'),n={name:"learn/project/13-multi-provider-support/12-summary.md"};function r(i,e,l,p,c,d){return t(),s("div",null,[...e[0]||(e[0]=[o(`<h1 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How the provider trait, adapters, and runtime features work together as a complete abstraction layer</li><li>Which aspects of multi-provider support are most impactful for real-world agent deployment</li><li>How to add support for new providers as they emerge in the rapidly evolving LLM landscape</li></ul></blockquote><p>You started this chapter with an agent hardwired to a single LLM provider. You now have a provider-agnostic agent that can talk to Anthropic, OpenAI, and local Ollama models through a unified interface, with automatic fallback, cost tracking, and runtime switching. Let&#39;s review what you built and how the pieces fit together.</p><h2 id="the-architecture-at-a-glance" tabindex="-1">The Architecture at a Glance <a class="header-anchor" href="#the-architecture-at-a-glance" aria-label="Permalink to &quot;The Architecture at a Glance&quot;">​</a></h2><p>The provider system has four layers:</p><p><strong>Layer 1 -- The Trait</strong> defines the contract: <code>send_message</code> and <code>stream_message</code>. Every provider must implement these two async methods. The trait uses provider-neutral types (<code>Message</code>, <code>ContentBlock</code>, <code>StreamEvent</code>) so the agent core never touches provider-specific data structures.</p><p><strong>Layer 2 -- The Adapters</strong> translate between the generic types and each provider&#39;s API format. The Anthropic adapter maps system prompts to a top-level field and parses SSE streaming events. The OpenAI adapter wraps tools in a &quot;function&quot; envelope and reassembles chunked streaming deltas. The Ollama adapter checks model availability, handles slower local inference, and falls back to text-based tool calling when native support is unavailable.</p><p><strong>Layer 3 -- The Runtime Features</strong> add intelligence on top of the trait. The model switcher enables mid-conversation provider changes. The fallback chain tries alternative providers when the primary one fails. The cost tracker records token usage and enforces budget limits. The capabilities registry tells the agent what each model can do.</p><p><strong>Layer 4 -- The Configuration</strong> ties everything together. TOML config files, environment variables, and CLI arguments merge into a single <code>ProviderConfig</code> that constructs the entire provider stack. A single call to <code>build_fallback_chain()</code> produces a resilient, cost-aware provider ready for the agent to use.</p><h2 id="what-you-built-by-concept" tabindex="-1">What You Built, by Concept <a class="header-anchor" href="#what-you-built-by-concept" aria-label="Permalink to &quot;What You Built, by Concept&quot;">​</a></h2><p>The code snapshot keeps everything in a single <code>main.rs</code> for clarity -- you can see the entire provider system at a glance. Here is how the logical sections map to what a production codebase would look like as separate modules:</p><table tabindex="0"><thead><tr><th>Section in <code>main.rs</code></th><th>Production module</th><th>Responsibility</th></tr></thead><tbody><tr><td>Provider trait + ProviderError</td><td><code>provider/mod.rs</code></td><td>Contract and error types</td></tr><tr><td>Message, ContentBlock, StreamEvent</td><td><code>provider/types.rs</code></td><td>Provider-neutral data types</td></tr><tr><td>AnthropicProvider</td><td><code>provider/anthropic.rs</code></td><td>Messages API adapter with SSE</td></tr><tr><td>OpenAIProvider</td><td><code>provider/openai.rs</code></td><td>Chat Completions adapter</td></tr><tr><td>ModelCapabilities, CapabilityRegistry</td><td><code>provider/capabilities.rs</code></td><td>Per-model feature queries</td></tr><tr><td>FallbackChain, CircuitBreaker</td><td><code>provider/fallback.rs</code></td><td>Resilient provider wrapping</td></tr><tr><td>CostTracker, CostRecord</td><td><code>provider/cost.rs</code></td><td>Usage tracking and budget enforcement</td></tr><tr><td>ProviderConfig</td><td><code>provider/config.rs</code></td><td>Multi-source configuration loading</td></tr></tbody></table><p>Each logical section has a clear boundary and a single responsibility. The adapter sections depend only on the shared types. The runtime sections (fallback, cost tracking) depend on the trait but not on any specific adapter. This means you can add a new provider by writing one adapter -- without touching any existing code.</p><h2 id="the-design-principles" tabindex="-1">The Design Principles <a class="header-anchor" href="#the-design-principles" aria-label="Permalink to &quot;The Design Principles&quot;">​</a></h2><p>Three principles guided the design:</p><p><strong>1. The trait is minimal.</strong> The <code>Provider</code> trait has four methods: <code>name()</code>, <code>model()</code>, <code>send_message()</code>, <code>stream_message()</code>. Everything else -- capabilities, costs, configuration -- lives outside the trait. This keeps the adapter implementations focused and makes the contract easy to satisfy.</p><p><strong>2. Errors carry routing information.</strong> <code>ProviderError</code> is not just an error message -- it tells the fallback chain whether to retry (<code>is_retryable()</code>) or try a different provider (<code>should_fallback()</code>). This turns error handling into a routing decision, which is the key insight behind transparent fallback.</p><p><strong>3. Composition over inheritance.</strong> The fallback chain is a <code>Provider</code> that contains other <code>Provider</code>s. The model switcher wraps a <code>Provider</code> in a swappable container. The cost tracker decorates every provider call with accounting. These are all composition patterns -- you build complex behavior by combining simple pieces rather than extending a base class.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>This composition approach might feel unfamiliar if you are used to Python&#39;s class inheritance:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">class</span><span style="color:#F9E2AF;font-style:italic;"> RetryingProvider</span><span style="color:#9399B2;">(</span><span style="color:#F9E2AF;font-style:italic;">BaseProvider</span><span style="color:#9399B2;">):</span></span>
<span class="line"><span style="color:#CBA6F7;">    def</span><span style="color:#89B4FA;font-style:italic;"> send_message</span><span style="color:#9399B2;">(</span><span style="color:#F38BA8;font-style:italic;">self</span><span style="color:#9399B2;">,</span><span style="color:#EBA0AC;font-style:italic;"> messages</span><span style="color:#9399B2;">):</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">        # retry logic wrapping super().send_message()</span></span>
<span class="line"><span style="color:#CDD6F4;">        ...</span></span></code></pre></div><p>In Rust, you cannot inherit from a trait implementation. Instead, you wrap it:</p><div class="language-rust"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">struct</span><span style="color:#F9E2AF;font-style:italic;"> RetryingProvider</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#CDD6F4;">    inner</span><span style="color:#94E2D5;">:</span><span style="color:#F9E2AF;font-style:italic;"> Arc</span><span style="color:#89DCEB;">&lt;</span><span style="color:#CBA6F7;">dyn</span><span style="color:#F9E2AF;font-style:italic;"> Provider</span><span style="color:#89DCEB;">&gt;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>This pattern is more explicit and avoids the fragile base class problem. Each layer of composition is independent and testable in isolation.</p></div><h2 id="trait-based-polymorphism-vs-duck-typing" tabindex="-1">Trait-Based Polymorphism vs. Duck Typing <a class="header-anchor" href="#trait-based-polymorphism-vs-duck-typing" aria-label="Permalink to &quot;Trait-Based Polymorphism vs. Duck Typing&quot;">​</a></h2><p>Throughout this chapter, you have been using Rust&#39;s trait-based polymorphism as a replacement for Python&#39;s duck typing. Let&#39;s make the comparison explicit.</p><p>In Python, any object with a <code>send_message</code> method works as a provider -- no declaration needed. This is flexible but fragile. A typo like <code>send_mesage</code> will not be caught until runtime, and only if that code path is actually executed.</p><p>In Rust, a struct must explicitly declare that it implements the <code>Provider</code> trait with <code>impl Provider for MyStruct</code>. The compiler verifies that every required method exists with the correct signature. If you forget <code>stream_message</code>, the code does not compile. If you return the wrong type, the code does not compile.</p><p>Python&#39;s <code>typing.Protocol</code> closes some of this gap by providing structural type checking at type-check time (with mypy or pyright). But it is optional and not enforced at runtime. Rust&#39;s traits are enforced at compile time, always.</p><p>The practical result: when you add a new method to the <code>Provider</code> trait, the compiler immediately shows you every adapter that needs updating. In Python, you would discover missing methods through test failures -- or through production errors if test coverage is incomplete.</p><h2 id="adding-a-new-provider" tabindex="-1">Adding a New Provider <a class="header-anchor" href="#adding-a-new-provider" aria-label="Permalink to &quot;Adding a New Provider&quot;">​</a></h2><p>The architecture makes it straightforward to add support for new providers. Here is the checklist:</p><ol><li><p><strong>Create the adapter.</strong> Implement the <code>Provider</code> trait for your new provider, translating between your generic types and the new provider&#39;s API format. In a multi-file project, this would be <code>src/provider/newprovider.rs</code>.</p></li><li><p><strong>Add to the capabilities registry.</strong> Register the new provider&#39;s models with their capabilities in <code>register_defaults()</code> or through configuration.</p></li><li><p><strong>Add to the configuration schema.</strong> Add a new config section for the provider&#39;s settings (API key, base URL, default model).</p></li><li><p><strong>Update <code>build_named_provider</code>.</strong> Add a match arm that constructs the new provider from configuration.</p></li><li><p><strong>Write tests.</strong> Add recorded response tests, a mock server, and run the contract tests against the new adapter.</p></li></ol><p>No existing code needs to change. The agentic loop, the tool system, the fallback chain, the cost tracker -- they all work with the new provider automatically because they depend on the trait, not on any specific adapter.</p><div class="language-rust"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#6C7086;font-style:italic;">// Step 1: The adapter</span></span>
<span class="line"><span style="color:#CBA6F7;">pub</span><span style="color:#CBA6F7;"> struct</span><span style="color:#F9E2AF;font-style:italic;"> MistralProvider</span><span style="color:#9399B2;"> {</span><span style="color:#6C7086;font-style:italic;"> /* ... */</span><span style="color:#9399B2;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="color:#F9E2AF;font-style:italic;">#</span><span style="color:#9399B2;">[</span><span style="color:#F9E2AF;font-style:italic;">async_trait</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CBA6F7;">impl</span><span style="color:#F9E2AF;font-style:italic;"> Provider</span><span style="color:#CBA6F7;"> for</span><span style="color:#F9E2AF;font-style:italic;"> MistralProvider</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#CBA6F7;">    fn</span><span style="color:#89B4FA;font-style:italic;"> name</span><span style="color:#9399B2;">(</span><span style="color:#94E2D5;">&amp;</span><span style="color:#F38BA8;">self</span><span style="color:#9399B2;">)</span><span style="color:#94E2D5;"> -&gt;</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#F9E2AF;font-style:italic;">str</span><span style="color:#9399B2;"> {</span><span style="color:#A6E3A1;"> &quot;mistral&quot;</span><span style="color:#9399B2;"> }</span></span>
<span class="line"><span style="color:#CBA6F7;">    fn</span><span style="color:#89B4FA;font-style:italic;"> model</span><span style="color:#9399B2;">(</span><span style="color:#94E2D5;">&amp;</span><span style="color:#F38BA8;">self</span><span style="color:#9399B2;">)</span><span style="color:#94E2D5;"> -&gt;</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#F9E2AF;font-style:italic;">str</span><span style="color:#9399B2;"> {</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#F38BA8;">self</span><span style="color:#94E2D5;">.</span><span style="color:#CDD6F4;">model </span><span style="color:#9399B2;">}</span></span>
<span class="line"></span>
<span class="line"><span style="color:#CBA6F7;">    async</span><span style="color:#CBA6F7;"> fn</span><span style="color:#89B4FA;font-style:italic;"> send_message</span><span style="color:#9399B2;">(</span></span>
<span class="line"><span style="color:#94E2D5;">        &amp;</span><span style="color:#F38BA8;">self</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#EBA0AC;">        system</span><span style="color:#94E2D5;">:</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#F9E2AF;font-style:italic;">str</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#EBA0AC;">        messages</span><span style="color:#94E2D5;">:</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#9399B2;">[</span><span style="color:#F9E2AF;font-style:italic;">Message</span><span style="color:#9399B2;">],</span></span>
<span class="line"><span style="color:#EBA0AC;">        tools</span><span style="color:#94E2D5;">:</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#9399B2;">[</span><span style="color:#F9E2AF;font-style:italic;">ToolDefinition</span><span style="color:#9399B2;">],</span></span>
<span class="line"><span style="color:#EBA0AC;">        max_tokens</span><span style="color:#94E2D5;">:</span><span style="color:#CBA6F7;"> u32</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">    )</span><span style="color:#94E2D5;"> -&gt;</span><span style="color:#F9E2AF;font-style:italic;"> Result</span><span style="color:#89DCEB;">&lt;</span><span style="color:#F9E2AF;font-style:italic;">ProviderResponse</span><span style="color:#9399B2;">,</span><span style="color:#F9E2AF;font-style:italic;"> ProviderError</span><span style="color:#89DCEB;">&gt;</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">        // Translate to Mistral&#39;s API format and send</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">        todo!</span><span style="color:#9399B2;">()</span></span>
<span class="line"><span style="color:#9399B2;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="color:#CBA6F7;">    async</span><span style="color:#CBA6F7;"> fn</span><span style="color:#89B4FA;font-style:italic;"> stream_message</span><span style="color:#9399B2;">(</span></span>
<span class="line"><span style="color:#94E2D5;">        &amp;</span><span style="color:#F38BA8;">self</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#EBA0AC;">        system</span><span style="color:#94E2D5;">:</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#F9E2AF;font-style:italic;">str</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#EBA0AC;">        messages</span><span style="color:#94E2D5;">:</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#9399B2;">[</span><span style="color:#F9E2AF;font-style:italic;">Message</span><span style="color:#9399B2;">],</span></span>
<span class="line"><span style="color:#EBA0AC;">        tools</span><span style="color:#94E2D5;">:</span><span style="color:#94E2D5;"> &amp;</span><span style="color:#9399B2;">[</span><span style="color:#F9E2AF;font-style:italic;">ToolDefinition</span><span style="color:#9399B2;">],</span></span>
<span class="line"><span style="color:#EBA0AC;">        max_tokens</span><span style="color:#94E2D5;">:</span><span style="color:#CBA6F7;"> u32</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">    )</span><span style="color:#94E2D5;"> -&gt;</span><span style="color:#F9E2AF;font-style:italic;"> Result</span><span style="color:#89DCEB;">&lt;</span><span style="color:#F9E2AF;font-style:italic;">StreamHandle</span><span style="color:#9399B2;">,</span><span style="color:#F9E2AF;font-style:italic;"> ProviderError</span><span style="color:#89DCEB;">&gt;</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">        todo!</span><span style="color:#9399B2;">()</span></span>
<span class="line"><span style="color:#9399B2;">    }</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>Since many providers now follow the OpenAI chat completions format, you can often reuse the OpenAI adapter with a different base URL. The <code>with_base_url</code> constructor handles providers like Together AI, Groq, Anyscale, and any vLLM deployment with no adapter code at all.</p><h2 id="what-comes-next" tabindex="-1">What Comes Next <a class="header-anchor" href="#what-comes-next" aria-label="Permalink to &quot;What Comes Next&quot;">​</a></h2><p>With provider abstraction in place, your agent is now truly flexible:</p><ul><li><strong>Chapter 14</strong> builds on this foundation with the extensibility and plugin system, where third-party plugins can register their own providers.</li><li><strong>Chapter 15</strong> covers production polish, where the provider system gets monitoring, logging, and graceful degradation under load.</li></ul><p>The multi-provider system is one of the most impactful features you can add to a coding agent. It transforms the agent from a single-vendor tool into a platform that adapts to each user&#39;s needs, budget, and constraints.</p><h2 id="exercises" tabindex="-1">Exercises <a class="header-anchor" href="#exercises" aria-label="Permalink to &quot;Exercises&quot;">​</a></h2><p>Practice each concept with these exercises. They build on the multi-provider system you created in this chapter.</p><h3 id="exercise-1-add-a-model-status-command-easy" tabindex="-1">Exercise 1: Add a /model Status Command (Easy) <a class="header-anchor" href="#exercise-1-add-a-model-status-command-easy" aria-label="Permalink to &quot;Exercise 1: Add a /model Status Command (Easy)&quot;">​</a></h3><p>Implement a <code>/model</code> REPL command that displays the current provider name, model ID, and a summary of the model&#39;s capabilities (supports tools, supports streaming, max context window). Pull the information from the <code>CapabilityRegistry</code> and the active provider.</p><ul><li>Call <code>provider.name()</code> and <code>provider.model()</code> for the basic info</li><li>Look up capabilities with <code>registry.get(provider.model())</code></li><li>Format as a compact display: <code>[Provider: anthropic | Model: claude-sonnet-4-20250514 | Tools: yes | Stream: yes | Context: 200k]</code></li></ul><h3 id="exercise-2-implement-a-request-response-logger-for-debugging-easy" tabindex="-1">Exercise 2: Implement a Request/Response Logger for Debugging (Easy) <a class="header-anchor" href="#exercise-2-implement-a-request-response-logger-for-debugging-easy" aria-label="Permalink to &quot;Exercise 2: Implement a Request/Response Logger for Debugging (Easy)&quot;">​</a></h3><p>Add a <code>ProviderLogger</code> wrapper that implements the <code>Provider</code> trait and logs every request and response to a file. Log the system prompt length, message count, tool count, response token usage, stop reason, and latency. This is invaluable for debugging provider differences.</p><ul><li>Wrap an <code>Arc&lt;dyn Provider&gt;</code> and delegate all trait methods to it</li><li>Before each <code>send_message</code>, log the request metadata (not the full content, to avoid leaking data)</li><li>After each response, log the usage, stop reason, and elapsed time</li><li>Write to a configurable log file path with one JSON object per line</li></ul><h3 id="exercise-3-add-response-normalization-tests-medium" tabindex="-1">Exercise 3: Add Response Normalization Tests (Medium) <a class="header-anchor" href="#exercise-3-add-response-normalization-tests-medium" aria-label="Permalink to &quot;Exercise 3: Add Response Normalization Tests (Medium)&quot;">​</a></h3><p>Write a test suite that verifies all three providers (Anthropic, OpenAI, Ollama) produce identical <code>ProviderResponse</code> structures for equivalent inputs. Create a set of recorded response fixtures (one per provider for the same prompt) and assert that after normalization, the <code>Message</code>, <code>ContentBlock</code>, and <code>TokenUsage</code> types are equivalent.</p><p><strong>Hints:</strong></p><ul><li>Create a <code>tests/fixtures/</code> directory with JSON files for each provider&#39;s raw response format</li><li>Write a helper that loads a fixture, passes it through the adapter&#39;s parsing logic, and returns a <code>ProviderResponse</code></li><li>Assert field-by-field equality: <code>response.content[0].text</code>, <code>response.usage.input_tokens</code>, <code>response.stop_reason</code></li><li>Test edge cases: empty responses, tool-use responses, and multi-block responses</li></ul><h3 id="exercise-4-implement-a-cost-tracking-dashboard-medium" tabindex="-1">Exercise 4: Implement a Cost Tracking Dashboard (Medium) <a class="header-anchor" href="#exercise-4-implement-a-cost-tracking-dashboard-medium" aria-label="Permalink to &quot;Exercise 4: Implement a Cost Tracking Dashboard (Medium)&quot;">​</a></h3><p>Extend the <code>CostTracker</code> to provide a per-provider and per-model cost breakdown. Add a <code>/costs</code> REPL command that displays a table showing each provider/model combination, the number of requests, total input tokens, total output tokens, and estimated cost in USD. Use configurable pricing rates per model.</p><p><strong>Hints:</strong></p><ul><li>Store cost records keyed by <code>(provider_name, model_id)</code> in a <code>HashMap</code></li><li>Define pricing as a <code>HashMap&lt;String, (f64, f64)&gt;</code> mapping model IDs to <code>(input_price_per_1k, output_price_per_1k)</code></li><li>Accumulate totals on each <code>record()</code> call</li><li>Format the table with aligned columns using <code>format!(&quot;{:&lt;20} {:&gt;8} {:&gt;10} {:&gt;10} {:&gt;10}&quot;, ...)</code></li></ul><h3 id="exercise-5-add-a-new-provider-adapter-hard" tabindex="-1">Exercise 5: Add a New Provider Adapter (Hard) <a class="header-anchor" href="#exercise-5-add-a-new-provider-adapter-hard" aria-label="Permalink to &quot;Exercise 5: Add a New Provider Adapter (Hard)&quot;">​</a></h3><p>Implement a complete adapter for a new provider (e.g., Mistral, Cohere, or Google Gemini). The adapter must implement the full <code>Provider</code> trait including <code>send_message</code> and <code>stream_message</code>, translate between your generic types and the provider&#39;s API format, map provider-specific errors to <code>ProviderError</code>, and register model capabilities. Write tests using recorded response fixtures.</p><p><strong>Hints:</strong></p><ul><li>Start by reading the provider&#39;s API documentation and mapping their request/response format to your <code>Message</code> and <code>ContentBlock</code> types</li><li>Most providers follow the OpenAI chat completions format, so you can start by copying <code>openai.rs</code> and modifying the differences</li><li>Pay special attention to how the provider handles tool calls -- some use the <code>function</code> wrapper, some use <code>tool_use</code> blocks, some use text-based tool calling</li><li>Register at least two models in the <code>CapabilityRegistry</code> with accurate context window sizes and feature flags</li><li>Write both unit tests (parsing fixtures) and an integration test (mock HTTP server) for the adapter</li></ul><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>The provider system has four layers: the trait (contract), adapters (translation), runtime features (intelligence), and configuration (setup) -- each with clear boundaries</li><li>The minimal trait design (four methods) keeps adapters simple and makes adding new providers a single-file change</li><li>Composition patterns (fallback wrapping providers, switcher containing a provider, cost tracker decorating calls) build complex behavior from simple pieces</li><li>Rust&#39;s trait-based polymorphism catches missing method implementations at compile time, unlike Python&#39;s duck typing which catches them at runtime</li><li>Adding a new provider requires only an adapter file, a capabilities entry, a config section, and tests -- no existing code changes</li></ul>`,57)])])}const m=a(n,[["render",r]]);export{y as __pageData,m as default};
