import{_ as e,o,c as a,a2 as s}from"./chunks/framework.B72_uj9U.js";const p=JSON.parse('{"title":"Agentic vs Chatbot","description":"Drawing a clear line between conversational chatbots and agentic systems that can perceive, plan, and act autonomously.","frontmatter":{"title":"Agentic vs Chatbot","description":"Drawing a clear line between conversational chatbots and agentic systems that can perceive, plan, and act autonomously."},"headers":[],"relativePath":"learn/linear/01-what-is-a-coding-agent/10-agentic-vs-chatbot.md","filePath":"learn/linear/01-what-is-a-coding-agent/10-agentic-vs-chatbot.md"}'),n={name:"learn/linear/01-what-is-a-coding-agent/10-agentic-vs-chatbot.md"};function i(r,t,l,h,c,d){return o(),a("div",null,[...t[0]||(t[0]=[s(`<h1 id="agentic-vs-chatbot" tabindex="-1">Agentic vs Chatbot <a class="header-anchor" href="#agentic-vs-chatbot" aria-label="Permalink to &quot;Agentic vs Chatbot&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>The fundamental architectural differences between a chatbot and an agentic system</li><li>Why the ability to use tools and maintain state across iterations defines agentic behavior</li><li>How to evaluate where a system falls on the chatbot-to-agent autonomy spectrum</li></ul></blockquote><h2 id="a-distinction-that-matters" tabindex="-1">A Distinction That Matters <a class="header-anchor" href="#a-distinction-that-matters" aria-label="Permalink to &quot;A Distinction That Matters&quot;">​</a></h2><p>At this point in the chapter, you&#39;ve seen four production coding agents analyzed in detail and the common architectural patterns extracted. But there&#39;s a question that still trips people up: &quot;Isn&#39;t a coding agent just a fancy chatbot?&quot;</p><p>The answer is no, and the distinction isn&#39;t pedantic — it&#39;s architectural. The difference between a chatbot and an agent determines how you design the system, what capabilities it has, and what kind of tasks it can handle. Getting this distinction right shapes every engineering decision from here forward.</p><h2 id="what-makes-a-chatbot" tabindex="-1">What Makes a Chatbot <a class="header-anchor" href="#what-makes-a-chatbot" aria-label="Permalink to &quot;What Makes a Chatbot&quot;">​</a></h2><p>A chatbot is a system that takes a text input, processes it, and returns a text output. The canonical chatbot architecture looks like this:</p><ol><li>User sends a message.</li><li>System constructs a prompt (system instructions + conversation history + user message).</li><li>System sends the prompt to an LLM.</li><li>LLM returns a text response.</li><li>System displays the response to the user.</li><li>Go to step 1.</li></ol><p>This is a perfectly good architecture for many use cases — answering questions, explaining concepts, generating code snippets, brainstorming solutions. ChatGPT, in its default mode, is a chatbot. So is a basic integration of any LLM into a messaging interface.</p><p>The key limitation is in step 4: the LLM returns a <strong>text response</strong>. It can describe what you should do, generate code you could paste into your editor, or explain an error message you showed it. But it can&#39;t <em>do</em> anything. It can&#39;t read your files, run your tests, or modify your code. It can only produce text.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>Think of it in Python terms. A chatbot is like a function that takes a string and returns a string:</p><div class="language-python"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">def</span><span style="color:#89B4FA;font-style:italic;"> chatbot</span><span style="color:#9399B2;">(</span><span style="color:#EBA0AC;font-style:italic;">user_message</span><span style="color:#9399B2;">:</span><span style="color:#FAB387;font-style:italic;"> str</span><span style="color:#9399B2;">,</span><span style="color:#EBA0AC;font-style:italic;"> history</span><span style="color:#9399B2;">:</span><span style="color:#EBA0AC;font-style:italic;"> list</span><span style="color:#9399B2;">[</span><span style="color:#FAB387;font-style:italic;">dict</span><span style="color:#9399B2;">])</span><span style="color:#9399B2;"> -&gt;</span><span style="color:#FAB387;font-style:italic;"> str</span><span style="color:#9399B2;">:</span></span>
<span class="line"><span style="color:#CDD6F4;">    response </span><span style="color:#94E2D5;">=</span><span style="color:#CDD6F4;"> llm</span><span style="color:#9399B2;">.</span><span style="color:#89B4FA;">complete</span><span style="color:#9399B2;">(</span><span style="color:#CDD6F4;">history </span><span style="color:#94E2D5;">+</span><span style="color:#9399B2;"> [{</span><span style="color:#A6E3A1;">&quot;role&quot;</span><span style="color:#9399B2;">:</span><span style="color:#A6E3A1;"> &quot;user&quot;</span><span style="color:#9399B2;">,</span><span style="color:#A6E3A1;"> &quot;content&quot;</span><span style="color:#9399B2;">:</span><span style="color:#CDD6F4;"> user_message</span><span style="color:#9399B2;">}])</span></span>
<span class="line"><span style="color:#CBA6F7;">    return</span><span style="color:#CDD6F4;"> response</span><span style="color:#9399B2;">.</span><span style="color:#CDD6F4;">text</span></span></code></pre></div><p>An agent is like a function that takes a string and returns after performing side effects on the real world — reading files, writing files, running commands. The return value (if any) is just a summary of what it did.</p></div><h2 id="what-makes-an-agent" tabindex="-1">What Makes an Agent <a class="header-anchor" href="#what-makes-an-agent" aria-label="Permalink to &quot;What Makes an Agent&quot;">​</a></h2><p>An agent extends the chatbot architecture with three additional capabilities: <strong>tool use</strong>, <strong>observation</strong>, and <strong>autonomous iteration</strong>.</p><ol><li>User sends a message.</li><li>System constructs a prompt (system instructions + conversation history + user message + <strong>tool definitions</strong>).</li><li>System sends the prompt to an LLM.</li><li>LLM returns a response that may contain text <strong>and/or tool calls</strong>.</li><li>If the response contains tool calls: a. Execute each tool call. b. Append the tool results to the conversation history. c. <strong>Go to step 3</strong> (not step 1 — the loop continues without user input).</li><li>If the response contains only text, display it to the user and go to step 1.</li></ol><p>The three additions are transformative:</p><p><strong>Tool use</strong> means the model can do things, not just say things. When it decides a file needs to be read, it doesn&#39;t tell you &quot;please read <code>src/main.rs</code>&quot; — it invokes the <code>read_file</code> tool with the path <code>src/main.rs</code>, and the tool returns the file contents directly to the model.</p><p><strong>Observation</strong> means the model can see the results of its actions. After it writes code and runs the tests, it sees the test output. After it modifies a file, it can read the file back to verify the change was applied correctly. The world state is visible to the model at every step.</p><p><strong>Autonomous iteration</strong> means the model can take multiple actions in sequence without waiting for human input. Step 5c is the critical difference — the loop goes back to the LLM with the tool results, and the LLM decides whether to take more actions or produce a final response. The human doesn&#39;t need to be in the loop for every step.</p><h2 id="the-human-in-the-loop-spectrum" tabindex="-1">The Human-in-the-Loop Spectrum <a class="header-anchor" href="#the-human-in-the-loop-spectrum" aria-label="Permalink to &quot;The Human-in-the-Loop Spectrum&quot;">​</a></h2><p>The chatbot-agent distinction isn&#39;t binary — it&#39;s a spectrum defined by how much the human participates in the loop.</p><p><strong>Pure chatbot (human in every loop).</strong> Every LLM response goes to the user. The user decides what to do next and manually performs any actions. The LLM has zero autonomy.</p><p><strong>Assisted chatbot (human reviews actions).</strong> The LLM can suggest actions (like code changes), but the human must review and apply them. The human is still in the critical path. Many IDE chat features work this way.</p><p><strong>Supervised agent (human approves some actions).</strong> The LLM can execute some actions autonomously (reading files, searching code) but needs human approval for risky operations (writing files, running commands). Claude Code in its default mode works this way.</p><p><strong>Autonomous agent (human reviews results).</strong> The LLM executes all actions autonomously. The human sees the final result and decides whether to accept it. Codex works this way within its sandbox.</p><p><strong>Fully autonomous agent (human not in the loop).</strong> The LLM completes entire tasks without human involvement. This is the theoretical end of the spectrum — used in CI/CD pipelines and automated refactoring systems where the agent commits directly.</p><p>The agent we build will operate primarily in the &quot;supervised agent&quot; mode — autonomous enough to iterate on a task without constant human input, but with safety checks for operations that could cause damage.</p><h2 id="the-architectural-consequences" tabindex="-1">The Architectural Consequences <a class="header-anchor" href="#the-architectural-consequences" aria-label="Permalink to &quot;The Architectural Consequences&quot;">​</a></h2><p>The chatbot-agent distinction has concrete implications for system architecture. Let&#39;s compare:</p><table tabindex="0"><thead><tr><th>Concern</th><th>Chatbot</th><th>Agent</th></tr></thead><tbody><tr><td>Message format</td><td>Text only</td><td>Text + tool calls + tool results</td></tr><tr><td>Loop structure</td><td>Single turn</td><td>Multi-turn with autonomous iterations</td></tr><tr><td>State management</td><td>Conversation history</td><td>Conversation history + environment state</td></tr><tr><td>Error handling</td><td>Return error message to user</td><td>Return error to model for recovery</td></tr><tr><td>Context growth</td><td>Linear (one message per turn)</td><td>Rapid (tool calls + results add tokens quickly)</td></tr><tr><td>Permission model</td><td>Not needed</td><td>Essential</td></tr><tr><td>Side effects</td><td>None</td><td>File writes, shell commands, etc.</td></tr></tbody></table><p>Each of these differences drives engineering decisions. The message format means you need a structured representation that can hold tool use blocks alongside text. The loop structure means you need a dispatcher that can route tool calls and collect results. The context growth means you need compaction or summarization strategies. The side effects mean you need a permission system.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>The architectural consequences of the chatbot-agent distinction are visible in how the tools handle errors. When ChatGPT generates code with a bug, it tells you &quot;here&#39;s the code&quot; and you discover the bug when you run it. When Claude Code generates code with a bug, it discovers the bug itself — because it runs the code, sees the error, and fixes it. The feedback loop between action and observation is what transforms error handling from a user problem into a system capability.</p></div><h2 id="the-tool-use-inflection-point" tabindex="-1">The &quot;Tool Use&quot; Inflection Point <a class="header-anchor" href="#the-tool-use-inflection-point" aria-label="Permalink to &quot;The &quot;Tool Use&quot; Inflection Point&quot;">​</a></h2><p>If there&#39;s a single moment where a chatbot becomes an agent, it&#39;s the introduction of tool use. Without tools, the system can only produce text. With tools, it can interact with the real world. Everything else — the iterative loop, the permission system, the context management — follows from that single capability.</p><p>This is why tool use is such a central topic in this tutorial. When we build our tool system in Chapters 5-8, we&#39;re building the mechanism that transforms our system from a chatbot wrapper around an LLM into an actual agent. The tools are the hands and eyes of the agent — without them, the brain (the LLM) is isolated from the world.</p><p>Tool use also changes the model&#39;s behavior in subtle ways. When a model has tools available, it reasons differently. Instead of generating a complete answer in one shot, it plans a sequence of observations and actions. It asks itself &quot;what do I need to know?&quot; (perception), &quot;what should I do?&quot; (reasoning), and &quot;how do I do it?&quot; (tool selection). The presence of tools activates a more deliberate, investigative mode of operation.</p><h2 id="common-misconceptions" tabindex="-1">Common Misconceptions <a class="header-anchor" href="#common-misconceptions" aria-label="Permalink to &quot;Common Misconceptions&quot;">​</a></h2><p>Let&#39;s clear up some misconceptions that arise from the chatbot-agent confusion:</p><p><strong>&quot;An agent is just a chatbot with extra prompting.&quot;</strong> No. The difference isn&#39;t in the prompt — it&#39;s in the architecture. A chatbot with an elaborate system prompt is still a chatbot if it can&#39;t use tools or iterate autonomously. The prompt might make the chatbot <em>seem</em> more capable, but it doesn&#39;t give it the ability to act on the world.</p><p><strong>&quot;Agents are always better than chatbots.&quot;</strong> Not at all. For many tasks — answering questions, explaining concepts, generating standalone code snippets — a chatbot is perfectly adequate and simpler to build. You don&#39;t need an agentic loop to explain how Python decorators work. Agents shine for tasks that require multi-step interaction with a codebase: debugging, refactoring, feature implementation, test writing.</p><p><strong>&quot;Agents replace developers.&quot;</strong> Current coding agents are powerful assistants, not replacements. They handle routine tasks effectively and can tackle moderately complex problems, but they still need human judgment for architectural decisions, requirement interpretation, and quality assessment. The agent handles the typing; you handle the thinking.</p><p><strong>&quot;More autonomy is always better.&quot;</strong> Higher autonomy means less human oversight, which means more risk of the agent going in the wrong direction. The optimal autonomy level depends on the task, the stakes, and the user&#39;s trust in the model. Sometimes the best agent is one that stops and asks.</p><h2 id="building-the-mental-model" tabindex="-1">Building the Mental Model <a class="header-anchor" href="#building-the-mental-model" aria-label="Permalink to &quot;Building the Mental Model&quot;">​</a></h2><p>Here&#39;s the mental model to carry forward: a coding agent is a chatbot that has been given hands (tools), eyes (observation of tool results), and a permission to keep working without being asked (the iterative loop). It&#39;s the same brain (the LLM), but embedded in a body that can interact with the development environment.</p><p>When you build your agent, you&#39;ll start with the chatbot architecture — a REPL that sends messages to an LLM and displays responses. Then you&#39;ll add tools, one by one. Then you&#39;ll add the iterative loop. And at that moment, you&#39;ll watch your simple chatbot transform into an agent that can read files, write code, run tests, and fix its own mistakes.</p><p>That transformation is the core learning experience of this tutorial.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>A chatbot processes text inputs and returns text outputs; an agent extends this with tool use, observation of results, and autonomous multi-step iteration — the distinction is architectural, not cosmetic.</li><li>The human-in-the-loop spectrum ranges from pure chatbot (human in every loop) through supervised agent (human approves risky operations) to fully autonomous agent (human not in the loop).</li><li>Tool use is the inflection point that transforms a chatbot into an agent — without tools, the model can only produce text; with tools, it can perceive and act on the real world.</li><li>The chatbot-agent distinction drives concrete engineering decisions in message format, loop structure, state management, error handling, context growth, and permission systems.</li><li>More autonomy isn&#39;t universally better — the optimal level depends on the task, the stakes, and the trust relationship between the user and the model.</li></ul>`,47)])])}const m=e(n,[["render",i]]);export{p as __pageData,m as default};
