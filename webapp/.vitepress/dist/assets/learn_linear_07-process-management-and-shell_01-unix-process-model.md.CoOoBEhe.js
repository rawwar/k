import{_ as s,o as t,c as o,a2 as n}from"./chunks/framework.B72_uj9U.js";const u=JSON.parse('{"title":"Unix Process Model","description":"The foundational concepts of Unix processes including fork, exec, wait, and the process lifecycle that underpins all command execution in a coding agent.","frontmatter":{"title":"Unix Process Model","description":"The foundational concepts of Unix processes including fork, exec, wait, and the process lifecycle that underpins all command execution in a coding agent."},"headers":[],"relativePath":"learn/linear/07-process-management-and-shell/01-unix-process-model.md","filePath":"learn/linear/07-process-management-and-shell/01-unix-process-model.md"}'),a={name:"learn/linear/07-process-management-and-shell/01-unix-process-model.md"};function r(l,e,i,c,p,d){return t(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="unix-process-model" tabindex="-1">Unix Process Model <a class="header-anchor" href="#unix-process-model" aria-label="Permalink to &quot;Unix Process Model&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How Unix processes are created via the fork/exec model and why this design persists</li><li>The relationship between parent and child processes, including process IDs, exit codes, and zombie processes</li><li>How file descriptor inheritance connects parent and child processes through pipes</li></ul></blockquote><p>Before you write a single line of Rust process-spawning code, you need a mental model of what the operating system actually does when you &quot;run a command.&quot; Every coding agent -- whether it is compiling code, running tests, or executing shell scripts on the user&#39;s behalf -- ultimately asks the OS kernel to create a new process. Understanding this mechanism will help you debug hanging pipes, orphaned processes, and mysterious exit codes that would otherwise feel like black magic.</p><h2 id="the-process-a-running-program" tabindex="-1">The Process: A Running Program <a class="header-anchor" href="#the-process-a-running-program" aria-label="Permalink to &quot;The Process: A Running Program&quot;">​</a></h2><p>A <strong>process</strong> is the OS&#39;s abstraction for a running program. Each process has:</p><ul><li>A <strong>process ID (PID)</strong> -- a unique integer assigned by the kernel.</li><li>A <strong>parent process ID (PPID)</strong> -- the PID of the process that created it.</li><li>An <strong>address space</strong> -- its own private view of memory (code, stack, heap).</li><li>A set of <strong>file descriptors</strong> -- numbered handles to open files, pipes, sockets, and devices.</li><li>An <strong>environment</strong> -- a collection of key-value string pairs (environment variables).</li><li>A <strong>working directory</strong> -- the directory that relative paths resolve against.</li></ul><p>When your coding agent process spawns <code>cargo test</code>, the kernel creates a new process with its own PID, its own copy of file descriptors, and its own memory space. The agent process becomes the <strong>parent</strong>, and <code>cargo test</code> becomes the <strong>child</strong>.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>In Python, you rarely think about processes at this level. When you call <code>subprocess.run([&quot;cargo&quot;, &quot;test&quot;])</code>, Python hides fork/exec behind a friendly API. Rust&#39;s <code>std::process::Command</code> does the same abstraction, but understanding what happens underneath helps you reason about pipe deadlocks, zombie processes, and signal delivery -- problems that surface more often when you are building a long-running agent rather than a one-shot script.</p></div><h2 id="fork-and-exec-the-two-step-dance" tabindex="-1">Fork and Exec: The Two-Step Dance <a class="header-anchor" href="#fork-and-exec-the-two-step-dance" aria-label="Permalink to &quot;Fork and Exec: The Two-Step Dance&quot;">​</a></h2><p>Unix creates new processes through a two-step mechanism that dates back to the 1970s but remains the foundation of every modern Unix-like system (Linux, macOS, BSDs).</p><h3 id="step-1-fork" tabindex="-1">Step 1: fork() <a class="header-anchor" href="#step-1-fork" aria-label="Permalink to &quot;Step 1: fork()&quot;">​</a></h3><p>The <code>fork()</code> system call creates an almost exact copy of the calling process. After fork returns, there are two processes running the same code:</p><ul><li>The <strong>parent</strong> receives the child&#39;s PID as the return value of fork.</li><li>The <strong>child</strong> receives 0 as the return value.</li></ul><p>Both processes share the same code, the same open file descriptors (by duplication), and the same environment variables. The child gets a copy of the parent&#39;s memory -- though modern kernels use <strong>copy-on-write</strong> to avoid physically duplicating pages until one process modifies them.</p><h3 id="step-2-exec" tabindex="-1">Step 2: exec() <a class="header-anchor" href="#step-2-exec" aria-label="Permalink to &quot;Step 2: exec()&quot;">​</a></h3><p>The child process then calls one of the <code>exec()</code> family of system calls (<code>execve</code>, <code>execvp</code>, etc.) to replace its own program image with a new one. After exec, the child is running entirely different code -- say, the <code>cargo</code> binary. Its PID stays the same, its file descriptors stay open (unless marked close-on-exec), but the code and data segments are replaced.</p><p>Here is the conceptual flow in pseudocode:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>parent process (PID 100)</span></span>
<span class="line"><span>  |</span></span>
<span class="line"><span>  |-- fork() --&gt; child process (PID 101)</span></span>
<span class="line"><span>  |                |</span></span>
<span class="line"><span>  |                |-- exec(&quot;cargo&quot;, [&quot;test&quot;])</span></span>
<span class="line"><span>  |                |   (child is now running cargo)</span></span>
<span class="line"><span>  |                |</span></span>
<span class="line"><span>  |-- wait()       |-- ... cargo does its work ...</span></span>
<span class="line"><span>  |                |</span></span>
<span class="line"><span>  |   &lt;-- exit(0) --  (cargo finishes)</span></span>
<span class="line"><span>  |</span></span>
<span class="line"><span>  |-- (parent resumes, reads exit code 0)</span></span></code></pre></div><p>This two-step design may seem redundant -- why not have a single &quot;create process from this program&quot; call? The answer is that the gap between fork and exec is where you configure the child. In that window, the child can:</p><ul><li>Close or redirect file descriptors (set up pipes for stdout capture).</li><li>Change its working directory.</li><li>Modify environment variables.</li><li>Set resource limits.</li><li>Drop privileges.</li></ul><p>Rust&#39;s <code>Command</code> builder configures all of these things, and under the hood it executes them in that fork-to-exec gap.</p><h2 id="process-groups-and-sessions" tabindex="-1">Process Groups and Sessions <a class="header-anchor" href="#process-groups-and-sessions" aria-label="Permalink to &quot;Process Groups and Sessions&quot;">​</a></h2><p>Processes are organized into <strong>process groups</strong>, and process groups are organized into <strong>sessions</strong>. This hierarchy matters for signal delivery.</p><p>A <strong>process group</strong> is a collection of related processes identified by a <strong>process group ID (PGID)</strong>, which is typically the PID of the group leader. When your agent spawns <code>sh -c &quot;cargo test &amp;&amp; cargo clippy&quot;</code>, the shell may create a process group containing the shell itself, <code>cargo test</code>, and <code>cargo clippy</code>. Sending a signal to the process group (using a negative PID) delivers it to every member.</p><p>A <strong>session</strong> is a collection of process groups associated with a controlling terminal. When the user presses Ctrl+C in a terminal, the kernel sends SIGINT to the entire foreground process group of that session.</p><p>For a coding agent, process groups are important because you often want to kill not just the child process you spawned, but all of its descendants. If you spawn <code>sh -c &quot;sleep 1000&quot;</code>, killing only the shell leaves <code>sleep</code> running as an orphan. Killing the entire process group cleans up everything.</p><h2 id="waiting-and-exit-codes" tabindex="-1">Waiting and Exit Codes <a class="header-anchor" href="#waiting-and-exit-codes" aria-label="Permalink to &quot;Waiting and Exit Codes&quot;">​</a></h2><p>After forking a child, the parent should eventually call <code>wait()</code> (or <code>waitpid()</code>) to collect the child&#39;s exit status. This is critical for two reasons:</p><ol><li><p><strong>Exit code retrieval</strong> -- the exit status tells you whether the command succeeded (conventionally, 0 means success, non-zero means failure). Your agent needs this to decide whether a tool invocation worked.</p></li><li><p><strong>Zombie prevention</strong> -- until the parent calls wait, a terminated child lingers in the process table as a <strong>zombie process</strong>. It consumes no CPU or memory, but it occupies a PID slot. A long-running agent that spawns thousands of commands without waiting will eventually exhaust the system&#39;s PID space.</p></li></ol><p>Exit codes are integers, but by convention:</p><table tabindex="0"><thead><tr><th>Exit Code</th><th>Meaning</th></tr></thead><tbody><tr><td>0</td><td>Success</td></tr><tr><td>1</td><td>General error</td></tr><tr><td>2</td><td>Misuse of command</td></tr><tr><td>126</td><td>Command found but not executable</td></tr><tr><td>127</td><td>Command not found</td></tr><tr><td>128 + N</td><td>Killed by signal N (e.g., 137 = killed by SIGKILL, signal 9)</td></tr></tbody></table><p>When your agent runs <code>cargo test</code> and gets exit code 101, it knows the tests failed. When it gets 137, it knows something sent SIGKILL -- perhaps your own timeout mechanism.</p><h2 id="file-descriptor-inheritance" tabindex="-1">File Descriptor Inheritance <a class="header-anchor" href="#file-descriptor-inheritance" aria-label="Permalink to &quot;File Descriptor Inheritance&quot;">​</a></h2><p>When a process forks, the child inherits copies of all the parent&#39;s open file descriptors. This is how pipes work:</p><ol><li>Before forking, the parent creates a pipe -- a pair of file descriptors, one for reading and one for writing.</li><li>After forking, the parent closes the write end and the child closes the read end (or vice versa).</li><li>Now the parent can read what the child writes to its stdout, because the child&#39;s stdout has been redirected to the write end of the pipe.</li></ol><p>This is exactly what happens when you tell <code>Command</code> to capture stdout. Rust creates the pipe, forks, wires up the child&#39;s file descriptors, and gives you the read end.</p><div class="language-rust"><button title="Copy Code" class="copy"></button><span class="lang">rust</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#CBA6F7;">use</span><span style="color:#F9E2AF;"> std</span><span style="color:#94E2D5;">::</span><span style="color:#F9E2AF;">process</span><span style="color:#94E2D5;">::</span><span style="color:#F9E2AF;font-style:italic;">Command</span><span style="color:#9399B2;">;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#CBA6F7;">fn</span><span style="color:#89B4FA;font-style:italic;"> main</span><span style="color:#9399B2;">()</span><span style="color:#9399B2;"> {</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">    // Rust handles all the fork/exec/pipe plumbing for you</span></span>
<span class="line"><span style="color:#CBA6F7;">    let</span><span style="color:#CDD6F4;"> output </span><span style="color:#94E2D5;">=</span><span style="color:#F9E2AF;font-style:italic;"> Command</span><span style="color:#94E2D5;">::</span><span style="color:#89B4FA;font-style:italic;">new</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;echo&quot;</span><span style="color:#9399B2;">)</span></span>
<span class="line"><span style="color:#94E2D5;">        .</span><span style="color:#89B4FA;font-style:italic;">arg</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;hello from the child process&quot;</span><span style="color:#9399B2;">)</span></span>
<span class="line"><span style="color:#94E2D5;">        .</span><span style="color:#89B4FA;font-style:italic;">output</span><span style="color:#9399B2;">()</span></span>
<span class="line"><span style="color:#94E2D5;">        .</span><span style="color:#89B4FA;font-style:italic;">expect</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;failed to spawn process&quot;</span><span style="color:#9399B2;">);</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;">    // The parent reads from the pipe that was connected to the child&#39;s stdout</span></span>
<span class="line"><span style="color:#CBA6F7;">    let</span><span style="color:#CDD6F4;"> stdout </span><span style="color:#94E2D5;">=</span><span style="color:#F9E2AF;font-style:italic;"> String</span><span style="color:#94E2D5;">::</span><span style="color:#89B4FA;font-style:italic;">from_utf8_lossy</span><span style="color:#9399B2;">(</span><span style="color:#94E2D5;">&amp;</span><span style="color:#CDD6F4;">output</span><span style="color:#94E2D5;">.</span><span style="color:#89B4FA;">stdout</span><span style="color:#9399B2;">);</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">    println!</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;Child said: </span><span style="color:#9399B2;">{}</span><span style="color:#A6E3A1;">&quot;</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> stdout</span><span style="color:#94E2D5;">.</span><span style="color:#89B4FA;font-style:italic;">trim</span><span style="color:#9399B2;">());</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">    println!</span><span style="color:#9399B2;">(</span><span style="color:#A6E3A1;">&quot;Exit code: </span><span style="color:#9399B2;">{}</span><span style="color:#A6E3A1;">&quot;</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> output</span><span style="color:#94E2D5;">.</span><span style="color:#CDD6F4;">status</span><span style="color:#94E2D5;">.</span><span style="color:#89B4FA;font-style:italic;">code</span><span style="color:#9399B2;">()</span><span style="color:#94E2D5;">.</span><span style="color:#89B4FA;font-style:italic;">unwrap_or</span><span style="color:#9399B2;">(</span><span style="color:#94E2D5;">-</span><span style="color:#FAB387;">1</span><span style="color:#9399B2;">));</span></span>
<span class="line"><span style="color:#9399B2;">}</span></span></code></pre></div><p>Even though Rust&#39;s API is high-level, every call to <code>.output()</code> triggers the full fork/exec/pipe/wait sequence under the hood. Understanding this helps you reason about what can go wrong -- a pipe that fills up and blocks, a child that never exits, a file descriptor that leaks to a grandchild.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Claude Code and Codex both spawn shell commands as child processes of the agent. Claude Code routes all commands through a sandboxed executor that creates dedicated process groups for each command, ensuring that timeouts can kill the entire tree of descendant processes rather than just the top-level shell. This prevents orphaned processes from accumulating during long agent sessions.</p></div><h2 id="why-this-matters-for-your-agent" tabindex="-1">Why This Matters for Your Agent <a class="header-anchor" href="#why-this-matters-for-your-agent" aria-label="Permalink to &quot;Why This Matters for Your Agent&quot;">​</a></h2><p>Every subsequent subchapter in this chapter builds on these concepts:</p><ul><li><strong>Spawning</strong> uses fork/exec (subchapter 2).</li><li><strong>Capturing output</strong> uses pipe inheritance (subchapter 3).</li><li><strong>Stdin/stdout/stderr</strong> are file descriptors 0, 1, and 2 (subchapter 4).</li><li><strong>Signals and timeouts</strong> are delivered to processes and process groups (subchapter 5).</li><li><strong>Environment and working directory</strong> are inherited from the parent across fork (subchapter 6).</li><li><strong>Sandboxing</strong> manipulates the fork-to-exec gap to restrict the child (subchapter 8).</li></ul><p>With this foundation, you are ready to see how Rust wraps these primitives into a safe, ergonomic API.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>Unix creates new processes via a two-step fork/exec model: fork copies the parent, exec replaces the copy with a new program.</li><li>The gap between fork and exec is where file descriptors, environment variables, working directory, and resource limits are configured for the child.</li><li>Parents must wait on children to collect exit codes and prevent zombie processes.</li><li>Process groups allow signals to be delivered to an entire tree of related processes -- essential for timeouts that cleanly kill a command and all its descendants.</li><li>File descriptor inheritance across fork is the mechanism that enables pipes, stdout capture, and stdin feeding in child processes.</li></ul>`,45)])])}const g=s(a,[["render",r]]);export{u as __pageData,g as default};
