import{_ as n,o as a,c as s,a2 as t}from"./chunks/framework.B72_uj9U.js";const h=JSON.parse('{"title":"Prompt Engineering for Agents","description":"Specialized prompt engineering techniques that improve tool use accuracy, multi-step reasoning, and reliable code generation in agents.","frontmatter":{"title":"Prompt Engineering for Agents","description":"Specialized prompt engineering techniques that improve tool use accuracy, multi-step reasoning, and reliable code generation in agents."},"headers":[],"relativePath":"learn/linear/03-understanding-llms/14-prompt-engineering-for-agents.md","filePath":"learn/linear/03-understanding-llms/14-prompt-engineering-for-agents.md"}'),o={name:"learn/linear/03-understanding-llms/14-prompt-engineering-for-agents.md"};function i(p,e,r,l,c,d){return a(),s("div",null,[...e[0]||(e[0]=[t(`<h1 id="prompt-engineering-for-agents" tabindex="-1">Prompt Engineering for Agents <a class="header-anchor" href="#prompt-engineering-for-agents" aria-label="Permalink to &quot;Prompt Engineering for Agents&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How agent system prompts differ from chatbot prompts in structure, specificity, and behavioral guidance</li><li>Techniques for improving tool selection accuracy including explicit tool descriptions and usage examples</li><li>Patterns for encouraging step-by-step reasoning and reducing common failure modes like premature completion</li></ul></blockquote><p>Prompt engineering for agents is fundamentally different from prompt engineering for chatbots. With a chatbot, you are optimizing for a single good response. With an agent, you are optimizing for a sequence of good decisions -- which tool to call, what arguments to pass, how to interpret results, when to retry, and when to declare the task complete. A slight miscalibration in the prompt can cause the agent to skip verification steps, choose the wrong tool, or give up too early. The techniques in this subchapter are specific to agentic use cases.</p><h2 id="the-agent-prompt-structure" tabindex="-1">The Agent Prompt Structure <a class="header-anchor" href="#the-agent-prompt-structure" aria-label="Permalink to &quot;The Agent Prompt Structure&quot;">​</a></h2><p>A production agent system prompt is organized into distinct sections, each serving a specific purpose. Here is the architecture:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>[Identity] - Who the agent is and what it can do</span></span>
<span class="line"><span>[Tool Usage Rules] - When and how to use each tool</span></span>
<span class="line"><span>[Workflow Patterns] - Multi-step procedures for common tasks</span></span>
<span class="line"><span>[Output Guidelines] - How to format responses and communicate</span></span>
<span class="line"><span>[Safety Constraints] - What operations to avoid or confirm</span></span>
<span class="line"><span>[Environment Context] - Dynamic information about the current session</span></span></code></pre></div><p>Let&#39;s look at each section with concrete examples.</p><h3 id="identity" tabindex="-1">Identity <a class="header-anchor" href="#identity" aria-label="Permalink to &quot;Identity&quot;">​</a></h3><p>The identity section establishes the agent&#39;s role and capabilities in specific terms:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>You are a coding assistant that helps developers with software projects.</span></span>
<span class="line"><span>You can read and write files, execute shell commands, and search codebases.</span></span>
<span class="line"><span>You operate on the user&#39;s local filesystem and can run build tools, tests,</span></span>
<span class="line"><span>and version control commands.</span></span></code></pre></div><p>Specific is better than generic. &quot;You are a coding assistant that can read files, write files, and run commands&quot; works better than &quot;You are a helpful AI.&quot; The specificity primes the model to think in terms of file operations and shell commands rather than generic conversation.</p><h3 id="tool-usage-rules" tabindex="-1">Tool Usage Rules <a class="header-anchor" href="#tool-usage-rules" aria-label="Permalink to &quot;Tool Usage Rules&quot;">​</a></h3><p>This is the highest-impact section. Each rule addresses a specific failure mode:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>## Tool Usage Rules</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- ALWAYS read a file before modifying it. Never assume you know the contents of a file.</span></span>
<span class="line"><span>- When editing a file, write the COMPLETE new file contents. Do not use partial edits</span></span>
<span class="line"><span>  or placeholders like &quot;... rest of file unchanged ...&quot;.</span></span>
<span class="line"><span>- After modifying code, ALWAYS run the appropriate build/check command (e.g., cargo check)</span></span>
<span class="line"><span>  to verify the change compiles.</span></span>
<span class="line"><span>- If a build fails, read the error message carefully, fix the issue, and try again.</span></span>
<span class="line"><span>  Do not report failure to the user until you have attempted at least 2 fixes.</span></span>
<span class="line"><span>- Use grep/ripgrep to find relevant code rather than reading files one by one.</span></span>
<span class="line"><span>- Prefer targeted file reads (specific files) over broad directory listings.</span></span></code></pre></div><p>Each rule follows the pattern: <strong>action + reason</strong>. &quot;ALWAYS read a file before modifying it&quot; (action) because &quot;never assume you know the contents&quot; (reason). The reason helps the model understand the intent behind the rule, making it more likely to apply the rule in novel situations.</p><h3 id="workflow-patterns" tabindex="-1">Workflow Patterns <a class="header-anchor" href="#workflow-patterns" aria-label="Permalink to &quot;Workflow Patterns&quot;">​</a></h3><p>Workflow patterns provide templates for multi-step operations:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>## Workflow Patterns</span></span>
<span class="line"><span></span></span>
<span class="line"><span>When fixing a bug:</span></span>
<span class="line"><span>1. Read the relevant source files to understand the code</span></span>
<span class="line"><span>2. Identify the root cause of the bug</span></span>
<span class="line"><span>3. Make the minimal change to fix the issue</span></span>
<span class="line"><span>4. Run the build to verify the fix compiles</span></span>
<span class="line"><span>5. Run tests to verify the fix works</span></span>
<span class="line"><span>6. Summarize what you changed and why</span></span>
<span class="line"><span></span></span>
<span class="line"><span>When adding a new feature:</span></span>
<span class="line"><span>1. Understand the existing code structure</span></span>
<span class="line"><span>2. Plan the changes needed</span></span>
<span class="line"><span>3. Implement the changes incrementally</span></span>
<span class="line"><span>4. Run the build after each significant change</span></span>
<span class="line"><span>5. Write or update tests</span></span>
<span class="line"><span>6. Run the full test suite</span></span>
<span class="line"><span>7. Summarize what was added</span></span></code></pre></div><p>These workflows reduce the model&#39;s decision space at each step. Instead of choosing from all possible actions, it follows a predefined sequence that encodes your engineering judgment about the best approach.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>Python agent frameworks like LangChain use &quot;chains&quot; and &quot;agents&quot; with predefined step sequences. In your Rust agent, the system prompt serves a similar function -- the workflow patterns in the prompt are like soft-coded chains that guide the model without rigidly constraining it. The model can deviate from the pattern when the situation demands it, but the pattern establishes a strong default behavior.</p></div><h2 id="technique-1-explicit-tool-selection-guidance" tabindex="-1">Technique 1: Explicit Tool Selection Guidance <a class="header-anchor" href="#technique-1-explicit-tool-selection-guidance" aria-label="Permalink to &quot;Technique 1: Explicit Tool Selection Guidance&quot;">​</a></h2><p>The model chooses tools based on the task and the tool descriptions. You can improve accuracy by providing explicit guidance about when to use each tool:</p><p><strong>Weak (model must infer from description):</strong></p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>read_file: Reads a file</span></span>
<span class="line"><span>shell: Runs a command</span></span></code></pre></div><p><strong>Strong (model gets explicit selection criteria):</strong></p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>read_file: Read the contents of a single file. Use this when you need to examine</span></span>
<span class="line"><span>source code, configuration, or any text file. Prefer this over using shell commands</span></span>
<span class="line"><span>like &#39;cat&#39; because it handles encoding correctly and provides better error messages.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>shell: Execute any shell command. Use this for:</span></span>
<span class="line"><span>- Build commands: cargo check, cargo build, cargo test</span></span>
<span class="line"><span>- Search: grep -r, ripgrep (rg), find</span></span>
<span class="line"><span>- Version control: git status, git diff, git log</span></span>
<span class="line"><span>- File operations: ls, mkdir, cp (but prefer read_file/write_file for content)</span></span>
<span class="line"><span>Do NOT use for: reading file contents (use read_file instead), writing files</span></span>
<span class="line"><span>(use write_file instead).</span></span></code></pre></div><p>The &quot;Do NOT use for&quot; guidance is particularly effective. It creates negative constraints that prevent the model from using a tool in suboptimal ways.</p><h2 id="technique-2-think-before-acting-instructions" tabindex="-1">Technique 2: Think-Before-Acting Instructions <a class="header-anchor" href="#technique-2-think-before-acting-instructions" aria-label="Permalink to &quot;Technique 2: Think-Before-Acting Instructions&quot;">​</a></h2><p>Encouraging the model to reason before acting improves the quality of tool calls:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>Before making any changes, briefly state:</span></span>
<span class="line"><span>1. What you understand about the problem</span></span>
<span class="line"><span>2. What approach you plan to take</span></span>
<span class="line"><span>3. Which files you expect to modify</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Then proceed with the implementation.</span></span></code></pre></div><p>This technique works because it forces the model to generate reasoning tokens before generating action tokens. The reasoning tokens condition the subsequent generation, leading to better decisions. It is not magic -- it is exploiting the autoregressive nature of the model.</p><p>However, be careful not to make the model too verbose. &quot;Briefly state&quot; is key -- without it, the model might generate paragraphs of analysis before every minor action.</p><h2 id="technique-3-error-recovery-patterns" tabindex="-1">Technique 3: Error Recovery Patterns <a class="header-anchor" href="#technique-3-error-recovery-patterns" aria-label="Permalink to &quot;Technique 3: Error Recovery Patterns&quot;">​</a></h2><p>Models tend to give up after a single failure. Explicit error recovery instructions prevent premature abandonment:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>When you encounter an error:</span></span>
<span class="line"><span>1. Read the error message carefully and completely</span></span>
<span class="line"><span>2. Identify the root cause (not just the symptom)</span></span>
<span class="line"><span>3. Fix the specific issue</span></span>
<span class="line"><span>4. Verify the fix</span></span>
<span class="line"><span>5. If the same error persists after 2 attempts, try a different approach</span></span>
<span class="line"><span>6. Only report failure to the user if you have exhausted your approaches</span></span></code></pre></div><p>Without this guidance, the model often responds with &quot;I encountered an error: [error message]. Would you like me to try a different approach?&quot; This puts the decision back on the user and breaks the autonomous behavior that makes agents valuable.</p><h2 id="technique-4-output-formatting-control" tabindex="-1">Technique 4: Output Formatting Control <a class="header-anchor" href="#technique-4-output-formatting-control" aria-label="Permalink to &quot;Technique 4: Output Formatting Control&quot;">​</a></h2><p>Control how the model presents its work to the user:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>## Communication</span></span>
<span class="line"><span></span></span>
<span class="line"><span>- When you make changes, show a brief summary of what changed and why.</span></span>
<span class="line"><span>  Do not repeat the entire file content.</span></span>
<span class="line"><span>- For multi-step operations, provide a brief status after each step.</span></span>
<span class="line"><span>  Example: &quot;Read src/main.rs (245 lines). Found the issue on line 87.&quot;</span></span>
<span class="line"><span>- If you need to make multiple changes, list them all at the end rather</span></span>
<span class="line"><span>  than explaining each one in detail during execution.</span></span>
<span class="line"><span>- Use code blocks with language tags when showing code snippets.</span></span></code></pre></div><p>Without formatting guidance, the model tends toward excessive verbosity -- explaining every decision, showing full file contents after every edit, and providing long justifications for simple changes. This is especially problematic for agents that make 10-20 tool calls per task.</p><h2 id="technique-5-preventing-common-failure-modes" tabindex="-1">Technique 5: Preventing Common Failure Modes <a class="header-anchor" href="#technique-5-preventing-common-failure-modes" aria-label="Permalink to &quot;Technique 5: Preventing Common Failure Modes&quot;">​</a></h2><p>Here are specific prompt patterns that address the most common agent failure modes:</p><p><strong>Premature completion</strong> -- the model declares &quot;done&quot; before verifying:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>Never say a task is complete until you have verified it. For code changes,</span></span>
<span class="line"><span>this means running the build. For bug fixes, this means running the test.</span></span>
<span class="line"><span>Do not assume your changes are correct -- verify them.</span></span></code></pre></div><p><strong>Hallucinated file contents</strong> -- the model guesses instead of reading:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>NEVER assume the contents of a file. Always read it first, even if you</span></span>
<span class="line"><span>think you know what it contains. File contents may have changed since</span></span>
<span class="line"><span>you last read them.</span></span></code></pre></div><p><strong>Overly broad changes</strong> -- the model rewrites more than necessary:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>Make the minimum change necessary to accomplish the task. Do not refactor,</span></span>
<span class="line"><span>reorganize, or &quot;improve&quot; code beyond what was requested unless the user</span></span>
<span class="line"><span>specifically asks for it.</span></span></code></pre></div><p><strong>Missing error handling</strong> -- the model ignores errors in tool results:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>Always check the exit code of shell commands. A non-zero exit code indicates</span></span>
<span class="line"><span>failure. Read stderr output when a command fails -- it usually contains</span></span>
<span class="line"><span>the most useful diagnostic information.</span></span></code></pre></div><h2 id="technique-6-few-shot-examples-in-system-prompts" tabindex="-1">Technique 6: Few-Shot Examples in System Prompts <a class="header-anchor" href="#technique-6-few-shot-examples-in-system-prompts" aria-label="Permalink to &quot;Technique 6: Few-Shot Examples in System Prompts&quot;">​</a></h2><p>For complex tool use patterns, including a concrete example in the system prompt dramatically improves accuracy:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>## Example Interaction</span></span>
<span class="line"><span></span></span>
<span class="line"><span>User: &quot;Fix the compilation error in src/main.rs&quot;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Good approach:</span></span>
<span class="line"><span>1. shell(&quot;cargo check 2&gt;&amp;1&quot;) -&gt; reads the error message</span></span>
<span class="line"><span>2. read_file(&quot;src/main.rs&quot;) -&gt; examines the source</span></span>
<span class="line"><span>3. write_file(&quot;src/main.rs&quot;, &lt;fixed content&gt;) -&gt; applies the fix</span></span>
<span class="line"><span>4. shell(&quot;cargo check&quot;) -&gt; verifies the fix</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Bad approach:</span></span>
<span class="line"><span>1. write_file(&quot;src/main.rs&quot;, &lt;guessed fix&gt;) -&gt; modifies without reading first</span></span></code></pre></div><p>The &quot;Good approach / Bad approach&quot; format is especially effective because it explicitly shows the model what not to do. This contrastive example is worth more than several paragraphs of instruction.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Claude Code&#39;s system prompt includes detailed tool usage guidelines, workflow patterns, and explicit instructions about error handling and verification. It is the product of iterative refinement -- each rule in the prompt corresponds to a specific failure mode observed during testing. The system prompt is one of the most carefully maintained components of the entire agent, and changes to it go through rigorous testing across diverse coding tasks.</p></div><h2 id="iterative-prompt-development" tabindex="-1">Iterative Prompt Development <a class="header-anchor" href="#iterative-prompt-development" aria-label="Permalink to &quot;Iterative Prompt Development&quot;">​</a></h2><p>Prompt engineering is empirical. Here is the process that works:</p><ol><li><strong>Start with a minimal prompt</strong> -- identity, basic tool descriptions, one or two rules</li><li><strong>Run a diverse set of tasks</strong> -- file editing, debugging, test writing, multi-file refactoring</li><li><strong>Document failures</strong> -- what went wrong, what the model should have done instead</li><li><strong>Add targeted rules</strong> -- one rule per failure mode, as specific as possible</li><li><strong>Test regressions</strong> -- verify new rules do not break previously working tasks</li><li><strong>Prune dead rules</strong> -- if a rule does not measurably affect behavior, remove it</li></ol><p>Keep a log of your prompt changes and the failures that motivated them. This log becomes invaluable documentation for understanding why each rule exists and whether it can be removed when model behavior improves with newer versions.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>Agent prompt engineering optimizes for sequences of good decisions (tool selection, argument quality, error recovery) rather than single good responses</li><li>The highest-impact prompt sections are tool usage rules and workflow patterns -- each rule should address a specific, observed failure mode with both action and reason</li><li>Explicit &quot;Do NOT use for&quot; guidance in tool descriptions and &quot;Good approach / Bad approach&quot; examples in system prompts are among the most effective techniques</li><li>Prevent common failures (premature completion, hallucinated file contents, overly broad changes) with targeted instructions that make the undesired behavior explicitly forbidden</li><li>Prompt development is iterative: start minimal, observe failures, add targeted rules, test regressions, and prune what does not help -- maintain a log of changes and their motivations</li></ul>`,61)])])}const g=n(o,[["render",i]]);export{h as __pageData,g as default};
