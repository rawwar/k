import{_ as t,o,c as n,a2 as a}from"./chunks/framework.B72_uj9U.js";const g=JSON.parse('{"title":"What Are LLMs","description":"A practical introduction to large language models for agent builders, covering architecture intuition without requiring ML expertise.","frontmatter":{"title":"What Are LLMs","description":"A practical introduction to large language models for agent builders, covering architecture intuition without requiring ML expertise."},"headers":[],"relativePath":"learn/linear/03-understanding-llms/01-what-are-llms.md","filePath":"learn/linear/03-understanding-llms/01-what-are-llms.md"}'),i={name:"learn/linear/03-understanding-llms/01-what-are-llms.md"};function s(r,e,l,h,d,c){return o(),n("div",null,[...e[0]||(e[0]=[a('<h1 id="what-are-llms" tabindex="-1">What Are LLMs <a class="header-anchor" href="#what-are-llms" aria-label="Permalink to &quot;What Are LLMs&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How transformer-based language models generate text through next-token prediction</li><li>Why LLMs can follow instructions, write code, and use tools despite being trained on prediction</li><li>The practical mental model for reasoning about LLM capabilities and limitations as an agent builder</li></ul></blockquote><p>You are about to build a coding agent that runs on top of a large language model. Before you write your first API call, you need an accurate mental model of what is actually happening inside that model. Not the linear algebra -- the practical behavior. How does it generate text? Why does it sometimes hallucinate? Why can it write working Rust code one moment and confidently produce a function that does not exist the next? Understanding the machine you are building on top of is what separates an agent that works reliably from one that fails in mysterious ways.</p><h2 id="the-core-idea-next-token-prediction" tabindex="-1">The Core Idea: Next-Token Prediction <a class="header-anchor" href="#the-core-idea-next-token-prediction" aria-label="Permalink to &quot;The Core Idea: Next-Token Prediction&quot;">​</a></h2><p>At its heart, every large language model does one thing: given a sequence of tokens (roughly, pieces of words), it predicts the most likely next token. That is the entire mechanism. There is no &quot;understanding&quot; module, no &quot;reasoning&quot; engine, no database of facts. There is a neural network that has learned statistical patterns across an enormous corpus of text, and it uses those patterns to predict what comes next.</p><p>Here is the intuition. Suppose you see the text:</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span>The function returns a Result&lt;T,</span></span></code></pre></div><p>A well-trained model assigns high probability to <code>E&gt;</code> as the next token, because it has seen this Rust pattern millions of times during training. It has learned that <code>Result&lt;T,</code> is almost always followed by <code>E&gt;</code> or a concrete error type. The model does not &quot;know&quot; what a Result type is in the way you do. It has learned the statistical structure of code so thoroughly that its predictions are functionally equivalent to understanding.</p><p>This next-token prediction happens autoregressively -- one token at a time, feeding each generated token back in as input for predicting the next one. When you ask a model to write a function, it generates the function signature token by token, then the body, then the closing brace. Each token is conditioned on everything that came before it: your prompt, the system message, and all previously generated tokens.</p><h2 id="from-prediction-to-instruction-following" tabindex="-1">From Prediction to Instruction Following <a class="header-anchor" href="#from-prediction-to-instruction-following" aria-label="Permalink to &quot;From Prediction to Instruction Following&quot;">​</a></h2><p>Raw next-token prediction on internet text would give you a model that continues text plausibly but does not follow instructions. If you typed &quot;Write a Rust function that sorts a vector,&quot; a base model might continue with &quot;...is a common interview question&quot; because that is a likely continuation of that sentence on the internet.</p><p>The key step that makes models useful for agents is <strong>instruction tuning</strong> and <strong>reinforcement learning from human feedback (RLHF)</strong>. During these training phases, the model learns to treat the input as an instruction and generate a helpful response rather than just a plausible continuation. The model learns the pattern: &quot;when a human asks me to do something, I should do it and present the result clearly.&quot;</p><p>This is why the message format matters so much (we will cover this in detail in <a href="/linear/03-understanding-llms/06-message-formats.html">Message Formats</a>). When you send a message with <code>role: &quot;user&quot;</code>, you are activating the instruction-following behavior the model learned during fine-tuning. The model has been trained on millions of examples of <code>user says X, assistant responds with Y</code>, and it has internalized that pattern deeply.</p><h2 id="why-llms-can-write-code" tabindex="-1">Why LLMs Can Write Code <a class="header-anchor" href="#why-llms-can-write-code" aria-label="Permalink to &quot;Why LLMs Can Write Code&quot;">​</a></h2><p>Code generation is not a special feature bolted onto language models -- it emerges naturally from training on code. Modern LLMs are trained on datasets that include billions of lines of source code from GitHub, Stack Overflow, documentation sites, and technical books. The model learns:</p><ul><li><strong>Syntax rules</strong> -- it has seen so many examples of valid Rust/Python/JavaScript that it rarely produces syntax errors</li><li><strong>Idioms and patterns</strong> -- it knows that Rust error handling uses <code>?</code> and <code>match</code>, that Python uses <code>try/except</code></li><li><strong>API usage</strong> -- it has seen thousands of examples of how popular libraries are used</li><li><strong>Logical structure</strong> -- it has learned that if a function opens a file, it typically does something with the contents and handles the error case</li></ul><p>This is also why models sometimes hallucinate API calls that do not exist. The model has learned the pattern of &quot;import library, call function&quot; so well that it can generate plausible-looking but fictitious function names. It is pattern matching, not looking things up in documentation.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>If you have used GitHub Copilot or asked ChatGPT to write Python code, you have already experienced LLM code generation. The same mechanism that completes your Python list comprehensions is what will power your Rust coding agent. The difference is that your agent will orchestrate multiple rounds of generation, inspect the results, and iterate -- turning a single-shot code generator into a persistent collaborator.</p></div><h2 id="the-transformer-architecture-just-enough" tabindex="-1">The Transformer Architecture (Just Enough) <a class="header-anchor" href="#the-transformer-architecture-just-enough" aria-label="Permalink to &quot;The Transformer Architecture (Just Enough)&quot;">​</a></h2><p>You do not need to understand backpropagation or attention head mathematics to build a coding agent. But knowing the basic shape of the architecture helps you reason about behavior.</p><p>A transformer processes input through these conceptual stages:</p><ol><li><strong>Tokenization</strong> -- your text is split into tokens (covered in <a href="/linear/03-understanding-llms/02-tokens-and-tokenization.html">Tokens and Tokenization</a>)</li><li><strong>Embedding</strong> -- each token is converted to a high-dimensional vector</li><li><strong>Attention layers</strong> -- the model processes all tokens in parallel, allowing each token to &quot;attend to&quot; every other token. This is how the model knows that the <code>it</code> in &quot;the function failed because it received null&quot; refers to &quot;the function&quot;</li><li><strong>Feed-forward layers</strong> -- dense neural network layers that transform the attended representations</li><li><strong>Output projection</strong> -- the final layer produces a probability distribution over all possible next tokens</li></ol><p>The attention mechanism is what makes transformers powerful for code. When generating token 500, the model can directly attend to token 3 -- it does not need to pass information through a chain of intermediate states like older architectures (RNNs) did. This means it can maintain coherence across long sequences, which is critical for generating functions that correctly reference variables defined hundreds of tokens earlier.</p><h2 id="what-this-means-for-agent-design" tabindex="-1">What This Means for Agent Design <a class="header-anchor" href="#what-this-means-for-agent-design" aria-label="Permalink to &quot;What This Means for Agent Design&quot;">​</a></h2><p>Understanding the prediction mechanism gives you practical insights for building agents:</p><p><strong>The model has no persistent memory.</strong> Every API call starts fresh. The model does not remember your previous conversation unless you send the entire conversation history in every request. This is why context window management (Chapter 3.3) is an essential agent design concern.</p><p><strong>The model does not execute code.</strong> When it writes a function, it is predicting what a correct function looks like based on patterns. It cannot run the code to verify it works. This is precisely why coding agents need a tool for executing shell commands -- the agent writes code, runs it, reads the output, and iterates. The execution feedback loop is what makes agents dramatically more capable than single-shot code generation.</p><p><strong>The model is probabilistic.</strong> The same prompt can produce different outputs on different runs (controlled by temperature, covered in <a href="/linear/03-understanding-llms/04-temperature-and-sampling.html">Temperature and Sampling</a>). Your agent needs to handle this variability gracefully.</p><p><strong>The model responds to framing.</strong> How you describe tools, what examples you provide in the system prompt, and how you format previous tool results all significantly affect the model&#39;s behavior. This is not a deterministic function call -- it is a system that responds to context and phrasing.</p><div class="custom-block wild"><p class="custom-block-title">In the Wild</p><p>Claude Code and other production coding agents treat the LLM as one component in a larger system. The agent provides the model with tools (file reading, shell execution, code search), feeds back real execution results, and maintains conversation history across turns. The model&#39;s job is to decide what to do next and generate tool calls or text -- the agent handles everything else. This separation of concerns -- LLM for decision-making, agent for execution -- is the foundational architecture you will build throughout this course.</p></div><h2 id="the-practical-mental-model" tabindex="-1">The Practical Mental Model <a class="header-anchor" href="#the-practical-mental-model" aria-label="Permalink to &quot;The Practical Mental Model&quot;">​</a></h2><p>Here is the mental model that will serve you throughout this course:</p><p>An LLM is a <strong>stateless text-processing function</strong>. You send it a sequence of messages (conversation history, system prompt, tool definitions), and it returns the most likely continuation. That continuation might be natural language, code, a tool call request, or a mix of all three. The model chooses what to generate based on the patterns it learned during training and the specific context you provide in your request.</p><p>Your job as an agent builder is to:</p><ol><li><strong>Provide good context</strong> -- clear system prompts, relevant conversation history, well-described tools</li><li><strong>Parse the output</strong> -- detect whether the model wants to call a tool, write text, or both</li><li><strong>Execute and feed back</strong> -- run the requested tool, capture the result, and send it back</li><li><strong>Loop until done</strong> -- repeat until the model indicates the task is complete</li></ol><p>This is the agentic loop, and every concept in this chapter -- tokens, context windows, tool use, streaming -- is something you need to understand to build it well.</p><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>LLMs generate text through next-token prediction, producing one token at a time conditioned on all previous tokens</li><li>Instruction tuning and RLHF transform raw text predictors into systems that follow user instructions, which is what enables the message-based API format agents rely on</li><li>Code generation works because models are trained on massive code corpora and learn syntax, idioms, and API patterns -- but they can also hallucinate nonexistent APIs for the same reason</li><li>The model is stateless: it has no memory between API calls, which means your agent must manage conversation history explicitly</li><li>Your mental model should be: the LLM is a stateless text-processing function, and the agent is the stateful system built around it</li></ul>',38)])])}const p=t(i,[["render",s]]);export{g as __pageData,p as default};
