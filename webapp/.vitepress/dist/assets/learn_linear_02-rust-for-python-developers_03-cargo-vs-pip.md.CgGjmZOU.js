import{_ as a,o as e,c as o,a2 as n}from"./chunks/framework.B72_uj9U.js";const u=JSON.parse(`{"title":"Cargo vs Pip","description":"Understanding Cargo as Rust's build system and package manager, mapped to familiar Python concepts like pip, venv, and pyproject.toml.","frontmatter":{"title":"Cargo vs Pip","description":"Understanding Cargo as Rust's build system and package manager, mapped to familiar Python concepts like pip, venv, and pyproject.toml."},"headers":[],"relativePath":"learn/linear/02-rust-for-python-developers/03-cargo-vs-pip.md","filePath":"learn/linear/02-rust-for-python-developers/03-cargo-vs-pip.md"}`),t={name:"learn/linear/02-rust-for-python-developers/03-cargo-vs-pip.md"};function l(p,s,c,r,i,d){return e(),o("div",null,[...s[0]||(s[0]=[n(`<h1 id="cargo-vs-pip" tabindex="-1">Cargo vs Pip <a class="header-anchor" href="#cargo-vs-pip" aria-label="Permalink to &quot;Cargo vs Pip&quot;">​</a></h1><blockquote><p><strong>What you&#39;ll learn:</strong></p><ul><li>How Cargo combines the roles of pip, setuptools, venv, and make into one unified tool</li><li>The structure of Cargo.toml compared to pyproject.toml and requirements.txt</li><li>How dependency resolution, versioning, and lock files work in the Rust ecosystem</li></ul></blockquote><p>In Python, building and distributing software involves a constellation of tools: pip for installing packages, venv or virtualenv for isolation, setuptools or poetry for packaging, pyproject.toml or setup.py for configuration, and requirements.txt or poetry.lock for pinning dependencies. Each tool handles one piece of the puzzle, and getting them all to work together is a rite of passage.</p><p>Rust&#39;s answer is Cargo. One tool. It handles creating projects, compiling code, managing dependencies, running tests, building documentation, and publishing packages. Once you understand Cargo, you understand the entire Rust build story.</p><h2 id="what-cargo-replaces" tabindex="-1">What Cargo replaces <a class="header-anchor" href="#what-cargo-replaces" aria-label="Permalink to &quot;What Cargo replaces&quot;">​</a></h2><p>Here is a mapping of Python tools to their Cargo equivalents:</p><table tabindex="0"><thead><tr><th>Python tool/concept</th><th>Cargo equivalent</th><th>Command</th></tr></thead><tbody><tr><td><code>python -m venv</code></td><td>(not needed — see below)</td><td>—</td></tr><tr><td><code>pip install requests</code></td><td>Adding a dependency</td><td><code>cargo add reqwest</code></td></tr><tr><td><code>pip install -e .</code></td><td>Building the project</td><td><code>cargo build</code></td></tr><tr><td><code>python script.py</code></td><td>Running the project</td><td><code>cargo run</code></td></tr><tr><td><code>pytest</code></td><td>Running tests</td><td><code>cargo test</code></td></tr><tr><td><code>pyproject.toml</code></td><td><code>Cargo.toml</code></td><td>—</td></tr><tr><td><code>requirements.txt</code> / <code>poetry.lock</code></td><td><code>Cargo.lock</code></td><td>—</td></tr><tr><td><code>pip install build &amp;&amp; python -m build</code></td><td>Building a release</td><td><code>cargo build --release</code></td></tr><tr><td><code>twine upload</code></td><td>Publishing a crate</td><td><code>cargo publish</code></td></tr></tbody></table><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>There is no virtual environment in Rust because there is no need for one. Each Rust project has its own <code>target/</code> directory where compiled dependencies live. Dependencies are compiled per-project, not installed globally. You never have the &quot;wrong virtualenv activated&quot; problem because the concept does not exist. Each <code>Cargo.toml</code> file defines a self-contained project with its own dependency tree.</p></div><h2 id="cargo-toml-—-your-project-manifest" tabindex="-1">Cargo.toml — your project manifest <a class="header-anchor" href="#cargo-toml-—-your-project-manifest" aria-label="Permalink to &quot;Cargo.toml — your project manifest&quot;">​</a></h2><p>Every Rust project has a <code>Cargo.toml</code> at its root. This is the equivalent of <code>pyproject.toml</code>. Let&#39;s compare them side by side.</p><p><strong>Python — pyproject.toml:</strong></p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">project</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CDD6F4;">name </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;my-agent&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">version </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;0.1.0&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">description </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;A CLI coding agent&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">requires-python </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;&gt;=3.11&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">dependencies </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> [</span></span>
<span class="line"><span style="color:#A6E3A1;">    &quot;httpx&gt;=0.25.0&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#A6E3A1;">    &quot;click&gt;=8.0.0&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#A6E3A1;">    &quot;rich&gt;=13.0.0&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">]</span></span>
<span class="line"></span>
<span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">project.optional-dependencies</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CDD6F4;">dev </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> [</span><span style="color:#A6E3A1;">&quot;pytest&quot;</span><span style="color:#9399B2;">,</span><span style="color:#A6E3A1;"> &quot;ruff&quot;</span><span style="color:#9399B2;">]</span></span></code></pre></div><p><strong>Rust — Cargo.toml:</strong></p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">package</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CDD6F4;">name </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;my-agent&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">version </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;0.1.0&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">edition </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;2021&quot;</span></span>
<span class="line"><span style="color:#CDD6F4;">description </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;A CLI coding agent&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">dependencies</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CDD6F4;">reqwest </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> {</span><span style="color:#CDD6F4;"> version </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;0.12&quot;</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> features </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> [</span><span style="color:#A6E3A1;">&quot;json&quot;</span><span style="color:#9399B2;">]</span><span style="color:#9399B2;"> }</span></span>
<span class="line"><span style="color:#CDD6F4;">clap </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> {</span><span style="color:#CDD6F4;"> version </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;4&quot;</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> features </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> [</span><span style="color:#A6E3A1;">&quot;derive&quot;</span><span style="color:#9399B2;">]</span><span style="color:#9399B2;"> }</span></span>
<span class="line"><span style="color:#CDD6F4;">tokio </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> {</span><span style="color:#CDD6F4;"> version </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;1&quot;</span><span style="color:#9399B2;">,</span><span style="color:#CDD6F4;"> features </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> [</span><span style="color:#A6E3A1;">&quot;full&quot;</span><span style="color:#9399B2;">]</span><span style="color:#9399B2;"> }</span></span>
<span class="line"></span>
<span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">dev-dependencies</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CDD6F4;">assert_cmd </span><span style="color:#94E2D5;">=</span><span style="color:#A6E3A1;"> &quot;2&quot;</span></span></code></pre></div><p>The structure is similar, but there are a few Rust-specific concepts to notice:</p><ul><li><strong>edition</strong> — Rust editions (2015, 2018, 2021, 2024) allow the language to evolve without breaking old code. Always use the latest edition for new projects. This is roughly analogous to specifying <code>requires-python</code>.</li><li><strong>features</strong> — Rust crates can have optional features that you opt into. <code>reqwest = { version = &quot;0.12&quot;, features = [&quot;json&quot;] }</code> means &quot;install reqwest, and also enable its JSON serialization support.&quot; This is like Python extras (<code>httpx[http2]</code>), but far more granular.</li><li><strong>dev-dependencies</strong> — dependencies only used for testing, equivalent to putting pytest in <code>[project.optional-dependencies] dev</code>.</li></ul><h2 id="adding-dependencies" tabindex="-1">Adding dependencies <a class="header-anchor" href="#adding-dependencies" aria-label="Permalink to &quot;Adding dependencies&quot;">​</a></h2><p>In Python, you run <code>pip install requests</code> and then manually add it to your requirements file. In Rust, there is one command:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#6C7086;font-style:italic;"># Add a dependency — updates Cargo.toml automatically</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> add</span><span style="color:#A6E3A1;"> serde</span><span style="color:#A6E3A1;"> --features</span><span style="color:#A6E3A1;"> derive</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Add a dev-only dependency</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> add</span><span style="color:#A6E3A1;"> --dev</span><span style="color:#A6E3A1;"> mockall</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Remove a dependency</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> remove</span><span style="color:#A6E3A1;"> serde</span></span></code></pre></div><p>This is equivalent to <code>poetry add</code> or <code>pip install</code> + editing <code>pyproject.toml</code>, but built directly into the standard tool.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>If you use Poetry, you are already familiar with the <code>poetry add</code> workflow. Cargo&#39;s <code>cargo add</code> works the same way. If you use plain pip, note that Cargo always updates the manifest file — you never have a disconnect between what is installed and what is declared. The &quot;I forgot to update requirements.txt&quot; problem does not exist.</p></div><h2 id="cargo-lock-—-deterministic-builds" tabindex="-1">Cargo.lock — deterministic builds <a class="header-anchor" href="#cargo-lock-—-deterministic-builds" aria-label="Permalink to &quot;Cargo.lock — deterministic builds&quot;">​</a></h2><p>When you build your project, Cargo resolves all dependency versions and writes the exact resolved versions to <code>Cargo.lock</code>. This is equivalent to <code>poetry.lock</code> or <code>pip freeze &gt; requirements.txt</code>.</p><p>The rules for committing <code>Cargo.lock</code>:</p><ul><li><strong>Binary projects (applications):</strong> Always commit <code>Cargo.lock</code>. You want reproducible builds.</li><li><strong>Libraries:</strong> Do not commit <code>Cargo.lock</code>. Let downstream users resolve versions.</li></ul><p>Since our coding agent is a binary application, you will always commit <code>Cargo.lock</code>.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#6C7086;font-style:italic;"># After any dependency change, Cargo.lock is automatically updated</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> build</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">git</span><span style="color:#A6E3A1;"> add</span><span style="color:#A6E3A1;"> Cargo.lock</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">git</span><span style="color:#A6E3A1;"> commit</span><span style="color:#A6E3A1;"> -m</span><span style="color:#A6E3A1;"> &quot;update dependencies&quot;</span></span></code></pre></div><h2 id="the-build-process" tabindex="-1">The build process <a class="header-anchor" href="#the-build-process" aria-label="Permalink to &quot;The build process&quot;">​</a></h2><p>Here is where Cargo diverges most from Python. Python is interpreted — <code>python script.py</code> parses and executes your code directly. Rust is compiled — Cargo compiles your source code into a native binary before you can run it.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#6C7086;font-style:italic;"># Compile in debug mode (fast compilation, slow binary, with debug info)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> build</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Binary lands at: target/debug/my-agent</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Compile in release mode (slow compilation, fast binary, optimized)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> build</span><span style="color:#A6E3A1;"> --release</span></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Binary lands at: target/release/my-agent</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Compile and run in one step (most common during development)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> run</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Compile and run with release optimizations</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> run</span><span style="color:#A6E3A1;"> --release</span></span></code></pre></div><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>The closest Python equivalent to <code>cargo build --release</code> is using PyInstaller or Nuitka to bundle your Python application into a standalone binary. But those tools wrap the Python interpreter inside the binary. Rust compiles to actual machine code — no interpreter, no runtime, no dependencies. The resulting binary is a single file you can copy to any compatible machine and run directly.</p></div><h2 id="common-cargo-commands-cheat-sheet" tabindex="-1">Common Cargo commands cheat sheet <a class="header-anchor" href="#common-cargo-commands-cheat-sheet" aria-label="Permalink to &quot;Common Cargo commands cheat sheet&quot;">​</a></h2><p>Here are the Cargo commands you will use daily, mapped to their Python equivalents:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#6C7086;font-style:italic;"># Create a new project (like \`mkdir project &amp;&amp; cd project &amp;&amp; poetry init\`)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> new</span><span style="color:#A6E3A1;"> my-project</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> new</span><span style="color:#A6E3A1;"> my-library</span><span style="color:#A6E3A1;"> --lib</span><span style="color:#6C7086;font-style:italic;">  # creates a library instead of binary</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Build the project (like \`python -m build\` but for compilation)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> build</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Run the project (like \`python main.py\`)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> run</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Run tests (like \`pytest\`)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> test</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Check if code compiles without producing a binary (faster than build)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> check</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Format code (like \`black .\` or \`ruff format .\`)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> fmt</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Lint code (like \`flake8 .\` or \`ruff check .\`)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> clippy</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Generate and open documentation (like \`pdoc\` or \`sphinx\`)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> doc</span><span style="color:#A6E3A1;"> --open</span></span>
<span class="line"></span>
<span class="line"><span style="color:#6C7086;font-style:italic;"># Update dependencies to latest compatible versions (like \`pip install --upgrade\`)</span></span>
<span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> update</span></span></code></pre></div><p>The command you will use most during development is <code>cargo check</code>. It runs all the compiler checks without actually producing a binary, which is significantly faster than <code>cargo build</code>. Use it constantly to verify your code compiles as you work.</p><h2 id="workspaces-—-cargo-s-monorepo-support" tabindex="-1">Workspaces — Cargo&#39;s monorepo support <a class="header-anchor" href="#workspaces-—-cargo-s-monorepo-support" aria-label="Permalink to &quot;Workspaces — Cargo&#39;s monorepo support&quot;">​</a></h2><p>For larger projects, Cargo supports <em>workspaces</em> — a way to manage multiple related packages in one repository. This is similar to Python&#39;s monorepo patterns with tools like <code>hatch</code> or having multiple packages in one directory with a shared virtualenv.</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#6C7086;font-style:italic;"># Root Cargo.toml</span></span>
<span class="line"><span style="color:#9399B2;">[</span><span style="color:#CDD6F4;">workspace</span><span style="color:#9399B2;">]</span></span>
<span class="line"><span style="color:#CDD6F4;">members </span><span style="color:#94E2D5;">=</span><span style="color:#9399B2;"> [</span></span>
<span class="line"><span style="color:#A6E3A1;">    &quot;agent-core&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#A6E3A1;">    &quot;agent-cli&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#A6E3A1;">    &quot;agent-tools&quot;</span><span style="color:#9399B2;">,</span></span>
<span class="line"><span style="color:#9399B2;">]</span></span></code></pre></div><p>Each member is a full Cargo project with its own <code>Cargo.toml</code>, but they share a single <code>target/</code> directory and <code>Cargo.lock</code>. This means dependencies are resolved once for the entire workspace, and a <code>cargo build</code> at the root builds everything.</p><p>We will use a workspace structure for our coding agent as the project grows.</p><h2 id="the-crates-io-registry" tabindex="-1">The crates.io registry <a class="header-anchor" href="#the-crates-io-registry" aria-label="Permalink to &quot;The crates.io registry&quot;">​</a></h2><p>Rust packages are called <em>crates</em>, and the central registry is <a href="https://crates.io" target="_blank" rel="noreferrer">crates.io</a>. This is the equivalent of <a href="https://pypi.org" target="_blank" rel="noreferrer">PyPI</a>. When you add a dependency in <code>Cargo.toml</code>, Cargo downloads it from crates.io by default.</p><p>You can search for crates:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki catppuccin-mocha vp-code" tabindex="0"><code><span class="line"><span style="color:#89B4FA;font-style:italic;">cargo</span><span style="color:#A6E3A1;"> search</span><span style="color:#A6E3A1;"> serde</span></span></code></pre></div><p>Or browse <a href="https://crates.io" target="_blank" rel="noreferrer">crates.io</a> and <a href="https://lib.rs" target="_blank" rel="noreferrer">lib.rs</a> (a community-curated alternative frontend) to find packages.</p><div class="custom-block python"><p class="custom-block-title">Coming from Python</p><p>One cultural difference: Rust crate names on crates.io follow a strict one-name-per-crate policy with no namespacing. In Python, you have organizational packages like <code>google-cloud-storage</code> or <code>azure-storage-blob</code>. In Rust, names are globally unique and often short: <code>serde</code>, <code>tokio</code>, <code>clap</code>. Check crates.io before naming your own crate to avoid conflicts.</p></div><h2 id="key-takeaways" tabindex="-1">Key Takeaways <a class="header-anchor" href="#key-takeaways" aria-label="Permalink to &quot;Key Takeaways&quot;">​</a></h2><ul><li>Cargo is a single tool that replaces pip, venv, setuptools, poetry, pytest runner, and make — there is one way to do each operation</li><li><code>Cargo.toml</code> is equivalent to <code>pyproject.toml</code>, and <code>Cargo.lock</code> is equivalent to <code>poetry.lock</code> — always commit <code>Cargo.lock</code> for binary applications</li><li><code>cargo check</code> is your most-used command during development — it verifies your code compiles without producing a binary, which is faster than <code>cargo build</code></li><li>Rust has no virtual environments because each project is self-contained with its own compiled dependencies in the <code>target/</code> directory</li><li>Crates (Rust packages) live on crates.io, equivalent to PyPI, and are managed entirely through <code>Cargo.toml</code></li></ul>`,48)])])}const h=a(t,[["render",l]]);export{u as __pageData,h as default};
